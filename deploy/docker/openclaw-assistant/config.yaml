# OpenClaw Assistant — Configuration Template
#
# WARNING: This is a placeholder configuration file. The exact schema, required
# fields, and valid values must be verified against official OpenClaw platform
# documentation at https://docs.openclaw.ai before production use.
#
# All values below are placeholders and are NOT production-ready defaults.

# Assistant identity
assistant:
  name: "<PLACEHOLDER — set assistant display name>"

# HostAdapter connection
hostadapter:
  base_url: "<PLACEHOLDER — e.g., http://host.docker.internal:4319/v1>"
  token_file: "<PLACEHOLDER — e.g., /run/openclaw/hostadapter.token>"

# Workspace
workspace:
  path: "<PLACEHOLDER — e.g., /workspace>"

# Enabled tools and skills
tools:
  - "<PLACEHOLDER — reference to TOOLS.md tool definitions>"
skills:
  - "<PLACEHOLDER — reference to SYSTEM.md system instructions>"
