Timestamp: 2026-07-10T15-02

Command (1, failing MCP invocation): mcp__drm-copilot__run_poshqc_test (workspace_root = repo root)
EXIT_CODE: non-zero (tool returned `{"ok":false,"tool":"run_poshqc_test","summary":"Command exited with code 4294967295."}`)

Command (2, corrected-runsettings workaround invocation): `pwsh -NoProfile -File <scratchpad>\run-poshqc-test-139.ps1`, which imports `C:\Users\DanMoisan\AppData\Local\npm-cache\_npx\bc9f2e765aac2c41\node_modules\@danmoisan\drm-copilot-mcp\resources\powershell\PoshQC\PoshQC.psd1` and calls `Invoke-PoshQCTest -Root <repo worktree root> -SettingsPath <scratchpad>\pester.runsettings.corrected.139.psd1`. The corrected runsettings rewrites `CodeCoverage.Path` from the bundled extension's `drm-copilot`-repo-specific hook/script paths (which do not exist in this repo) to this repository's actual production PowerShell files enumerated under `scripts/**` (27 files: 21 `.ps1` + 6 `.psm1`, the full current inventory of `scripts/**`), with an empty `ExcludedPath` per the Coverage Exclusion Policy (no production file excluded).
EXIT_CODE: 0

Output Summary: Reproduced known defect (F11 #111, F16 #125, #135, #137): the bundled MCP `run_poshqc_test` invocation fails on every invocation in this repository (bundled settings reference `drm-copilot`-repo-only paths). Corrected-runsettings workaround invocation result: `Tests Passed: 370, Failed: 0, Skipped: 0, Inconclusive: 0, NotRun: 0`. Coverage result: `Covered 89.93% / 0%. 2,015 analyzed Commands in 30 Files.` Numeric repo-wide baseline command/line-coverage percentage: **89.93%** (line/command-coverage proxy; Pester's engine does not report a separate branch metric, hence the `/0%` in its own output). All 27 production PowerShell files under `scripts/**` were measured (none excluded); `scripts/Deploy.ps1` does not exist yet at this baseline point (it is created in Phase 2), so it is intentionally absent from this baseline's coverage `Path` list and will be added for the Phase 3 post-change measurement.
