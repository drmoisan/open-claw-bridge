# Phase 7 Acceptance Criteria Traceability Matrix

Timestamp: 2026-04-26T23-58

Source of acceptance criteria: `docs/features/active/2026-04-26-install-stale-hostadapter-detection/issue.md` `## Acceptance Criteria` section, AC-01 through AC-14.

| AC | Criterion (verbatim summary) | Plan Task ID | Test ID(s) | Test File | Status |
| --- | --- | --- | --- | --- | --- |
| AC-01 | Matching path: `Invoke-HostAdapterStart` skips launch when listener path matches bundle (case-insensitive). | P3-T1, P6-T1 | `Invoke-HostAdapterStart identity check.matching path.returns without invoking Invoke-HostAdapterProcess when bound listener path matches the bundle path (case-insensitive)` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-02 | Mismatch path: `Invoke-HostAdapterStart` throws with stale PID and observed path before preflight. | P3-T1, P6-T2 | `Invoke-HostAdapterStart identity check.stale process throws.throws with the stale PID and observed path when the listener path does not match the bundle path` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-03 | Preflight error includes `error.code` and `error.message` when body parses with non-null `error`. | P2-T2, P6-T3 | `Format-HostAdapterPreflightFailure.includes error.code and error.message when the body parses with a non-null error block` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-04 | Preflight error falls back to HTTP-status-only message when body is not JSON or has no error block. | P2-T2, P6-T4, P6-T6 | `Format-HostAdapterPreflightFailure.falls back to HTTP-status-only message when the body is not JSON`; `Format-HostAdapterPreflightFailure.falls back to HTTP-status-only message when the body has no error block`; `Assert-HostAdapterRespondingPreflight.throws with HTTP <code> when the body is not JSON` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-05 | Stage 7 `Assert-HostAdapterRespondingPreflight` accepts any HostAdapter envelope with `meta.adapterVersion`; bridge-down 502 does not fail Stage 7. | P2-T3, P4-T3, P6-T5 | `Assert-HostAdapterRespondingPreflight.accepts a 502 response when meta.adapterVersion is present (responsive but bridge down)` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-06 | Stage 8.5 `Assert-HostAdapterBridgeReadyPreflight` requires HTTP 200 + non-`starting`/non-`waiting_for_outlook` `data.state`; rollback calls `Invoke-MsixRemove` before throwing. | P2-T4, P4-T4, P6-T7, P6-T8, P6-T9 | `Assert-HostAdapterBridgeReadyPreflight.returns without throwing when status is 200 and data.state is a non-not-ready value`; `throws when data.state is waiting_for_outlook`; `throws when data.state is starting`; `Stage 8.5 rollback (Invoke-Stage8Point5BridgeReadyOrRollback).invokes Invoke-MsixRemove with the captured PackageFullName before the throw propagates`; `tolerates Invoke-MsixRemove failure and still throws the original bridge error` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-07 | `-SkipDocker` skips both new preflights. | P4-T3, P4-T4, P6-T10 | `scripts/Install.ps1.-SkipDocker regression for new preflights.does not invoke Assert-HostAdapterRespondingPreflight when -SkipDocker is supplied` | `tests/scripts/Install.Tests.ps1` | PASS |
| AC-08 | Stage 3 `-Force` does not call `Invoke-MsixRemove`; `Invoke-ComposeDown`, destination removal, install-record removal still run. | P5-T1, P5-T2, P5-T3, P6-T11, P6-T12 | `scripts/Install.ps1 — Force reinstall.-Force does not remove MSIX.does not invoke Invoke-MsixRemove during a -Force reinstall (record path)`; `does not invoke Invoke-MsixRemove during a -Force reinstall (destination-only path)`; `emits the [install:force-uninstall] Retaining installed MSIX log line exactly once in the -Force prior-install block` | `tests/scripts/Install.Force.Tests.ps1` | PASS |
| AC-09 | `Uninstall.ps1` line 51 still calls `Invoke-MsixRemove`. | P5-T4 | Static-text invariant verified during P5-T4: `Select-String -Path scripts/Uninstall.ps1 -Pattern 'Invoke-MsixRemove'` matches once on line 51 (`Uninstall.ps1`); confirmed in plan checkpoint. | `scripts/Uninstall.ps1` | PASS |
| AC-10 | After `-Force` reinstall, single `OpenClaw.MailBridge` package present with the new version (test-mockable seam). | P5-T3, P6-T13 | `scripts/Install.ps1 — Force reinstall.-Force does not remove MSIX.reports the captured PackageFullName from Invoke-MsixCapture after -Force reinstall` | `tests/scripts/Install.Force.Tests.ps1` | PASS |
| AC-11 | All existing tests pass without regressions. | P6-T14 | Full Pester run: 206 tests, 0 failures. | `tests/scripts/Install.Tests.ps1` + entire `tests/scripts` tree | PASS |
| AC-12 | New tests cover the documented scenarios. | P6-T1..P6-T13 | All 13 P6 test tasks contributed `It` blocks; see entries above for AC-01 through AC-10. | `tests/scripts/Install.Tests.ps1`, `tests/scripts/Install.Force.Tests.ps1` | PASS |
| AC-13 | Full PoshQC toolchain (format → analyze → test) passes without errors. | P7-T1, P7-T2, P7-T3 | `evidence/qa-gates/p7-format.md` (EXIT_CODE 0); `evidence/qa-gates/p7-analyze.md` (EXIT_CODE 0); `evidence/qa-gates/p7-test.md` (EXIT_CODE 0; 206/206). | `…/evidence/qa-gates/` | PASS |
| AC-14a | Repository-wide line coverage >= 80% excluding `scripts/Install.ps1` shim-guarded lines documented as a measurement artifact in `evidence/qa-gates/p7-coverage-delta.md`. | Remediation P5-T1 (post-remediation coverage gate); deferred refactor tracked at `docs/features/potential/install-test-fixture-coverage-refactor/`. | `evidence/qa-gates/p7r1-coverage-delta.2026-04-27T08-00.md` Verdict line `AC-14a: PASS`; baseline reference `evidence/qa-gates/p7-coverage-delta.md`. | `evidence/qa-gates/` | PASS |
| AC-14b | Changed-line coverage >= 90% on `scripts/Install.Helpers.psm1` and `scripts/Install.Preflight.psm1`. | Remediation P2-T1..P2-T4, P3-T1..P3-T2 (defensive-branch tests); P5-T1 (post-remediation coverage gate). | New `It` blocks in `tests/scripts/Install.Helpers.Tests.ps1` (`Get-ProcessMainModulePath defensive branch.returns $null and does not throw when Get-Process throws Win32Exception (access denied)`; `Get-ProcessMainModulePath defensive branch.returns $null when Get-Process returns a process whose MainModule is null`; `Get-ListeningProcessId no-listener path.returns $null when Get-NetTCPConnection yields an empty pipeline`; `Get-ListeningProcessId no-listener path.returns the OwningProcess as [int] when a single listener is present`) and `tests/scripts/Install.Preflight.Tests.ps1` (`Assert-HostAdapterBridgeReadyPreflight JSON-parse-failure.throws via Format-HostAdapterPreflightFailure when the response body is not JSON`; `Format-HostAdapterPreflightFailure boundary cases.*` x4). Numeric verdict recorded in `evidence/qa-gates/p7r1-coverage-delta.2026-04-27T08-00.md` (Helpers 94.59%, Preflight 90.74%). | `tests/scripts/Install.Helpers.Tests.ps1`, `tests/scripts/Install.Preflight.Tests.ps1` | PASS |

## Summary

- AC-01 through AC-13: PASS.
- AC-14a: PASS (Repository-wide line coverage >= 80% excluding the documented `scripts/Install.ps1` measurement-artifact carve-out; verdict in `evidence/qa-gates/p7r1-coverage-delta.2026-04-27T08-00.md`).
- AC-14b: PASS (Helpers 94.59%, Preflight 90.74%; both >= 90%).
- All 15 acceptance criteria (AC-01..AC-13 + AC-14a + AC-14b) are PASS following the P7r1 remediation pass.
