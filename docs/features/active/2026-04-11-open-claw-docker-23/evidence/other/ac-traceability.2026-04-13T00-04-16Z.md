# Acceptance Criteria Traceability

Timestamp: 2026-04-13T00:04:16Z
Plan Task: [P7-T9]
Plan Source: `docs/features/active/2026-04-11-open-claw-docker-23/plan.2026-04-12T16-58.md`
Traceability Scope: Current spec and user-story checkbox mapping for delivered HostAdapter and Core implementation work. Manual demo commands listed below are intended for execution and evidence capture during [P7-T10] through [P7-T12]; they are included here as explicit traceability targets and do not by themselves mark any checkbox complete.

## Spec Traceability

| Source | Criterion | Traceability |
| --- | --- | --- |
| Definition of Done 1 | Acceptance criteria in `issue.md`, `spec.md`, and `user-story.md` are traceable to named automated tests and/or explicit manual demo commands. | Manual inspection command: `Get-ChildItem 'docs/features/active/2026-04-11-open-claw-docker-23/evidence/other' -Filter 'ac-traceability.*.md' | Sort-Object LastWriteTimeUtc | Select-Object -Last 1`; heading verification command: `rg -n "^## (Spec Traceability|User Story Traceability|Unmapped Criteria)$" docs/features/active/2026-04-11-open-claw-docker-23/evidence/other/ac-traceability.2026-04-13T00-04-16Z.md` |
| Definition of Done 2 | Windows host plus Docker Desktop validation demonstrates that the current bridge/client stack still works and that HostAdapter/Core behavior matches this spec in safe and degraded bridge states. | Manual demo commands for [P7-T10]: `docker compose --env-file .env -f .\docker-compose.yml -f .\docker-compose.dev.yml up --build -d openclaw-core`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" http://localhost:4319/v1/status`; `curl.exe http://localhost:8080/health/ready`; degraded-state validation command target: stop HostAdapter, then run `curl.exe http://localhost:8080/api/status` and `curl.exe "http://localhost:8080/api/messages/recent?kind=all&limit=5"` |
| Definition of Done 3 | MSTest coverage exists for HostAdapter auth, UTC and range validation, `limit` handling, CLI exit-code mapping, response-envelope consistency, Core polling, and SQLite persistence/readback behavior. | Automated tests: `HostAdapter_should_return_401_for_request_without_authorization_header_and_not_invoke_cli`; `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `HostAdapter_should_return_400_invalid_request_for_non_utc_since_query`; `HostAdapter_should_return_400_invalid_request_when_calendar_end_is_not_later_than_start`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; `HostAdapter_should_map_not_found_bridge_errors_to_404`; `HostAdapter_should_map_outlook_unavailable_bridge_errors_to_503`; `HostAdapter_should_include_request_id_adapter_version_and_bridge_metadata_in_success_envelope`; `Core_message_poller_should_insert_new_rows_and_advance_the_message_cursor`; `Core_calendar_poller_should_persist_bounded_window_events_and_ingest_run_status`; coverage evidence command from [P7-T4]: `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "$resultsDir = 'TestResults/qa-csharp'; ..."` |
| Definition of Done 4 | Negative and boundary scenarios are covered by tests or demos, including missing token, invalid UTC input, `end <= start`, `limit > 250`, `NOT_FOUND`, `OUTLOOK_UNAVAILABLE`, and stale-cache serving behavior. | Automated tests: `HostAdapter_should_return_401_for_request_without_authorization_header_and_not_invoke_cli`; `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `HostAdapter_should_return_400_invalid_request_for_non_utc_since_query`; `HostAdapter_should_return_400_invalid_request_when_calendar_end_is_not_later_than_start`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; `HostAdapter_should_map_not_found_bridge_errors_to_404`; `HostAdapter_should_map_outlook_unavailable_bridge_errors_to_503`; `HostAdapter_should_return_200_for_degraded_cached_reads_and_mark_bridge_cache_as_stale`; `Core_readiness_should_return_503_on_hostadapter_outage_while_cached_reads_remain_available` |
| Definition of Done 5 | `README.md`, `docs/api-reference.md`, `docs/architecture-diagrams.md`, `docs/mailbridge-runbook.md`, and the feature-doc folder describe the host/container split, local-only exposure, and fallback path. | Manual inspection command: `rg -n "OpenClaw.HostAdapter|OpenClaw.Core|host.docker.internal|127.0.0.1|fallback to OpenClaw.MailBridge.Client" README.md docs/api-reference.md docs/architecture-diagrams.md docs/mailbridge-runbook.md docs/features/active/2026-04-11-open-claw-docker-23/*.md` |
| Definition of Done 6 | Structured logging and health/status endpoints are updated so operators can diagnose readiness, stale cache, and bridge unavailability without exposing sensitive content. | Automated tests: `Core_readiness_should_return_503_when_sqlite_cannot_initialize`; `Core_readiness_should_return_503_on_hostadapter_outage_while_cached_reads_remain_available`; `Core_status_should_report_last_successful_poll_time_bridge_freshness_and_stale_cache_state`; `Core_ui_should_surface_freshness_and_redaction_badges_without_message_body_or_attendee_leakage`; endpoint inspection command: `rg -n "/health/live|/health/ready|/api/status|requestId|bridge error code|CLI exit code" src/OpenClaw.HostAdapter src/OpenClaw.Core -g "*.cs"` |
| Definition of Done 7 | Final repo-standard toolchain pass succeeds for the touched C# assets: `csharpier .`, analyzer build, nullable/treat-warnings-as-errors build, and the relevant MSTest assemblies or solution test pass. | QA gate commands already recorded by [P7-T1] through [P7-T6]: `csharpier .`; `msbuild OpenClaw.MailBridge.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; `msbuild OpenClaw.MailBridge.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "$resultsDir = 'TestResults/qa-csharp'; ..."` |
| Seeded Test Conditions 1 | Unit coverage for HostAdapter token validation, UTC-only timestamp parsing, `bridgeId` pass-through handling, `limit` clamp/default behavior, status-cache TTL behavior, CLI exit-code/error-code mapping, and response-envelope consistency. | Automated tests: `HostAdapter_should_return_401_for_request_without_authorization_header_and_not_invoke_cli`; `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `HostAdapter_should_return_400_invalid_request_for_non_utc_since_query`; `HostAdapter_should_pass_url_decoded_bridge_id_through_unchanged_for_message_and_event_detail_routes`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; `HostAdapter_should_reuse_one_status_lookup_for_consecutive_data_requests_within_cache_ttl`; `HostAdapter_should_map_not_found_bridge_errors_to_404`; `HostAdapter_should_map_outlook_unavailable_bridge_errors_to_503`; `HostAdapter_should_include_request_id_adapter_version_and_bridge_metadata_in_success_envelope` |
| Seeded Test Conditions 2 | Unit coverage for Core polling cursors, SQLite upsert behavior for `messages` and `events`, stale-cache flag propagation, readiness-status computation, and cached read APIs filtering by `kind`, `start`, `end`, and `limit`. | Automated tests: `Core_message_poller_should_insert_new_rows_and_advance_the_message_cursor`; `Core_meeting_request_poller_should_preserve_kind_and_redaction_state`; `Core_calendar_poller_should_persist_bounded_window_events_and_ingest_run_status`; `Core_readiness_should_return_503_when_sqlite_cannot_initialize`; `Core_readiness_should_return_503_on_hostadapter_outage_while_cached_reads_remain_available`; `Core_status_should_report_last_successful_poll_time_bridge_freshness_and_stale_cache_state`; `Core_messages_recent_should_enforce_kind_and_limit_filters`; `Core_events_window_should_enforce_start_end_and_limit_filters` |
| Seeded Test Conditions 3 | Integration scenarios covering Windows HostAdapter to current CLI client wiring, container-to-host HTTP access through `host.docker.internal`, SQLite-backed stale-cache serving during HostAdapter outages, local-only port exposure on `127.0.0.1`, and preservation of sequential bridge access patterns. | Manual demo commands for [P7-T10] and [P7-T12]: `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" "http://localhost:4319/v1/messages?since=2026-04-12T00:00:00Z&limit=5"`; `docker compose --env-file .env -f .\docker-compose.yml -f .\docker-compose.dev.yml up --build -d openclaw-core`; `curl.exe http://localhost:8080/api/status`; `curl.exe "http://localhost:8080/api/messages/recent?kind=all&limit=5"`; local-only binding inspection command: `docker compose -f .\docker-compose.yml -f .\docker-compose.dev.yml config`; sequential-access evidence from automated test: `HostAdapter_should_reuse_one_status_lookup_for_consecutive_data_requests_within_cache_ttl` |
| Seeded Test Conditions 4 | Manual and automated contract checks for `GET /v1/status`, `GET /v1/messages`, `GET /v1/messages/{bridgeId}`, `GET /v1/meeting-requests`, `GET /v1/calendar`, `GET /v1/events/{bridgeId}`, `/health/live`, `/health/ready`, `/api/status`, `/api/messages/recent`, and `/api/events/window`. | Automated tests: `HostAdapter_should_include_request_id_adapter_version_and_bridge_metadata_in_success_envelope`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; `HostAdapter_should_pass_url_decoded_bridge_id_through_unchanged_for_message_and_event_detail_routes`; `Core_messages_recent_should_enforce_kind_and_limit_filters`; `Core_message_detail_should_return_cached_detail_with_unchanged_bridge_id`; `Core_events_window_should_enforce_start_end_and_limit_filters`; `Core_event_detail_should_return_cached_detail_with_unchanged_bridge_id`; `Core_readiness_should_return_503_when_sqlite_cannot_initialize`; `Core_status_should_report_last_successful_poll_time_bridge_freshness_and_stale_cache_state`; manual contract commands for [P7-T11]: `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" http://localhost:4319/v1/status`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" "http://localhost:4319/v1/messages?since=2026-04-12T00:00:00Z&limit=5"`; `curl.exe http://localhost:8080/health/live`; `curl.exe http://localhost:8080/health/ready`; `curl.exe http://localhost:8080/api/status`; `curl.exe "http://localhost:8080/api/messages/recent?kind=all&limit=5"`; `curl.exe "http://localhost:8080/api/events/window?start=2026-04-12T00:00:00Z&end=2026-04-14T00:00:00Z&limit=5"` |
| Seeded Test Conditions 5 | Operator troubleshooting coverage for missing token files, invalid bearer tokens, unavailable Outlook, bridge `waiting_for_outlook` or `starting` states, empty calendar-window results outside cache range, stale bridge cache, and degraded readiness. | Automated tests: `HostAdapter_should_return_401_for_request_without_authorization_header_and_not_invoke_cli`; `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `HostAdapter_should_map_outlook_unavailable_bridge_errors_to_503`; `HostAdapter_should_return_200_for_degraded_cached_reads_and_mark_bridge_cache_as_stale`; `Core_readiness_should_return_503_on_hostadapter_outage_while_cached_reads_remain_available`; manual operator commands for [P7-T12]: remove or redirect `%ProgramData%\OpenClaw\HostAdapter\adapter.token`, then run `curl.exe -H "Authorization: Bearer invalid-token" http://localhost:4319/v1/status`; run `curl.exe "http://localhost:4319/v1/calendar?start=2026-01-01T00:00:00Z&end=2026-01-02T00:00:00Z&limit=5"` with a valid token; inspect readiness via `curl.exe http://localhost:8080/health/ready` and `curl.exe http://localhost:8080/api/status` |

## User Story Traceability

| Source | Criterion | Traceability |
| --- | --- | --- |
| Acceptance Criteria 1 | The feature is implemented as an additive architecture extension: `OpenClaw.MailBridge`, `OpenClaw.MailBridge.Client`, `OpenClaw.MailBridge.Contracts`, existing scripts, and the existing named-pipe contract continue to work without breaking changes. | Manual validation commands: `msbuild OpenClaw.MailBridge.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:EnableNETAnalyzers=true /p:EnforceCodeStyleInBuild=true`; `msbuild OpenClaw.MailBridge.sln /t:Build /p:Configuration=Debug /p:Platform='Any CPU' /p:Nullable=enable /p:TreatWarningsAsErrors=true`; `dotnet test OpenClaw.MailBridge.sln -c Debug --no-build --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --results-directory TestResults/qa-csharp`; existing CLI fallback command: `dotnet run --project .\src\OpenClaw.MailBridge.Client\OpenClaw.MailBridge.Client.csproj --configuration Debug -- status` |
| Acceptance Criteria 2 | A Windows-side `OpenClaw.HostAdapter` is introduced as the only network seam for the container, and it maps exactly to the current six read-only bridge/client operations (`status`, recent messages, message by ID, recent meeting requests, calendar window, event by ID) using the existing CLI client before any direct pipe integration is considered. | Automated tests: `HostAdapter_should_pass_url_decoded_bridge_id_through_unchanged_for_message_and_event_detail_routes`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; manual contract commands for [P7-T11]: `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" http://localhost:4319/v1/status`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" "http://localhost:4319/v1/messages?since=2026-04-12T00:00:00Z&limit=5"`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" http://localhost:4319/v1/messages/<bridgeId>`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" "http://localhost:4319/v1/meeting-requests?since=2026-04-12T00:00:00Z&limit=5"`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" "http://localhost:4319/v1/calendar?start=2026-04-12T00:00:00Z&end=2026-04-14T00:00:00Z&limit=5"`; `curl.exe -H "Authorization: Bearer $(Get-Content $env:HOSTADAPTER_TOKEN_FILE)" http://localhost:4319/v1/events/<bridgeId>` |
| Acceptance Criteria 3 | A containerized `OpenClaw.Core` is introduced for the pre-MVP, with local-only published ports, non-root execution, read-only root filesystem, SQLite persistence, health endpoints, and UI/API behavior that surfaces bridge freshness and redaction state. | Automated tests: `Core_message_poller_should_insert_new_rows_and_advance_the_message_cursor`; `Core_calendar_poller_should_persist_bounded_window_events_and_ingest_run_status`; `Core_readiness_should_return_503_when_sqlite_cannot_initialize`; `Core_status_should_report_last_successful_poll_time_bridge_freshness_and_stale_cache_state`; `Core_ui_should_surface_freshness_and_redaction_badges_without_message_body_or_attendee_leakage`; manual container validation commands: `docker compose -f .\docker-compose.yml -f .\docker-compose.dev.yml config`; `docker compose --env-file .env -f .\docker-compose.yml -f .\docker-compose.dev.yml up --build -d openclaw-core`; `curl.exe http://localhost:8080/health/live`; `curl.exe http://localhost:8080/health/ready`; `curl.exe http://localhost:8080/api/status` |
| Acceptance Criteria 4 | The implementation preserves current privacy and safety behavior: safe mode remains the default, redacted fields stay redacted when required, and no token values, message bodies, or attendee details are logged. | Automated tests: `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `Core_ui_should_surface_freshness_and_redaction_badges_without_message_body_or_attendee_leakage`; manual inspection command: `rg -n "safe|message bodies|attendee|token" README.md docs/mailbridge-runbook.md src/OpenClaw.HostAdapter src/OpenClaw.Core -g "*.cs"` |
| Acceptance Criteria 5 | Prototype assets from `artifacts\gpt-web-dev\openclaw-pre-mvp-docker-bundle` are normalized to current repo reality before use, including project names, solution/workspace references, framework targets, docs, scripts, and C# test framework conventions. | Manual inspection commands: `rg -n "OpenClaw\\.HostAdapter|OpenClaw\\.Core|OpenClaw\\.MailBridge\\.Client|TargetFramework|MSTest|FluentAssertions" src tests README.md docs -g "*.cs" -g "*.csproj" -g "*.md"`; `rg -n "Project\\(\" OpenClaw.MailBridge.sln`; solution inclusion verification by [P1-T7] and test-project scaffolding by [P4-T1], [P5-T1] |
| Acceptance Criteria 6 | Docker, devcontainer, and deployment assets are merged carefully with existing repository files rather than overwriting them, and they remain consistent with the current solution structure and operational guidance. | Manual inspection commands: `docker compose -f .\docker-compose.yml -f .\docker-compose.dev.yml config`; `pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "$files = @( '.devcontainer/devcontainer.json', '.devcontainer/local/devcontainer.json', '.devcontainer/codespaces/devcontainer.json' ); foreach ($file in $files) { Get-Content -Path $file -Raw | ConvertFrom-Json | Out-Null }; Write-Output \"ValidatedFiles: $($files -join '; ')\""`; documentation verification command: `rg -n "OpenClaw.Core|host.docker.internal|local-only" README.md docs .devcontainer -g "*.md" -g "*.json"` |
| Acceptance Criteria 7 | New automated tests cover HostAdapter contract/error behavior and Core polling/cache behavior, while existing bridge tests and current repository quality gates continue to pass without regression. | Automated tests: all methods in `tests/OpenClaw.HostAdapter.Tests/*.cs` and `tests/OpenClaw.Core.Tests/*.cs`; final QA command from [P7-T4]: `dotnet test OpenClaw.MailBridge.sln -c Debug --no-build --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --results-directory TestResults/qa-csharp` |
| Acceptance Criteria 8 | Invalid and boundary inputs are handled deterministically: missing or invalid bearer tokens return `401`, malformed or non-UTC timestamps return `400`, `end <= start` returns `400`, missing items return `404`, and `limit` never exceeds `250` at the adapter boundary. | Automated tests: `HostAdapter_should_return_401_for_request_without_authorization_header_and_not_invoke_cli`; `HostAdapter_should_return_401_for_invalid_bearer_token_without_exposing_expected_token`; `HostAdapter_should_return_400_invalid_request_for_non_utc_since_query`; `HostAdapter_should_return_400_invalid_request_when_calendar_end_is_not_later_than_start`; `HostAdapter_should_apply_default_limit_and_reject_limit_values_above_maximum`; `HostAdapter_should_map_not_found_bridge_errors_to_404` |
| Acceptance Criteria 9 | Degraded bridge reads remain explicit rather than hidden: when the bridge serves cached data in a degraded state, HostAdapter responses still return `200` with freshness metadata, and Core surfaces stale-cache warnings through health/status APIs and the UI. | Automated tests: `HostAdapter_should_return_200_for_degraded_cached_reads_and_mark_bridge_cache_as_stale`; `Core_readiness_should_return_503_on_hostadapter_outage_while_cached_reads_remain_available`; `Core_status_should_report_last_successful_poll_time_bridge_freshness_and_stale_cache_state`; `Core_ui_should_surface_freshness_and_redaction_badges_without_message_body_or_attendee_leakage`; manual commands for [P7-T10] and [P7-T12]: `curl.exe http://localhost:8080/health/ready`; `curl.exe http://localhost:8080/api/status`; `curl.exe "http://localhost:8080/api/messages/recent?kind=all&limit=5"` |

## Unmapped Criteria

None. Every current checkbox in `spec.md` and `user-story.md` is mapped to at least one named automated test or one explicit manual demo or inspection command.
