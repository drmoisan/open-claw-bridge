# Remediation Plan: hostadapter-sendmail-com-send (#75)

**Entry Timestamp:** 2026-06-16T07-44
**Feature Folder:** `docs/features/active/hostadapter-sendmail-com-send-75`
**Base Branch:** `main` (merge-base `0cb7de6`)
**Head:** `feature/hostadapter-sendmail-com-send-75` (`269d3bb`)
**Work Mode:** full-feature
**Requirements source:** `spec.md` (AC-01..AC-11), `user-story.md`
**Remediation inputs:** `docs/features/active/hostadapter-sendmail-com-send-75/remediation-inputs.2026-06-16T07-44.md`

This plan addresses the FAIL/PARTIAL findings from the 2026-06-16T07-44 audit cycle (R-1, R-2, R-3). It conforms to `.claude/skills/atomic-plan-contract/SKILL.md`. Evidence paths resolve to `<FEATURE>/evidence/<kind>/` per the non-overridable evidence path clause. This is the target plan file, updated in place per the plan-path continuity contract.

Environment constraint (from remediation-inputs R-3): no live-Outlook host is available in this remediation environment. The two `[TestCategory("Integration")]` live-COM tests in `OutlookComMailSenderIntegrationTests.cs` `Assert.Inconclusive`-gate when Outlook is unavailable and therefore cannot run here. AC-06 and AC-10(a,b) are recorded as covered-by-design via an exception dossier; they are NOT marked unconditionally PASS. No attempt to run live COM is planned. CSharpier is invoked as the global tool form `csharpier format .` / `csharpier check .` (this repo has no local dotnet-tool manifest, so the `dotnet csharpier` driver is not used).

### Phase 0 — Policy reads and baseline capture

- [x] [P0-T1] Read the policy files in the required order from `policy-compliance-order` (`CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/architecture-boundaries.md`) and write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/other/phase0-instructions-read.remediation.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Capture the baseline format state by running `csharpier check .` and write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/remediation-baseline/format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T3] Capture the baseline test + coverage state by running `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --filter "TestCategory!=Integration"` and write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/remediation-baseline/test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including the numeric line coverage %, branch coverage %, and total passing test count (expected baseline 587 passing).
- [x] [P0-T4] Record the per-file line counts of all changed `.cs` files (`for f in $(git diff --name-only 0cb7de6..HEAD | grep -E '\.cs$'); do wc -l "$f"; done | sort -rn`) into `docs/features/active/hostadapter-sendmail-com-send-75/evidence/remediation-baseline/file-sizes.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, confirming the `tests/OpenClaw.MailBridge.Tests/MailBridgeProgramTests.cs` 573-line starting state.

### Phase 1 — Split the oversized test file (R-1)

- [x] [P1-T1] Extract the send_mail program-level test methods from `tests/OpenClaw.MailBridge.Tests/MailBridgeProgramTests.cs` into a new partial-class sibling `tests/OpenClaw.MailBridge.Tests/MailBridgeProgramTests.SendMail.cs` (same `namespace OpenClaw.MailBridge.Tests`, same `partial class MailBridgeProgramTests`). Move every send_mail test method verbatim with its attributes; add no new tests and weaken/skip/delete none. (R-1, AC-11)
- [x] [P1-T2] If `tests/OpenClaw.MailBridge.Tests/MailBridgeProgramTests.cs` still exceeds 500 lines after P1-T1, split the remaining pre-existing tests by concern into an additional partial-class sibling (e.g., `tests/OpenClaw.MailBridge.Tests/MailBridgeProgramTests.<Concern>.cs`) so that file and every new partial are each ≤ 500 lines; preserve all test methods and behavior. If P1-T1 alone brought `MailBridgeProgramTests.cs` ≤ 500 lines, complete this task with a no-op note in the P1-T3 evidence. (R-1, AC-11)
- [x] [P1-T3] Verify every changed `.cs` file is ≤ 500 lines: `for f in $(git diff --name-only 0cb7de6..HEAD | grep -E '\.cs$'); do wc -l "$f"; done | sort -rn`. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-file-sizes.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (confirm `MailBridgeProgramTests.cs` and each new partial are ≤ 500 and note whether P1-T2 was applied). (R-1, AC-11)
- [x] [P1-T4] Run the C# loop on the split to confirm behavior is preserved: `csharpier format .`, then `dotnet build OpenClaw.MailBridge.sln -c Debug -p:EnableNETAnalyzers=true -p:EnforceCodeStyleInBuild=true`, then `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --filter "TestCategory!=Integration"`. Restart from `csharpier format .` if any stage fails or auto-fixes files. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-split-loop.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming the passing test count did not regress (≥ 587). (R-1, AC-11)

### Phase 2 — Correct inaccurate evidence claim (R-2)

- [x] [P2-T1] Correct the file-size statements in `docs/features/active/hostadapter-sendmail-com-send-75/evidence/other/acceptance-criteria-map.md` to match the post-split `wc -l` reality: update the AC-11 row (currently asserts "no file > 500 lines") and the "No file exceeds 500 lines" bullet so they cite the split result and the actual largest changed test file after the split. The corrected text must match the P1-T3 evidence. (R-2)

### Phase 3 — Live-COM exception dossier (R-3)

- [x] [P3-T1] Write the fail-before / coverage exception dossier `docs/features/active/hostadapter-sendmail-com-send-75/evidence/regression-testing/fail-before-exception.2026-06-16T07-44.md` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. It MUST document: (a) the two gated tests `SendMail_with_one_recipient_should_create_a_sent_items_entry` and `SendMail_end_to_end_through_seam_should_complete_and_release_com` in `tests/OpenClaw.MailBridge.Tests/OutlookComMailSenderIntegrationTests.cs`; (b) the three `[ExcludeFromCodeCoverage]` live-COM members in `src/OpenClaw.MailBridge/OutlookComMailSender.cs` that these tests cover by design; (c) the environment gating (`Assert.Inconclusive` when Outlook is unavailable; no live Outlook host in this environment); (d) `WhyFailingRunImpossible:` (live-COM send requires a running Outlook host absent here; the tests cannot be made to fail-then-pass without it); (e) the repo precedent for environment-gated COM skips; (f) `SearchScope:`, `SearchPatterns:`, `SearchResult:` for the live-run search. Record AC-06 and AC-10(a,b) as covered-by-design via this dossier, NOT as unconditional PASS. Do not attempt to run live COM. (R-3, AC-06, AC-10a, AC-10b)

### Phase 4 — Final QA loop

- [x] [P4-T1] Run formatting: `csharpier format .`, then verify with `csharpier check .`. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-final-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Restart the loop from P4-T1 if `csharpier format .` changes any file.
- [x] [P4-T2] Run linting/analyzers: `dotnet build OpenClaw.MailBridge.sln -c Debug -p:EnableNETAnalyzers=true -p:EnforceCodeStyleInBuild=true`. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-final-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Restart from P4-T1 on failure.
- [x] [P4-T3] Run nullable type-check: `dotnet build OpenClaw.MailBridge.sln -c Debug -p:TreatWarningsAsErrors=true`. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-final-typecheck.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Restart from P4-T1 on failure.
- [x] [P4-T4] Run architecture-boundary verification: `dotnet build OpenClaw.MailBridge.sln -c Debug` and confirm the project-reference graph in `.claude/rules/architecture-boundaries.md` is unchanged (this remediation touches only test files and evidence). Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-final-architecture.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Restart from P4-T1 on failure.
- [x] [P4-T5] Run tests + coverage: `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --filter "TestCategory!=Integration"`. Write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-final-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording numeric post-change line and branch coverage; confirm no test-count regression (≥ 587 passing) and line ≥ 85% / branch ≥ 75%. Restart from P4-T1 on failure or file change.
- [x] [P4-T6] Verify coverage no-regression: write `docs/features/active/hostadapter-sendmail-com-send-75/evidence/qa-gates/remediation-coverage-delta.md` with `Timestamp:`, `Command:` (references P0-T3 baseline and P4-T5 post-change artifacts), `EXIT_CODE:`, `Output Summary:` reporting baseline line/branch %, post-change line/branch %, changed/new-code coverage, and a no-regression verdict (changed-line coverage not reduced; combined line ≥ 85% / branch ≥ 75%).

### Phase 5 — Reaudit handoff

- [ ] [P5-T1] Hand off to `feature-review` for reaudit at the exit timestamp, producing `code-review.<exit-ts>.md`, `feature-audit.<exit-ts>.md`, and `policy-audit.<exit-ts>.md` under the active feature folder. Exit condition: blocking_count == 0 (R-1 file-size FAIL resolved and AC-11 satisfied; R-2 evidence corrected; R-3 AC-06/AC-10(a,b) covered-by-design via the accepted exception dossier).
