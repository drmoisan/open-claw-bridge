# 2026-07-07-env-array-wrap-corruption - Plan (Cycle 2)

- **Issue:** #135
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-09T09-15
- **Status:** Ready for Preflight
- **Version:** 2.0 (second work cycle on branch `bug/env-array-wrap-corruption-135`, PR #136 already open)
- **Work Mode:** minor-audit (per `issue.md` metadata: `- Work Mode: minor-audit`)

## Required References

- General Coding Standards: `.claude/rules/general-code-change.md`
- General Unit Test Policy: `.claude/rules/general-unit-test.md`
- PowerShell Standards: `.claude/rules/powershell.md`
- Evidence Conventions: `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`
- Sole Requirements Source (minor-audit): `docs/features/active/2026-07-07-env-array-wrap-corruption-135/issue.md`, specifically its `## Acceptance Criteria` section. AC-1 through AC-6 were completed in work cycle 1 (already checked off) and cover the redundant-`@()` call-site defect. This cycle's requirements are AC-7 through AC-10, appended to the same section by Phase 0 of this plan (see P0-T6) and cover a distinct, newly-confirmed parameter-binding defect in `Write-EnvFileContent`. `spec.md` and `user-story.md` remain intentionally absent for this minor-audit work mode and are NOT required.

**All work must comply with these policies; do not duplicate their content here.**

## Conventions Used by This Plan

- `FEATURE` = `docs/features/active/2026-07-07-env-array-wrap-corruption-135`
- `<ts>` = ISO-8601 timestamp `yyyy-MM-ddTHH-mm` captured at artifact-creation time.
- All evidence artifacts live under `FEATURE/evidence/<kind>/` (canonical kinds used by this plan: `baseline/`, `regression-testing/`, `qa-gates/`). No evidence may be written under any `artifacts/` sub-path. Every command-step evidence artifact must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- This is a second, follow-on work cycle on the same feature folder, branch, and PR as cycle 1 (plan `plan.2026-07-07T11-17.md`, already executed and merged into the branch). Cycle 1's evidence files under `FEATURE/evidence/**` are left untouched; this cycle writes new, freshly-timestamped evidence files alongside them. Baselines captured in cycle 1 predate this cycle's fix and are not reused as this cycle's baseline.
- **PowerShell toolchain loop** (Phases 0 and 2): run `mcp__drm-copilot__run_poshqc_format` -> `mcp__drm-copilot__run_poshqc_analyze` -> `mcp__drm-copilot__run_poshqc_test` in that order, repo-wide. If any step fails or changes files, restart the loop from format. `EXIT_CODE: SKIPPED` is never a passing outcome for any command task in this plan; every Phase 2 command task is unconditional (no IN_SCOPE/OUT_OF_SCOPE branching).
- **Coverage tooling note (mandatory workaround):** `mcp__drm-copilot__run_poshqc_test` in coverage mode fails on every invocation in this repository because its bundled `pester.runsettings.psd1` hardcodes `CodeCoverage.Path` to files that exist only in the `drm-copilot` source repository, and the repo-local override path referenced in `.claude/rules/powershell.md` (`scripts/powershell/PoshQC/settings/pester.runsettings.psd1`) does not exist here. This is an established, reproduced defect (F11 `#111`, F16 `#125`), not something to re-diagnose. The numeric-coverage source for both baseline and final-QC tasks below is: import the bundled `PoshQC.psd1` module directly and call `Invoke-PoshQCTest -Root <repo> -SettingsPath <corrected>`, where `<corrected>` is a SCRATCHPAD-only copy of the bundled runsettings with `CodeCoverage.Path` rewritten to this repository's actual production PowerShell files under `scripts/**` (glob all `.ps1`/`.psm1`) and no `ExcludedPath` entry (per `.claude/rules/general-unit-test.md`'s Coverage Exclusion Policy, no production file may be excluded from measurement). Pester v5 emits command-level coverage only; record that percentage as the line/branch proxy, consistent with prior precedent. Do not write the corrected runsettings file into the repo tree.
- **AC tracking source:** per minor-audit mode, the sole AC source is `FEATURE/issue.md`'s `## Acceptance Criteria` section. This cycle's scope is AC-7 through AC-10, appended by P0-T6. No other document is treated as an AC source.
- **Change budget:** this fix touches exactly 1 production PowerShell file (`scripts/Publish.Env.psm1`, a single-attribute addition) and 2 test files (`tests/scripts/Publish.Env.Tests.ps1`, `tests/scripts/Publish.Tests.ps1`), within the direct-mode budget (up to 2 production files) and the per-batch cap (at most 3 production + 3 test files) in `.claude/rules/powershell.md`.
- **Explicit out-of-scope items:** `Get-EnvFileMap`, `Set-EnvFileValue`, `Read-EnvFileContent`, and `Step-PackageVersion` in `scripts/Publish.Env.psm1` remain unchanged (only `Write-EnvFileContent`'s parameter block gains one attribute). `scripts/Publish.ps1`, `scripts/New-MsixDevCert.ps1`, and `README.md` are not touched by this cycle. Adding any handling for an operator invoking the script with the literal string `-CertThumbprint 'OPENCLAW_CERT_THUMBPRINT'` is explicitly out of scope; that is an operator usage error, not a code defect, and must not be addressed by this plan.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md` at the repository root.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` is created (or appended to, if already created by a later task in this phase) recording that `CLAUDE.md` was read, with a `Timestamp:` field. **Note:** `CLAUDE.md` does not exist anywhere in this repository (confirmed via Glob and directory listing); this gap is recorded in the evidence artifact and escalated in the executor's final report.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/general-code-change.md` as read.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/general-unit-test.md` as read.
- [x] [P0-T4] Read `.claude/rules/powershell.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/powershell.md` as read.
- [x] [P0-T5] Finalize the Phase 0 policy-read evidence artifact covering P0-T1 through P0-T4.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` exists containing `Timestamp:`, `Policy Order:` (listing the four files in the exact order read: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`), and an explicit list of files read.
- [x] [P0-T6] Verify `FEATURE/issue.md` contains an explicit `## Acceptance Criteria` section (already present, with AC-1 through AC-6 checked off from cycle 1) and append four new, unchecked acceptance criteria for this cycle's fix, immediately after AC-6:
  - `AC-7: scripts/Publish.Env.psm1's Write-EnvFileContent -Content parameter carries [AllowEmptyString()] in addition to its existing [AllowEmptyCollection()], matching the pattern already used by Get-EnvFileMap and Set-EnvFileValue in the same module. No other function or file is changed.`
  - `AC-8: tests/scripts/Publish.Env.Tests.ps1 contains a test asserting Write-EnvFileContent -WhatIf accepts a -Content array containing an empty-string element without throwing a parameter-binding error.`
  - `AC-9: tests/scripts/Publish.Tests.ps1 contains an end-to-end regression test covering the stage 0c path (Read-EnvFileContent -> Set-EnvFileValue -> Write-EnvFileContent) with a blank-line .env fixture, asserting no parameter-binding error occurs and the blank line is preserved verbatim in the persisted content.`
  - `AC-10: The full PowerShell toolchain passes with no regression: PoshQC format is clean, PSScriptAnalyzer reports 0 errors, all Pester tests pass with no drop from this cycle's repo-wide baseline pass count, and coverage meets repository policy with no regression on the changed lines.`
  - Acceptance: `git diff docs/features/active/2026-07-07-env-array-wrap-corruption-135/issue.md` shows exactly four new `- [ ] AC-7` through `- [ ] AC-10` lines appended inside the `## Acceptance Criteria` section (after the existing AC-6 line), with no other line in the file changed, and the `## Acceptance Criteria` heading and AC-1 through AC-6 content remain unmodified.
- [x] [P0-T7] Capture the PowerShell format baseline: run `mcp__drm-copilot__run_poshqc_format` (workspace_root = repo root) against the current pre-fix repository state.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-format.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail status and count of files changed by the run).
- [x] [P0-T8] Capture the PowerShell analyze baseline: run `mcp__drm-copilot__run_poshqc_analyze` (workspace_root = repo root) against the current pre-fix repository state.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-analyze.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (diagnostic counts by severity, expected 0 errors).
- [x] [P0-T9] Capture the PowerShell test-and-coverage baseline: run `mcp__drm-copilot__run_poshqc_test`; when it fails on the known coverage-path defect, apply the corrected-runsettings workaround (`Invoke-PoshQCTest -Root <repo> -SettingsPath <scratchpad-corrected-runsettings>`, scoped to all `scripts/**` `.ps1`/`.psm1` files with no `ExcludedPath`) and record the numeric result. This must include a baseline run of the isolated repro `Write-EnvFileContent -Path <any> -Content @('A=1','','B=2') -WhatIf` confirming it currently throws (fail-before evidence for AC-7/AC-8).
  - Acceptance: `FEATURE/evidence/baseline/poshqc-test.<ts>.md` exists with `Timestamp:`, `Command:` (both the failing MCP invocation and the corrected-runsettings workaround invocation, plus the isolated repro command), `EXIT_CODE:` for each, and `Output Summary:` containing the pass/fail test counts, the numeric repo-wide baseline command/line-coverage percentage (no placeholders), and confirmation that the isolated repro throws "Cannot bind argument to parameter 'Content' because it is an empty string." on the pre-fix module.

### Phase 1 — Constrained Small-Path Implementation Placeholder

- [x] [P1-T1] Hand off the constrained implementation batch to `powershell-typed-engineer` via `atomic-executor`, scoped strictly to: (a) the single-attribute production edit to `Write-EnvFileContent`'s `-Content` parameter in `scripts/Publish.Env.psm1`, (b) one new `It` block in `tests/scripts/Publish.Env.Tests.ps1`, and (c) one new `It` block in `tests/scripts/Publish.Tests.ps1`. The delegation prompt must quote AC-7 through AC-10 verbatim from `FEATURE/issue.md`'s `## Acceptance Criteria` section (as appended by P0-T6) and must explicitly prohibit: any edit to `Get-EnvFileMap`, `Set-EnvFileValue`, `Read-EnvFileContent`, or `Step-PackageVersion`; any edit to `scripts/Publish.ps1`, `scripts/New-MsixDevCert.ps1`, or `README.md`; and any handling for an operator supplying the literal string `-CertThumbprint 'OPENCLAW_CERT_THUMBPRINT'`.
  - Acceptance: the delegation prompt sent to `atomic-executor`/`powershell-typed-engineer` names all three editable files (`scripts/Publish.Env.psm1` limited to the `Write-EnvFileContent` param block; `tests/scripts/Publish.Env.Tests.ps1`; `tests/scripts/Publish.Tests.ps1`), lists the four out-of-scope functions and three out-of-scope files/behaviors verbatim, and quotes AC-7 through AC-10 verbatim.
- [x] [P1-T2] Apply the confirmed fix: add `[AllowEmptyString()]` to the `-Content` parameter's attribute list in `Write-EnvFileContent` (`scripts/Publish.Env.psm1`, current lines 224-229), placed immediately after the existing `[AllowEmptyCollection()]` attribute, matching the ordering already used in `Get-EnvFileMap` and `Set-EnvFileValue`.
  - Acceptance: AC-7 — `git diff scripts/Publish.Env.psm1` shows exactly one added line (`[AllowEmptyString()]`) within the `Write-EnvFileContent` param block, and no other line in the file differs (in particular, `Get-EnvFileMap`, `Set-EnvFileValue`, `Read-EnvFileContent`, and `Step-PackageVersion` are byte-identical to their pre-fix state).
- [x] [P1-T3] Add one new `It` block to `tests/scripts/Publish.Env.Tests.ps1`'s existing `Context 'Write-EnvFileContent (file seam)'`, asserting that `Write-EnvFileContent -Path 'C:\fake\.env' -Content @('A=1', '', 'B=2') -WhatIf` does not throw a parameter-binding error, with `Set-Content` mocked and asserted not called (consistent with the existing `-WhatIf does not write` test's mocking pattern in the same file).
  - Acceptance: AC-8 — the new `It` block exists in `tests/scripts/Publish.Env.Tests.ps1`, asserts `{ Write-EnvFileContent -Path 'C:\fake\.env' -Content @('A=1', '', 'B=2') -WhatIf } | Should -Not -Throw`, asserts `Set-Content` was called 0 times, fails on the pre-fix module (parameter-binding error), and passes once P1-T2 is applied.
- [x] [P1-T4] Add one new `It` block to `tests/scripts/Publish.Tests.ps1`'s existing `Context 'env-driven version (AC-1, AC-2, AC-3)'`, mirroring the multi-line regression test already present in that context (the `.env` fixture with a comment line and two `KEY=value` lines) but adding a blank-line element to the fixture, exercising the full stage 0c path (`Read-EnvFileContent` -> `Set-EnvFileValue` -> `Write-EnvFileContent`, with `Write-EnvFileContent` mocked per the file's existing `BeforeEach` mock, which preserves the real function's parameter attributes for binding purposes).
  - Acceptance: AC-9 — the new `It` block exists, uses a fixture containing at least one blank-line element among comment/key lines (for example `@('# leading comment', 'OPENCLAW_PACKAGE_VERSION=1.0.2.0', '', 'OTHER_KEY=unchanged')`), asserts the script invocation does not throw, asserts the persisted content's line count equals the fixture's line count (the blank line is preserved, not dropped or collapsed), asserts the blank line is present verbatim in the persisted content, asserts the target key is updated in place with no duplicate, fails on the pre-fix module (parameter-binding error propagating from the mocked `Write-EnvFileContent` call), and passes once P1-T2 is applied.
- [x] [P1-T5] Confirm `scripts/Publish.ps1`, `scripts/New-MsixDevCert.ps1`, and `README.md` remain unchanged by this fix, and confirm `Get-EnvFileMap`, `Set-EnvFileValue`, `Read-EnvFileContent`, and `Step-PackageVersion` in `scripts/Publish.Env.psm1` remain unchanged.
  - Acceptance: `git diff scripts/Publish.ps1 scripts/New-MsixDevCert.ps1 README.md` returns no output, and `git diff scripts/Publish.Env.psm1` shows changes confined to the `Write-EnvFileContent` param block (the single added `[AllowEmptyString()]` line from P1-T2).
- [x] [P1-T6] Run the two edited test files in targeted mode (`mcp__drm-copilot__run_poshqc_test` scoped to `tests/scripts/Publish.Env.Tests.ps1` and `tests/scripts/Publish.Tests.ps1`, or the corrected-runsettings workaround scoped the same way if the MCP tool's coverage-path defect blocks the run) to confirm the P1-T2 through P1-T4 edits are internally consistent before the full-repo Phase 2 loop runs.
  - Acceptance: `FEATURE/evidence/regression-testing/targeted-verification.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming all tests in both files pass, including the two new regression tests added in P1-T3 and P1-T4.

### Phase 2 — Final QC Loop

- [x] [P2-T1] Run the final PowerShell format check: `mcp__drm-copilot__run_poshqc_format` (workspace_root = repo root) over the full repository, including the three edited files. If the run fails or modifies any file, apply the needed fix and restart this task before proceeding to P2-T2.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-format.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (pass status, 0 files changed on the recorded clean pass).
- [x] [P2-T2] Run the final PowerShell analyzer check: `mcp__drm-copilot__run_poshqc_analyze` (workspace_root = repo root) over the full repository. If any error-severity finding is reported, apply the needed fix and restart from P2-T1.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-analyze.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (diagnostic counts by severity, 0 errors).
- [x] [P2-T3] Run the final PowerShell test-and-coverage check: `mcp__drm-copilot__run_poshqc_test`; when it fails on the known coverage-path defect, apply the corrected-runsettings workaround (`Invoke-PoshQCTest -Root <repo> -SettingsPath <scratchpad-corrected-runsettings>`, scoped to all `scripts/**` `.ps1`/`.psm1` files with no `ExcludedPath`) over the full repository, including the three edited files and the two new regression tests. If any test fails, apply the needed fix and restart from P2-T1.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-test.<ts>.md` exists with `Timestamp:`, `Command:` (both the failing MCP invocation and the corrected-runsettings workaround invocation), `EXIT_CODE:` for each, and `Output Summary:` containing full pass/fail test counts (all tests passing, including the two new regression tests from P1-T3/P1-T4, and no drop from the P0-T9 baseline pass count) and the numeric repo-wide post-change command/line-coverage percentage (no placeholders).
- [x] [P2-T4] Produce the coverage delta/threshold verification comparing the P0-T9 baseline against the P2-T3 post-change values: confirm repo-wide coverage shows no regression versus baseline, confirm coverage for the one changed production file (`scripts/Publish.Env.psm1`) meets or exceeds its pre-change per-file coverage, confirm line coverage >= 85% and the command-coverage branch proxy >= 75% per `.claude/rules/general-unit-test.md`, and confirm no production PowerShell file was excluded from measurement.
  - Acceptance: `FEATURE/evidence/qa-gates/coverage-comparison.<ts>.md` reports baseline coverage, post-change coverage, and per-changed-file coverage as numeric values with an explicit PASS/FAIL against each threshold; any below-threshold, regressed, or unavailable value makes the outcome remediation-required, not PASS.
- [x] [P2-T5] Confirm AC-7 through AC-10 are satisfied: verify P2-T1 (`EXIT_CODE: 0`), P2-T2 (`EXIT_CODE: 0`, 0 errors), P2-T3 (all tests passing, `EXIT_CODE: 0` on the workaround invocation), and P2-T4 (PASS on all thresholds, no regression) all hold simultaneously, then check off AC-7 through AC-10 in `FEATURE/issue.md`'s `## Acceptance Criteria` section.
  - Acceptance: `FEATURE/evidence/qa-gates/ac10-acceptance-summary.<ts>.md` lists each of AC-7 through AC-10 with a PASS status and a pointer to its supporting evidence artifact (P1-T2 / P1-T3 / P1-T4 / P2-T1 through P2-T4 respectively); the four `- [ ]` checkboxes for AC-7 through AC-10 in `FEATURE/issue.md`'s `## Acceptance Criteria` section are changed to `- [x]` with criterion text unchanged; AC-1 through AC-6 remain checked and unmodified.

## Test Plan

- Unit (Pester v5): `tests/scripts/Publish.Env.Tests.ps1` (new empty-string-element `-WhatIf` regression test, P1-T3) and `tests/scripts/Publish.Tests.ps1` (new blank-line stage-0c end-to-end regression test, P1-T4) — both fail on the pre-fix module (missing `[AllowEmptyString()]`) and pass once the fix is applied.
- Targeted verification: `FEATURE/evidence/regression-testing/targeted-verification.<ts>.md` (P1-T6).
- Coverage evidence (PowerShell):
  - Baseline: `FEATURE/evidence/baseline/poshqc-test.<ts>.md` (P0-T9)
  - Post-change: `FEATURE/evidence/qa-gates/final-poshqc-test.<ts>.md` (P2-T3)
  - Comparison: `FEATURE/evidence/qa-gates/coverage-comparison.<ts>.md` (P2-T4)

## Open Questions / Notes

- No `spec.md` or `user-story.md` exists in this feature folder; per minor-audit mode this is expected and is not a blocker.
- Cycle 1's AC-1 through AC-6 (redundant-`@()` call-site defect in `scripts/Publish.ps1` / `scripts/New-MsixDevCert.ps1`) are already complete and are not re-verified by this plan beyond the P1-T5 unchanged-files check.
- `scripts/Publish.Env.psm1`'s `Get-EnvFileMap`, `Set-EnvFileValue`, `Read-EnvFileContent`, and `Step-PackageVersion` are confirmed correct and are not modified by this plan; only `Write-EnvFileContent`'s parameter attribute list is changed.
- Operator misuse of the literal string `OPENCLAW_CERT_THUMBPRINT` as a `-CertThumbprint` argument value is explicitly out of scope and is not addressed anywhere in this plan.
