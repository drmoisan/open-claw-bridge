# Feature Audit: graph-backed-adapter (#115)

**Audit Date:** 2026-07-02
**Work Mode:** `full-feature` (persisted marker `- Work Mode: full-feature` in `issue.md`)
**AC Sources:** `spec.md` `## Acceptance Criteria` and `user-story.md` (mirrored in `issue.md`)

## Scope and Baseline

- **Base branch (resolved):** `main` — evaluated as `origin/main` @ merge-base `ffbb1a077ade1b41ac01d292a0eda948db0479eb` (the local `main` ref is stale per the caller inputs; the PR-context artifacts resolve the same range).
- **Feature branch:** `feature/graph-backed-adapter-115` @ head `43e2709e3a465a54d56d30915b07e54527d3951f`.
- **Diff:** 51 files, +6444/-7 — 12 new production `.cs` under `src/OpenClaw.Core/CloudGraph/`, 1 modified `src/OpenClaw.Core/Program.cs` (the D8 backend-selection conditional), 19 new test `.cs` under `tests/OpenClaw.Core.Tests/CloudGraph/`, 18 feature docs/evidence Markdown, 2 agent-memory Markdown.
- **Evidence:** PR-context artifacts `artifacts/pr_context.summary.txt` / `artifacts/pr_context.appendix.txt` (refreshed per the caller); executor evidence under `docs/features/active/2026-07-02-graph-backed-adapter-115/evidence/`; reviewer re-run cobertura under `evidence/qa-gates/coverage-review/`; companion artifacts `policy-audit.2026-07-02T21-08.md` and `code-review.2026-07-02T21-08.md`.

## Acceptance Criteria Inventory

Both AC source files carry the same six criteria (verbatim mirror, reviewer-verified identical in `spec.md`, `user-story.md`, and the `issue.md` mirror). All six were checked `[x]` by the executor.

| # | Criterion (abbreviated) | Source state (spec.md / user-story.md) |
|---|---|---|
| AC-1 | `GraphHostAdapterClient` (`OpenClaw.Core.CloudGraph`) implements all nine `IHostAdapterClient` members; handler-level tests over a mocked `HttpMessageHandler` verify each endpoint's request shape: URL/query composition (`$select`/`$filter`/`$top`/paging), method, `Authorization: Bearer` from `IAppTokenProvider`, `client-request-id`, both Prefer headers, and the `getSchedule`/`sendMail` bodies (with `from` = principal when principal != assistant) | `[x]` / `[x]` |
| AC-2 | Response mapping from recorded Graph v1.0 payloads populates every wire-DTO field in the parity minimum set, including sensitivity (`private` -> 2), `iCalUId`/`seriesMasterId`, attendee-type partitioning into OR-5 JSON, importance, and `meetingMessageType`; mappers are pure static functions with CsCheck property tests for the enum and attendee-JSON mappings | `[x]` / `[x]` |
| AC-3 | 429/`Retry-After` handling is deterministic: `Retry-After` (delta-seconds or HTTP-date) takes precedence over exponential fallback, attempts bounded by configuration, all delays through the injected `TimeProvider` (verified with `FakeTimeProvider`; no wall-clock sleeps), exhaustion returns a retryable failure envelope carrying the request id in `ApiMeta` | `[x]` / `[x]` |
| AC-4 | Contract parity demonstrated: `HostAdapterSchedulingService` flows pass against `GraphHostAdapterClient` over recorded payloads; `OpenClaw.Core.Agent` production code unchanged; namespace-scoped NetArchTest rules assert the MailBridge-namespace ban with the Contracts carve-out, no COM interop, and Agent (incl. Runtime) independence from CloudGraph | `[x]` / `[x]` |
| AC-5 | Backend selection is opt-in: `AddGraphHostAdapterClient` takes effect only when `OpenClaw:GraphAdapter:Enabled` is true; with the flag absent or false the composition root registers `HostAdapterHttpClient` exactly as today (untouched-surface verification for the Program.cs default path and docker-compose) | `[x]` / `[x]` |
| AC-6 | Full C# toolchain passes (CSharpier, analyzers, nullable, architecture tests, MSTest + FluentAssertions + Moq + CsCheck) and coverage holds at line >= 85% / branch >= 75% with changed lines covered; no live Graph calls and no temporary files in any test; every new file <= 500 lines | `[x]` / `[x]` |

## Acceptance Criteria Evaluation

| # | Verdict | Evidence |
|---|---|---|
| AC-1 | **PASS** | Reviewer read the client core and all three partials: all nine members implemented in the specified namespace against the spec's interface-to-endpoint table (status probe, messages list/get, meeting requests, calendarView, event get, mailboxSettings, getSchedule POST, sendMail POST). Request-shape suites pin every element per endpoint: exact URL/query assertions incl. `$filter=receivedDateTime ge`/`$orderby`/`$top = min(limit, PageSize)`/full `$select` lists (`ListMessages_ComposesTheExactRequestShape`, `ListCalendarWindow_ComposesTheExactRequestShape`, `GetMailboxSettings_ComposesTheExactRequestShape`, escaped-id get routes); bearer sourcing per attempt (`BearerToken_SourcedFromTokenProviderOnEveryAttempt`); `client-request-id` echo and GUID fallback; both Prefer headers via `BuildReadRequest`; `GetFreeBusy_PostsTheSpecBodyFieldForField`; `SendMail_PrincipalDiffersFromAssistant_InjectsFrom` and `SendMail_PrincipalEqualsAssistant_OmitsFrom`. All pass in the reviewer's 616/616 run. |
| AC-2 | **PASS** | Mapper suites map recorded payloads across the full spec field tables: event private occurrence populates every row (`Map_PrivateOccurrence_PopulatesEveryFieldRowOfTheSpecTable`) incl. sensitivity `private` -> 2, `iCalUId`/`seriesMasterId`, and attendee partitioning with the `resource` type; message mapping covers importance and the full `meetingMessageType` vocabulary; missing-optional defaults and missing-required fail-fast (`id`, `start`, `end`, unknown zone, unparseable time) are DataRow-pinned. All mappers are pure static (reviewer-read: no I/O, no mutation, no clock). CsCheck properties: 8 mapper properties round-trip the four enum vocabularies against `SchedulingDtoMapper` inverses plus attendee-partition and recipient OR-5 `ParseAttendees` round-trips, and a ninth characterizes the D11 busy partition. |
| AC-3 | **PASS** | `GraphRequestExecutorRetryTests` (8 tests, all `FakeTimeProvider`-driven): `RetryAfterDeltaSeconds_IsHonoredExactly`, `RetryAfterHttpDate_IsHonoredRelativeToFakeUtcNow` (the HTTP-date form evaluated against the injected clock), `RetryAfter_TakesPrecedenceOverExponentialFallback`, the 1-2-4 default sequence, the `MaxDelaySeconds` cap, 429-then-success recovery, and both exhaustion classes returning retryable envelopes with the request id in `ApiMeta` and the attempt count in the message. The production delay path is exclusively `Task.Delay(delay, timeProvider, ct)` (reviewer-read line 138); reviewer hygiene scans confirm zero wall-clock sleeps in test code; attempts are bounded by `MaxAttempts` (validator-enforced 1-10). |
| AC-4 | **PASS** | `CloudGraphContractParityTests` (5 flows) drive the production `HostAdapterSchedulingService` through `GraphHostAdapterClient` over recorded payloads: mailbox-settings, D11 free/busy, calendar-window, sendMail failure-code propagation, and calendar-window failure degrading to the local backend's empty-list behavior. `OpenClaw.Core.Agent` production unchanged: reviewer-verified empty diff for `src/OpenClaw.Core/Agent` (and for the Contracts and MailBridge trees). `CloudGraphArchitectureBoundaryTests` implements all three D12 rules — MailBridge-namespace ban with the `OpenClaw.MailBridge.Contracts` carve-out via dependency inspection, COM-interop ban (`Microsoft.Office.Interop.Outlook`, `System.Runtime.InteropServices`), and Agent-partition (incl. Runtime) independence — passing in the reviewer's run. |
| AC-5 | **PASS** | Reviewer read the `Program.cs` hunk: the conditional selects `AddGraphHostAdapterClient` only when `builder.Configuration.GetValue<bool>("OpenClaw:GraphAdapter:Enabled")` is true; the else branch carries the previous `HostAdapterHttpClient` registration verbatim. `GraphBackendSelectionTests` pins both arms through `WebApplicationFactory` (flag absent -> local client; flag true -> Graph client) with red/green evidence (`graph-backend-selection-fail-before.2026-07-02T20-45.md`, EXIT 1 -> EXIT 0). Untouched-surface verification: reviewer-confirmed empty diff for all docker-compose files; executor evidence `untouched-surface.2026-07-02T20-56.md` EXIT 0. Program.cs coverage 100.00% line / 100.00% branch with both arms exercised. |
| AC-6 | **PASS** | Reviewer re-ran the toolchain at head `43e2709`: `csharpier check .` EXIT 0 (281 files); `dotnet build` 0 warnings / 0 errors (analyzers + nullable as errors); architecture suites + full tests 1063 passed / 0 failed / 5 pre-existing skips (MSTest + FluentAssertions + Moq + CsCheck stack); coverage pooled 92.34% line / 83.16% branch (>= 85%/75%), new-code CloudGraph slice 99.71%/90.65%, every instrumented new file at or above the per-file gates, modified Program.cs 100%/100% (changed lines covered; both pooled dimensions improved over baseline 91.26%/81.38%; untouched packages byte-identical). No live Graph calls (mocked handlers only; reviewer grep for `HttpClientHandler`/bare `HttpClient`: zero) and no temp files (raw-string fixtures; reviewer grep: zero). File sizes: production max 269, Program.cs 344, test max 344 — all <= 500. The async-body and auto-property instrumentation exclusions are pre-existing runsettings behavior, stated explicitly and behaviorally verified in the policy audit Section 8. |

## Summary

All six acceptance criteria evaluate to **PASS** against the resolved baseline (`origin/main` @ `ffbb1a0`), verified by reviewer re-execution of the full toolchain at branch head, independent per-file cobertura re-parsing (line AND branch for all 13 changed production files), direct reading of the production sources and the load-bearing test suites, and empty-diff verification of every surface the spec declares untouched (Agent production, wire contracts, MailBridge, the local client, docker-compose). The adapter is delivered exactly as specified and is dark by default; the D10 `meetingMessageType` `$select` acceptance remains a live-tenant verification item for F17, pinned by handler tests in the shipped form. Companion artifacts: `policy-audit.2026-07-02T21-08.md` (FULLY COMPLIANT, no Blocking findings) and `code-review.2026-07-02T21-08.md` (Approve, two Minor optional-hardening findings and three informational observations). Remediation is not required; no `remediation-inputs` artifact was produced.

**Go/No-Go recommendation: Go — ready for PR.**

## Acceptance Criteria Check-off

Per `acceptance-criteria-tracking`, the reviewer checks off PASS criteria in the authoritative source files. All six criteria were already checked `[x]` by the executor in both authoritative sources (`spec.md`, `user-story.md`) and in the `issue.md` mirror. The reviewer independently confirmed each PASS verdict above; no source-file edits were required and no new items were checked off by this review. No phantom criteria were added; criterion text is unmodified.

### Acceptance Criteria Status

- Source: `docs/features/active/2026-07-02-graph-backed-adapter-115/spec.md` and `docs/features/active/2026-07-02-graph-backed-adapter-115/user-story.md` (mirrored in `issue.md`)
- Total AC items: 6 (per source file)
- Checked off (delivered): 6
- Remaining (unchecked): 0
- Items remaining: none
