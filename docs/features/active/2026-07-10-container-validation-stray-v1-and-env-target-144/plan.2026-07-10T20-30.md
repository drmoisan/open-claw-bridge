# container-validation-stray-v1-and-env-target (Plan)

- **Issue:** #144
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-10T20-30
- **Status:** Draft (pending preflight validation)
- **Version:** 2.0 (scope expanded by maintainer decision: AC6 documentation + AC7 validation-accuracy added; AC1–AC5 tasks retained)
- **Work Mode:** minor-audit (per `issue.md` metadata: `- Work Mode: minor-audit`)

**DIRECTIVE: MINIMAL-AUDIT PLAN REQUIRED** — this plan uses `docs/features/active/2026-07-10-container-validation-stray-v1-and-env-target-144/issue.md` as the sole requirements source. The explicit `## Acceptance Criteria` section of that file (AC1–AC7) is the only acceptance-criteria source. `spec.md` and `user-story.md` are not required and must not exist in this feature folder.

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Evidence location invariant:** All evidence artifacts resolve to `docs/features/active/2026-07-10-container-validation-stray-v1-and-env-target-144/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Non-canonical `artifacts/` evidence paths (e.g. `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`) are forbidden; if a caller supplies one, it is rejected and replaced with the canonical path, recorded as `EVIDENCE_LOCATION_OVERRIDE_REJECTED: <supplied path> replaced with <canonical path>`.

## Required References

- General Coding Standards: `.claude/rules/general-code-change.md`
- General Unit Test Policy: `.claude/rules/general-unit-test.md`
- PowerShell Standards: `.claude/rules/powershell.md`
- Evidence Conventions: `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`
- AC source (authoritative, minor-audit mode): `docs/features/active/2026-07-10-container-validation-stray-v1-and-env-target-144/issue.md`, `## Acceptance Criteria` section (7 items, AC1–AC7 below).

**All work must comply with these policies; do not duplicate their content here.**

## Acceptance Criteria (verbatim from `issue.md`, numbered for traceability)

- AC1 — The in-container HostAdapter probe in `OpenClawContainerValidation.psm1` requests the root `/status` path (`http://host.docker.internal:4319/status`) with no `/v1` segment. Verified by a Pester test asserting the probe shell command contains `/status` and does not contain `/v1`.
- AC2 — The `HostAdapterInContainer` result's `ExpectedCondition` text references `/status` (no `/v1`). Verified by a Pester assertion on the emitted result.
- AC3 — `Invoke-OpenClawContainerPathValidation.ps1` resolves its default `-EnvFilePath` to the version-neutral deployed operator env file (`%LOCALAPPDATA%\OpenClaw\operator-config\.env`) when that file exists, and falls back to `./.env` when it does not. The resolution is a testable pure helper (no ambient side effects) verified by Pester for both the present and absent cases.
- AC4 — No regression: the full `tests/scripts` Pester suite passes and PoshQC format + analyze report clean on all changed PowerShell files in a single pass.
- AC5 — No HostAdapter routing change: no production code under `src/OpenClaw.HostAdapter/**` is modified; the fix is confined to the validation tooling and documentation.
- AC6 — Dashboard-access documentation is corrected. `README.md` (the dashboard section around line 706) and `docs/mailbridge-runbook.md` (Dashboard access section) no longer claim the dashboard "reads the token without an operator paste step" or that the token "is the only credential the dashboard accepts." They document the accurate upstream (OpenClaw 2026.6.11) procedure: open `http://127.0.0.1:18789/#token=<OPENCLAW_GATEWAY_TOKEN>` (token in the URL fragment) or paste the token in Control UI settings; and the device re-pair reset for when the agent container is recreated (`openclaw devices clear` inside the agent container + clear browser site data for the origin + reopen the fragment URL). A note records that `OPENCLAW_AGENT_IMAGE` tracks a floating upstream tag, so the auth flow can change across upgrades.
- AC7 — Dashboard validation reports auth accurately. Because the gateway exposes no auth-gated HTTP endpoint (verified: `/`, `/readyz`, `/healthz`, `/control`, `/gateway/status` return 200 without a token; `/api/*` 404) and operator auth is WebSocket + device-pairing, the `AgentDashboard` probe's `ExpectedCondition`/summary state that a 200 on `/` confirms the Control UI is served but does NOT verify operator authentication (which requires the `#token=` fragment). Additionally, a check verifies `OPENCLAW_GATEWAY_TOKEN` is present and non-empty inside the running agent container (via the docker seam), distinct from the `.env`-file presence check. Verified by Pester with the docker/HTTP seam mocked for present and absent cases. No fragile WebSocket/pairing handshake is added.

## Explicit Scope and Non-Goals

- Production PowerShell files (2, within the direct-mode 2-file budget in `.claude/rules/powershell.md`): `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` (modified), `scripts/Invoke-OpenClawContainerPathValidation.ps1` (modified). Both files are edited for AC1–AC3 and again for AC7; the file set does not grow.
- Documentation files (2, Markdown — exempt from the 500-line cap per `.claude/rules/general-code-change.md`; not counted against the production-code batch cap): `README.md` (modified), `docs/mailbridge-runbook.md` (modified). AC6 only.
- Test files (5): `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1` (modified — AC1/AC2), `tests/scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.EnvFilePathDefault.Tests.ps1` (new — AC3), `tests/scripts/Invoke-OpenClawContainerPathValidation.EnvFilePathDefault.Tests.ps1` (new — AC3), `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` (modified — AC7 aggregation count + fake-docker token-exec handler), `tests/scripts/Invoke-OpenClawContainerPathValidation.GatewayTokenInContainer.Tests.ps1` (new — AC7).
- **Batch accounting (per-batch cap <= 3 production + <= 3 test):** Phase 1 executes as two batches. Batch A (AC1–AC3): 2 production PS files + 3 test files (HostAdapter.Tests, the two new EnvFilePathDefault test files). Batch B (AC6 + AC7): the same 2 production PS files + 2 test files (Invoke-OpenClawContainerPathValidation.Tests modified, GatewayTokenInContainer.Tests new) + 2 Markdown documentation files (cap-exempt). Each batch respects the per-batch cap.
- Non-goals (must remain byte-identical, verified at Phase 2): everything under `src/OpenClaw.HostAdapter/**` (AC5); `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1`; `tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1`; `tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1`. (The `Readyz` and `TokenPresence` tests invoke the entry script fully; their fake dockers already return the default "unexpected docker command" for `compose exec`, so the pre-existing `HostAdapterInContainer` probe and the new `GatewayTokenInContainer` probe both report Unexpected there, but those tests assert only their own probe and no aggregate count, so they need no change — confirmed by reading them during planning.)
- No changes to `Install.Preflight.psm1` / `Install.Tests.ps1` / `Install.Preflight.Tests.ps1`, which contain unrelated `/v1/status` references for a different HostAdapter preflight probe (out of scope for issue #144).

## Location Correction (delegation-prompt reconciliation)

The scope-expansion message states the `AgentDashboard` probe is "In OpenClawContainerValidation.psm1". The probe function `Invoke-OpenClawAgentDashboardEndpointValidation` is in fact defined in the entry script `scripts/Invoke-OpenClawContainerPathValidation.ps1` (lines 230–241), not in the module. AC7's `ExpectedCondition`/summary edit is therefore applied in the entry script where the function lives. The new in-container token check (`Test-OpenClawGatewayTokenInContainer`) is added to the module `OpenClawContainerValidation.psm1`, matching the module's existing container-probe pattern (`Invoke-OpenClawHostAdapterInContainerProbe`, `Test-OpenClawGatewayTokenPresence`). Both files are already in scope, so the 2-production-file budget is unchanged.

## Line-Count Check (measured during planning, 2026-07-10)

- `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`: 362 lines (well under the 500-line cap; room for the AC3 helpers and the AC7 `Test-OpenClawGatewayTokenInContainer` probe).
- `scripts/Invoke-OpenClawContainerPathValidation.ps1`: 301 lines (well under cap; room for the AC3 wiring and AC7 aggregation).
- `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1`: 171 lines (well under cap).
- `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1`: 362 lines (modified for AC7; well under cap after the small aggregation-count + fake-docker additions).
- `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1`: 91 lines (unmodified).
- `README.md` and `docs/mailbridge-runbook.md`: Markdown documentation, exempt from the 500-line cap.
- No PowerShell file in scope is at or near the 500-line cap; no sibling-ADD split is required.

## Conventions Used by This Plan

- `FEATURE` = `docs/features/active/2026-07-10-container-validation-stray-v1-and-env-target-144`
- `<ts>` = `2026-07-10T20-30` (this plan's own timestamp, reused as the evidence-artifact suffix per established repo convention; execution may append a more precise timestamp if artifacts are captured at a materially later time).
- All evidence artifacts live under `FEATURE/evidence/<kind>/` (kinds used: `baseline/`, `qa-gates/`, `other/`, `issue-updates/`). No evidence may be written under any `artifacts/` sub-path. Every command-step evidence artifact must contain `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- **PowerShell toolchain loop**: `mcp__drm-copilot__run_poshqc_format` -> `mcp__drm-copilot__run_poshqc_analyze` -> `mcp__drm-copilot__run_poshqc_test`, in that order. If any step fails or changes files, restart the loop from format.
- **PowerShell coverage tooling note (established workaround):** `mcp__drm-copilot__run_poshqc_test` in coverage mode fails on every invocation in this repository because its bundled `pester.runsettings.psd1` hardcodes `CodeCoverage.Path` to files that exist only in the `drm-copilot` source repository (reproduced defect: `#111`, `#125`, `#135`, `#137`, `#139`, `#142`). The numeric-coverage source for both baseline and final-QC PowerShell tasks is: import the bundled `PoshQC.psd1` module directly and call `Invoke-PoshQCTest -Root <repo> -SettingsPath <corrected>`, where `<corrected>` is a SCRATCHPAD-only copy of the bundled runsettings with `CodeCoverage.Path` rewritten to this repository's production PowerShell files under `scripts/**` (no production `ExcludedPath` entry per the Coverage Exclusion Policy). Record both the failing MCP invocation and the corrected-runsettings invocation in the evidence artifact.
- **New AC3 helper seam:** `Get-OpenClawOperatorEnvFilePath -LocalAppDataPath <string>` (default `$env:LOCALAPPDATA`) and `Resolve-OpenClawDefaultEnvFilePath -OperatorEnvFilePath <string> -FallbackEnvFilePath <string>`, added to and exported from `OpenClawContainerValidation.psm1`, matching the module's existing pure-helper pattern (`Get-OpenClawEnvFileMap`, `Get-OpenClawEndpointUri`). Neither parameter is named `Args`.
- **New AC7 probe seam:** `Test-OpenClawGatewayTokenInContainer -DockerExecutablePath <string> -AgentContainerName <string>`, added to and exported from `OpenClawContainerValidation.psm1`, routing through the existing `Invoke-OpenClawDockerCommand` docker seam (so Pester mocks the same seam already used by `Invoke-OpenClawHostAdapterInContainerProbe`). Neither parameter is named `Args`.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read `CLAUDE.md` at the repository root.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` is created (or appended to) recording that `CLAUDE.md` was read, with a `Timestamp:` field.
- [x] [P0-T2] Read `.claude/rules/general-code-change.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/general-code-change.md` as read.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/general-unit-test.md` as read.
- [x] [P0-T4] Read `.claude/rules/powershell.md`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` lists `.claude/rules/powershell.md` as read.
- [x] [P0-T5] Confirm `FEATURE/issue.md` contains an explicit `## Acceptance Criteria` section (AC1–AC7) and that `spec.md`, `user-story.md`, and `research.md` are absent from `FEATURE/`.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` records the confirmed presence of `## Acceptance Criteria` (7 items) in `issue.md` and the confirmed absence of `spec.md`/`user-story.md`/`research.md` in `FEATURE/`.
- [x] [P0-T6] Finalize the Phase 0 policy-read evidence artifact covering P0-T1 through P0-T5.
  - Acceptance: `FEATURE/evidence/baseline/phase0-instructions-read.<ts>.md` exists containing `Timestamp:`, `Policy Order:` (the four files in the exact order read: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`), and an explicit list of files read.
- [x] [P0-T7] Capture the PowerShell format baseline: run `mcp__drm-copilot__run_poshqc_format` against the current pre-change state of `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` and `scripts/Invoke-OpenClawContainerPathValidation.ps1`.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-format.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail status and count of files changed by the run).
- [x] [P0-T8] Capture the PowerShell analyze baseline: run `mcp__drm-copilot__run_poshqc_analyze` against the same two files' current pre-change state.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-analyze.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (diagnostic counts by severity, expected 0 errors).
- [x] [P0-T9] Capture the PowerShell test baseline scoped to the four existing `Invoke-OpenClawContainerPathValidation*.Tests.ps1` files: run `mcp__drm-copilot__run_poshqc_test` targeted to `tests/scripts/Invoke-OpenClawContainerPathValidation*.Tests.ps1`.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-test.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail test counts for the four existing test files; the current `HostAdapterInContainer` regex assertions on `v1/status` and the `SupportingDiagnostics` count of 14 are expected to pass against the pre-change production code).
- [x] [P0-T10] Capture the PowerShell test-and-coverage baseline: run `mcp__drm-copilot__run_poshqc_test` in coverage mode (record the known bundled-runsettings failure), then run the corrected-runsettings `Invoke-PoshQCTest` workaround (per Conventions) scoped to `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` and `scripts/Invoke-OpenClawContainerPathValidation.ps1`.
  - Acceptance: `FEATURE/evidence/baseline/poshqc-coverage.<ts>.md` exists with `Timestamp:`, `Command:` (both the failing MCP invocation and the corrected-runsettings invocation), `EXIT_CODE:` for each, and `Output Summary:` containing the numeric baseline line-coverage % and branch-coverage % for the two production files (no placeholders).

### Phase 1 — Constrained Small-Path Implementation (AC1, AC2, AC3, AC6, AC7)

- [x] [P1-T1] Record the implementation scope-confirmation handoff note.
  - Acceptance: `FEATURE/evidence/other/scope-confirmation.<ts>.md` exists listing exactly the 2 in-scope production PS files, the 2 in-scope documentation files, and the 5 in-scope test files named in "Explicit Scope and Non-Goals" above, records the two-batch grouping (Batch A: AC1–AC3; Batch B: AC6+AC7), and states the AC5 constraint that no path under `src/OpenClaw.HostAdapter/**` may be modified.

**Batch A — AC1, AC2, AC3 (production: 2 PS files; tests: 3 files)**

- [x] [P1-T2] [AC1] In `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`, in `Invoke-OpenClawHostAdapterInContainerProbe`, change the probe shell command string from `http://host.docker.internal:4319/v1/status` to `http://host.docker.internal:4319/status` (the `$shellCommand` assignment, currently at line 282).
  - Acceptance: `git diff scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` shows the `$shellCommand` literal changed from `.../v1/status` to `.../status`; Grep of the function body for `/v1/` returns zero matches.
- [x] [P1-T3] [AC2] In the same file and function, change the `ExpectedCondition` string passed to `Get-OpenClawValidationResult` (currently `'docker compose exec against the agent container reports HTTP 200 for /v1/status with bearer token'` at line 303) to reference `/status` instead of `/v1/status`.
  - Acceptance: Grep of `OpenClawContainerValidation.psm1` for the `ExpectedCondition` string used by `Invoke-OpenClawHostAdapterInContainerProbe` shows `/status` and does not show `/v1`.
- [x] [P1-T4] [AC3] Add a new exported function `Get-OpenClawOperatorEnvFilePath` to `OpenClawContainerValidation.psm1`: `[CmdletBinding()] [OutputType([string])] param([string]$LocalAppDataPath = $env:LOCALAPPDATA)`. When `$LocalAppDataPath` is null/whitespace, return `$null`; otherwise return `Join-Path $LocalAppDataPath 'OpenClaw/operator-config/.env'`. The function makes no `Test-Path`/`Get-Content` call (pure path composition only).
  - Acceptance: AC3 (composition half) — the function exists with the documented signature and default, and contains no `Test-Path` or `Get-Content` call.
- [x] [P1-T5] [AC3] Add a second exported function `Resolve-OpenClawDefaultEnvFilePath` to `OpenClawContainerValidation.psm1`: `[CmdletBinding()] [OutputType([string])] param([string]$OperatorEnvFilePath, [Parameter(Mandatory = $true)][string]$FallbackEnvFilePath)`. When `$OperatorEnvFilePath` is null/whitespace, return `$FallbackEnvFilePath`. Otherwise, when `Test-Path -LiteralPath $OperatorEnvFilePath` is `$true`, return `$OperatorEnvFilePath`; otherwise return `$FallbackEnvFilePath`.
  - Acceptance: AC3 (resolution half) — the function exists with the documented signature and both branches (present/absent) implemented exactly as specified.
- [x] [P1-T6] [AC3] Update the `Export-ModuleMember -Function` list in `OpenClawContainerValidation.psm1` to add `'Get-OpenClawOperatorEnvFilePath'` and `'Resolve-OpenClawDefaultEnvFilePath'`.
  - Acceptance: Grep of the `Export-ModuleMember` block in `OpenClawContainerValidation.psm1` lists both new function names.
- [x] [P1-T7] [AC3] In `scripts/Invoke-OpenClawContainerPathValidation.ps1`, remove the hardcoded default from the `-EnvFilePath` parameter declaration (currently `[string]$EnvFilePath = './.env'` at line 33), leaving it as `[string]$EnvFilePath` with no default, so the script can distinguish "not supplied" from an explicit value.
  - Acceptance: Grep of the file's `param()` block shows `[string]$EnvFilePath` with no `= './.env'` default.
- [x] [P1-T8] [AC3] In `scripts/Invoke-OpenClawContainerPathValidation.ps1`, immediately before the `if ($null -eq $CoreBaseUrl)` block (currently line 243), add: `if ([string]::IsNullOrWhiteSpace($EnvFilePath)) { $operatorEnvFilePath = Get-OpenClawOperatorEnvFilePath; $EnvFilePath = Resolve-OpenClawDefaultEnvFilePath -OperatorEnvFilePath $operatorEnvFilePath -FallbackEnvFilePath './.env' }`. This preserves the existing downstream consumption of `$EnvFilePath` by `Get-OpenClawCoreBaseUrl` (line 244) and `Test-OpenClawGatewayTokenPresence` (line 252), and by the `EnvFilePath` field of the returned result object (line 271).
  - Acceptance: AC3 (wiring half) — Grep confirms the resolution block appears after the module import and before the first consumer of `$EnvFilePath`; the script's `$result.EnvFilePath` reflects the resolved value in both the present and absent cases exercised by P1-T13.
- [x] [P1-T9] [AC1] In `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1`, update both `switch -Regex` fake-docker case patterns matching `'exec.*openclaw-agent.*v1/status'` (the success case at line 60 and the non-200 case at line 112) to match the post-fix command text (e.g. `'exec.*openclaw-agent.*[^v]status'` or an equivalent pattern that matches `/status` without requiring `/v1/status`), so the mocked docker exec continues to route the probe correctly after P1-T2.
  - Acceptance: both fake-docker `switch -Regex` cases match the shell command produced by the post-fix `Invoke-OpenClawHostAdapterInContainerProbe`, and neither pattern requires the literal substring `v1/status`.
- [x] [P1-T10] [AC1] In the same file, in the first `It` block ('HostAdapterInContainer probe reports IsExpected when docker exec returns 200'), add the assertion `$execRequest | Should -Not -Match '/v1/'` immediately after the existing `$execRequest | Should -Match 'hostadapter\.token'` assertion, plus `$execRequest | Should -Match '/status'`.
  - Acceptance: AC1 (Pester verification) — both new assertions exist and pass against the post-fix probe command (verified in Phase 2). The `$execRequest` selector `@($script:DockerRequests | Where-Object { $_ -like '*compose exec*' })[0]` continues to select the HostAdapter probe (which the entry script emits before the new AC7 token-in-container probe).
- [x] [P1-T11] [AC2] In the same `It` block, add assertions on the emitted result's `ExpectedCondition` field: `$probe.ExpectedCondition | Should -Match '/status'` and `$probe.ExpectedCondition | Should -Not -Match '/v1'`.
  - Acceptance: AC2 (Pester verification) — both assertions exist and pass against the post-fix `ExpectedCondition` text from P1-T3.
- [x] [P1-T12] [AC3] Create new test file `tests/scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.EnvFilePathDefault.Tests.ps1` (Pester v5, `Describe 'OpenClawContainerValidation.psm1 (default EnvFilePath resolution)'`, `BeforeAll` importing the module via `Join-Path $PSScriptRoot '..\..\..\..\..\scripts\powershell\modules\OpenClawContainerValidation\OpenClawContainerValidation.psd1'`) with `It` blocks: (a) `Get-OpenClawOperatorEnvFilePath -LocalAppDataPath 'C:\Users\Test\AppData\Local'` returns `'C:\Users\Test\AppData\Local\OpenClaw\operator-config\.env'` (path-separator-normalized comparison); (b) `Get-OpenClawOperatorEnvFilePath -LocalAppDataPath ''` returns `$null`; (c) `Resolve-OpenClawDefaultEnvFilePath -OperatorEnvFilePath 'C:\op\.env' -FallbackEnvFilePath './.env'` returns `'C:\op\.env'` when `Test-Path` (mocked `-ModuleName OpenClawContainerValidation` with a `-ParameterFilter` on the literal path) returns `$true`; (d) the same call returns `'./.env'` when the mocked `Test-Path` returns `$false`; (e) `Resolve-OpenClawDefaultEnvFilePath -OperatorEnvFilePath $null -FallbackEnvFilePath './.env'` returns `'./.env'` without calling `Test-Path`. No temp files; no real filesystem access beyond the injected `-LocalAppDataPath`/`-OperatorEnvFilePath` parameters.
  - Acceptance: AC3 (unit-level, present + absent cases) — the file exists with all five `It` blocks and each passes in targeted mode (verified in Phase 2).
- [x] [P1-T13] [AC3] Create new test file `tests/scripts/Invoke-OpenClawContainerPathValidation.EnvFilePathDefault.Tests.ps1` (Pester v5, following the existing `BeforeDiscovery`/`BeforeAll`/`BeforeEach`/`AfterEach` harness pattern from `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1`, reusing `tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1`) with two `It` blocks invoking the entry script without `-EnvFilePath`: (a) mock `Get-OpenClawOperatorEnvFilePath` (`-ModuleName OpenClawContainerValidation`) to return a fixed fake path `'C:\FakeAppData\OpenClaw\operator-config\.env'` and mock `Test-Path` (`-ModuleName OpenClawContainerValidation`, `-ParameterFilter` matching that literal path) to return `$true`; assert `$result.EnvFilePath` equals the fake operator path; (b) same `Get-OpenClawOperatorEnvFilePath` mock, but the `Test-Path` mock (same `-ParameterFilter`) returns `$false`; assert `$result.EnvFilePath` equals `'./.env'`. Both cases mock `Get-Content`/`Invoke-WebRequest` per the existing fixture pattern so no real filesystem or network access occurs.
  - Acceptance: AC3 (end-to-end, present + absent cases) — the file exists with both `It` blocks and each passes in targeted mode (verified in Phase 2).

**Batch B — AC6 documentation and AC7 validation accuracy (production: same 2 PS files; docs: 2 Markdown files; tests: 2 files)**

- [x] [P1-T14] [AC6] In `README.md`, correct the dashboard-access documentation. In the dashboard paragraph at line 706, remove the claim that "the dashboard reads the token without an operator paste step" and rewrite it to document the accurate upstream (OpenClaw 2026.6.11) auth procedure: open `http://127.0.0.1:${OPENCLAW_AGENT_PORT:-18789}/#token=<OPENCLAW_GATEWAY_TOKEN>` (token in the URL fragment) or paste the token in the Control UI settings; describe the device re-pair reset after the `openclaw-agent` container is recreated (`openclaw devices clear` inside the agent container, clear browser site data for the origin, reopen the fragment URL); and add a note that `OPENCLAW_AGENT_IMAGE` tracks a floating upstream tag so the auth flow can change across upgrades. Also reconcile the adjacent inaccurate "the dashboard will not accept any other credential" claim in the first-run onboarding paragraph at line 666 so it no longer asserts the token is the only accepted credential.
  - Acceptance: AC6 (README half) — Grep of `README.md` returns zero matches for `reads the token without an operator paste step` and for `will not accept any other credential`; Grep confirms the `#token=` fragment URL, the `openclaw devices clear` re-pair procedure, and the floating-tag note are present in the dashboard section.
- [x] [P1-T15] [AC6] In `docs/mailbridge-runbook.md`, correct the dashboard-auth prose: rewrite the "Dashboard access" section (line 636) and the overview line 22 ("the onboarding-produced gateway token is the only credential the dashboard accepts") so neither asserts tokenless/auto or single-credential dashboard authentication; document the same `#token=` fragment URL / Control-UI-paste procedure and the device re-pair reset; and update the aggregated-validation description (line 622, "a live dashboard auth probe") and the `AgentDashboard` expectation bullet (line 492) to state that the dashboard probe confirms the Control UI is served but does not verify operator authentication.
  - Acceptance: AC6 (runbook half) — Grep of `docs/mailbridge-runbook.md` returns zero matches for `only credential the dashboard accepts` and for any remaining `dashboard auth probe` claim implying an authenticated HTTP probe; Grep confirms the `#token=` fragment URL and the re-pair procedure are present in the "Dashboard access" section.
- [x] [P1-T16] [AC7] In `scripts/Invoke-OpenClawContainerPathValidation.ps1`, in `Invoke-OpenClawAgentDashboardEndpointValidation` (lines 230–241), change the `ExpectedCondition` (currently `'HTTP 200 with a non-empty response body'`) and the `$summary` text so both state that HTTP 200 on `/` confirms the Control UI is served but does NOT verify operator authentication (which requires the `#token=` URL fragment plus device pairing). Do not add a WebSocket or device-pairing handshake; the probe logic (`RequestSucceeded -and HttpStatusCode -eq 200 -and hasBody`) is unchanged — only the descriptive text changes.
  - Acceptance: AC7 (dashboard-text half) — Grep of the function shows the `ExpectedCondition`/summary reference "Control UI is served" and "does not verify" (or equivalent explicit wording) and no longer imply authenticated access; the `$isExpected` computation is unchanged.
- [x] [P1-T17] [AC7] Add a new exported function `Test-OpenClawGatewayTokenInContainer -DockerExecutablePath <string> -AgentContainerName <string>` to `OpenClawContainerValidation.psm1`, modeled on `Invoke-OpenClawHostAdapterInContainerProbe`: run `Invoke-OpenClawDockerCommand -ExecutablePath $DockerExecutablePath -CommandArguments @('compose','exec','-T',$AgentContainerName,'sh','-c',<shell>)` where `<shell>` is `'if [ -n "$OPENCLAW_GATEWAY_TOKEN" ]; then echo present; else echo absent; fi'`; set `$isExpected = $command.Succeeded -and (trimmed output) -eq 'present'`; return `Get-OpenClawValidationResult -Category 'Container' -Name 'GatewayTokenInContainer' -Target $AgentContainerName -ExpectedCondition 'OPENCLAW_GATEWAY_TOKEN is present and non-empty inside the running agent container (checked via docker exec), distinct from the .env-file presence check' -IsExpected $isExpected -Summary <summary> -Details @{ dockerExitCode = $command.ExitCode; probeResult = <trimmed output> }`, with distinct Expected / not-present / docker-exec-failed summary branches. Add `'Test-OpenClawGatewayTokenInContainer'` to the `Export-ModuleMember` list.
  - Acceptance: AC7 (probe-function half) — the function exists with the documented signature, routes solely through `Invoke-OpenClawDockerCommand`, adds no WebSocket/pairing logic, and is named in `Export-ModuleMember`.
- [x] [P1-T18] [AC7] In `scripts/Invoke-OpenClawContainerPathValidation.ps1`, wire the new probe into aggregation: after the `$hostAdapterProbe = ...` line (259), add `$gatewayTokenInContainer = Test-OpenClawGatewayTokenInContainer -DockerExecutablePath $DockerPath -AgentContainerName $AgentContainerName`; include it in `$supportingDiagnostics` (e.g. change line 261 to `@($dockerEngine) + $containerDiagnostics + @($hostAdapterProbe, $gatewayTokenInContainer) + $endpointDiagnostics`) so it contributes to `$isExpected`/`OverallResult`; and add a `GatewayTokenInContainer = $gatewayTokenInContainer` property to the returned result `[pscustomobject]`. Do not add it to `$endpointDiagnostics` (which must remain the 6 endpoint probes).
  - Acceptance: AC7 (aggregation half) — Grep confirms `Test-OpenClawGatewayTokenInContainer` is invoked, appears in the `$supportingDiagnostics` composition, and the result object exposes `GatewayTokenInContainer`; `$endpointDiagnostics` still contains exactly 6 entries.
- [x] [P1-T19] [AC7] In `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` (the `'returns expected when all container endpoints match their validation contracts'` test): extend its inline fake-docker `switch -Regex` so a `compose exec` command whose text contains `OPENCLAW_GATEWAY_TOKEN` returns `'present'` (and sets `$global:LASTEXITCODE = 0`), keeping the existing HostAdapter `compose exec` -> `'200'` behavior for the status probe; change the `SupportingDiagnostics` count assertion from `Should -Be 14` to `Should -Be 15`; add `$result.GatewayTokenInContainer.IsExpected | Should -BeTrue`; and confirm the `EndpointDiagnostics` count assertion remains `Should -Be 6`. Apply the same fake-docker token-exec handler to the second full-run test (`'derives the default CoreBaseUrl ...'`) so its run stays green.
  - Acceptance: AC7 (regression-count half) — the aggregation-count assertion is `15`, the `GatewayTokenInContainer` assertion is present, the `EndpointDiagnostics` count is unchanged at `6`, and both full-run tests pass in targeted mode (verified in Phase 2).
- [x] [P1-T20] [AC7] Create new test file `tests/scripts/Invoke-OpenClawContainerPathValidation.GatewayTokenInContainer.Tests.ps1` (Pester v5, following the existing harness pattern and reusing `tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1`) with `It` blocks that invoke the entry script with a per-test `Invoke-FakeDocker` seam: (a) token-present case — the fake docker returns `'present'` for the `OPENCLAW_GATEWAY_TOKEN` `compose exec` command; assert `$result.GatewayTokenInContainer.IsExpected` is `$true`; (b) token-absent case — the fake docker returns `'absent'`; assert `$result.GatewayTokenInContainer.IsExpected` is `$false` and the probe `Summary`/`Details` reference the missing in-container token; (c) assert `$result.AgentDashboard.ExpectedCondition` no longer matches `authenticat` (case-insensitive) and does match "Control UI" (or the exact wording chosen in P1-T16). Mock `Get-Content`/`Test-Path`/`Invoke-WebRequest` per the existing fixture pattern; no temp files; deterministic.
  - Acceptance: AC7 (Pester half) — the file exists with the three `It` blocks (present, absent, dashboard-text) and each passes in targeted mode (verified in Phase 2).
- [x] [P1-T21] Verify no PowerShell file touched in this phase exceeds 500 lines.
  - Acceptance: `(Get-Content <file>).Count` <= 500 for each of the 2 production PS files and the 5 test files listed in "Explicit Scope and Non-Goals". (Markdown documentation files are exempt from the cap.)

### Phase 2 — Final QC Loop (AC4, AC5, AC6, AC7) and Reduced Audit

- [x] [P2-T1] Run the final PowerShell format check: `mcp__drm-copilot__run_poshqc_format` scoped to the 2 production PS files and 5 test files changed in Phase 1. If the run modifies any file, restart this task before proceeding to P2-T2.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-format.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (pass status, 0 files changed on the recorded clean pass).
- [x] [P2-T2] Run the final PowerShell analyzer check: `mcp__drm-copilot__run_poshqc_analyze` scoped to the same PowerShell files. If any error-severity finding is reported, apply the needed fix and restart from P2-T1.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-analyze.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` (diagnostic counts by severity, 0 errors).
- [x] [P2-T3] Run the final PowerShell test check (AC4): `mcp__drm-copilot__run_poshqc_test` over the full `tests/scripts` Pester suite. If any test fails, apply the needed fix and restart from P2-T1.
  - Acceptance: AC4 (test half) — `FEATURE/evidence/qa-gates/final-poshqc-test.<ts>.md` exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, `Output Summary:` recording the full suite pass/fail counts (all tests passing, including the new P1-T12/P1-T13/P1-T20 tests, the updated P1-T10/P1-T11 assertions, and the updated P1-T19 aggregation-count assertions).
- [x] [P2-T4] Run the final PowerShell test-and-coverage check: run `mcp__drm-copilot__run_poshqc_test` in coverage mode (record the known bundled-runsettings failure), then run the corrected-runsettings `Invoke-PoshQCTest` workaround scoped to the 2 production PS files.
  - Acceptance: `FEATURE/evidence/qa-gates/final-poshqc-coverage.<ts>.md` exists with `Timestamp:`, `Command:` (both invocations), `EXIT_CODE:` for each, `Output Summary:` containing the numeric post-change line-coverage % and branch-coverage % for the 2 production files, with an explicit comparison against `FEATURE/evidence/baseline/poshqc-coverage.<ts>.md` showing no regression against the P0-T10 baseline, line coverage >= 85%, and branch coverage >= 75%. If any required numeric value is unavailable, the outcome is remediation-required, not PASS.
- [x] [P2-T5] Verify AC5: confirm no path under `src/OpenClaw.HostAdapter/**` appears in the change set.
  - Acceptance: AC5 — `FEATURE/evidence/qa-gates/ac5-hostadapter-unchanged.<ts>.md` exists with `Timestamp:`, `Command: git diff --name-only -- src/OpenClaw.HostAdapter`, `EXIT_CODE: 0`, `Output Summary:` recording zero output lines (no changed paths under `src/OpenClaw.HostAdapter/**`).
- [x] [P2-T6] Verify the non-goal invariants from "Explicit Scope and Non-Goals": confirm `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1`, `tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1`, and `tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1` remain byte-identical.
  - Acceptance: `FEATURE/evidence/qa-gates/non-goal-diffs.<ts>.md` exists with `Timestamp:`, `Command: git diff --name-only -- tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1 tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1 tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1`, `EXIT_CODE: 0`, `Output Summary:` recording zero output lines.
- [x] [P2-T7] Verify AC6 documentation accuracy by Grep over `README.md` and `docs/mailbridge-runbook.md`.
  - Acceptance: AC6 — `FEATURE/evidence/qa-gates/ac6-doc-accuracy.<ts>.md` exists with `Timestamp:`, `Command:` (the Grep commands), `EXIT_CODE:`, `Output Summary:` confirming zero matches for the removed inaccurate claims (`reads the token without an operator paste step`, `will not accept any other credential`, `only credential the dashboard accepts`) and presence of the corrected `#token=` fragment procedure, the `openclaw devices clear` re-pair steps, and the `OPENCLAW_AGENT_IMAGE` floating-tag note across both files.
- [x] [P2-T8] Verify AC7 dashboard-text accuracy by Grep: confirm the `AgentDashboard` `ExpectedCondition`/summary in `scripts/Invoke-OpenClawContainerPathValidation.ps1` no longer implies authenticated access and that `Test-OpenClawGatewayTokenInContainer` adds no WebSocket/pairing handshake.
  - Acceptance: AC7 — `FEATURE/evidence/qa-gates/ac7-dashboard-text.<ts>.md` exists with `Timestamp:`, `Command:` (Grep for the dashboard text and for `WebSocket`/`websocket`/`ws://`/pairing-handshake terms in the two production files), `EXIT_CODE:`, `Output Summary:` confirming the dashboard text states the Control UI is served but authentication is not verified, and confirming zero WebSocket/pairing-handshake additions.
- [x] [P2-T9] Write the reduced small-audit summary mapping AC1–AC7 to their supporting evidence.
  - Acceptance: `FEATURE/evidence/other/minor-audit-summary.<ts>.md` exists listing AC1 (P1-T2/P1-T9/P1-T10), AC2 (P1-T3/P1-T11), AC3 (P1-T4/P1-T5/P1-T6/P1-T7/P1-T8/P1-T12/P1-T13), AC4 (P2-T1/P2-T2/P2-T3), AC5 (P2-T5), AC6 (P1-T14/P1-T15/P2-T7), AC7 (P1-T16/P1-T17/P1-T18/P1-T19/P1-T20/P2-T8), each with a PASS status and a pointer to its supporting evidence artifact.
- [x] [P2-T10] Mirror an issue-update comment/body change for issue #144 summarizing the fix (probe URL `/v1/status` -> `/status`; `HostAdapterInContainer` `ExpectedCondition` update; `-EnvFilePath` default retargeted to the operator env file with fallback to `./.env`; dashboard-auth documentation corrected in `README.md` and `docs/mailbridge-runbook.md`; `AgentDashboard` probe text no longer implies authenticated access; new in-container `GatewayTokenInContainer` check; full toolchain pass) per the issue-update mirroring convention.
  - Acceptance: `FEATURE/evidence/issue-updates/issue-144.<ts>.md` exists with `Timestamp:`, the exact text intended/posted, `PostedAs: body` or `PostedAs: comment` (or `POSTING BLOCKED` with reason if not posted), and the GitHub URL when posted. If `PostedAs: body`, the same update is mirrored into `FEATURE/issue.md`.

## Test Plan

- Unit (Pester v5, updated): `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1` — probe command contains `/status` and not `/v1` (P1-T10); `ExpectedCondition` references `/status` and not `/v1` (P1-T11); fake-docker regex updated to match the post-fix command (P1-T9).
- Unit (Pester v5, new): `tests/scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.EnvFilePathDefault.Tests.ps1` — `Get-OpenClawOperatorEnvFilePath` path composition and empty-input case; `Resolve-OpenClawDefaultEnvFilePath` present/absent/null-operator cases (P1-T12).
- Unit (Pester v5, new): `tests/scripts/Invoke-OpenClawContainerPathValidation.EnvFilePathDefault.Tests.ps1` — end-to-end default resolution when the operator env file is present and when it is absent (P1-T13).
- Unit (Pester v5, updated): `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` — aggregation `SupportingDiagnostics` count 14 -> 15, new `GatewayTokenInContainer` assertion, `EndpointDiagnostics` count unchanged at 6, fake-docker token-exec handler added to both full-run tests (P1-T19).
- Unit (Pester v5, new): `tests/scripts/Invoke-OpenClawContainerPathValidation.GatewayTokenInContainer.Tests.ps1` — in-container token present/absent cases and `AgentDashboard` `ExpectedCondition` no-authentication assertion (P1-T20).
- Coverage evidence (PowerShell): baseline `FEATURE/evidence/baseline/poshqc-coverage.<ts>.md` (P0-T10); post-change `FEATURE/evidence/qa-gates/final-poshqc-coverage.<ts>.md` (P2-T4).
- Manual/CLI: none required this pass; a post-merge integration retest against a healthy install (`Invoke-OpenClawContainerPathValidation.ps1 -PassThru` returning `OverallResult: Expected`, including the new `GatewayTokenInContainer` probe) is recorded as a follow-up in the PR notes, per `issue.md`'s "Integration scenario to retest".

## Open Questions / Notes

- `Resolve-OpenClawDefaultEnvFilePath`'s `Test-Path -LiteralPath` call only runs when `$OperatorEnvFilePath` is non-blank (P1-T5), avoiding an ambiguous `Test-Path -LiteralPath ''` call when `$env:LOCALAPPDATA` is unset.
- Delegation-prompt location reconciliation: the AgentDashboard probe named "in OpenClawContainerValidation.psm1" is actually the entry-script function `Invoke-OpenClawAgentDashboardEndpointValidation` in `scripts/Invoke-OpenClawContainerPathValidation.ps1`; the AC7 text edit is applied there. The new `Test-OpenClawGatewayTokenInContainer` probe is added to the module, matching the module's existing container-probe pattern. Both files were already in the 2-production-file scope, so the budget is unchanged.
- AC7 aggregation adds one probe to `SupportingDiagnostics`, which is why `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` moves from non-goal (in the AC1–AC5-only revision) to a modified test file: its `Should -Be 14` count assertion and its full-run fake docker must both be updated. The `TokenPresence` and `Readyz` test files remain byte-identical because they assert only their own probe and no aggregate count (confirmed by reading them during planning).
- AC6 corrects two related inaccurate claims in `README.md` (line 706 "reads the token without an operator paste step" and the adjacent line 666 "will not accept any other credential") and the matching runbook claims (line 22 "the only credential the dashboard accepts", the line 636 "Dashboard access" section, the line 622 "dashboard auth probe" description, and the line 492 `AgentDashboard` expectation bullet), because each asserts tokenless/auto or single-credential dashboard authentication that AC6/AC7 correct.
- No WebSocket or device-pairing handshake is added (AC7 constraint): the gateway exposes no auth-gated HTTP endpoint, so the in-container check verifies only that `OPENCLAW_GATEWAY_TOKEN` is present and non-empty inside the running agent container via the existing docker seam.
