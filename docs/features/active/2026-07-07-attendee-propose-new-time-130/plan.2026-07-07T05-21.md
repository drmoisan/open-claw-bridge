# attendee-propose-new-time - Plan

- **Issue:** #130
- **Parent (optional):** epic `docs/features/epics/openclaw-vision/epic-plan.md` (Epic D, feature F19, wave 5; depends on F18 #128, merged at branch base)
- **Owner:** drmoisan
- **Last Updated:** 2026-07-07
- **Status:** Ready for Preflight
- **Version:** 1.0
- **Work Mode:** full-feature (per `issue.md` metadata; `spec.md` and `user-story.md` both present and authoritative)

## Required References

Policy reading order (per `.claude/skills/policy-compliance-order/SKILL.md`):

1. `CLAUDE.md` / auto-loaded `.claude/rules/` standing instructions
2. `.claude/rules/general-code-change.md`
3. `.claude/rules/general-unit-test.md`
4. `.claude/rules/csharp.md`
5. `.claude/rules/quality-tiers.md`

**All work must comply with these policies; do not duplicate their content here.**

Authoritative feature documents (full-feature mode: `spec.md` + `user-story.md` are the acceptance-criteria sources):

- `docs/features/active/2026-07-07-attendee-propose-new-time-130/spec.md` (AC-1..AC-9; gate truth table; evaluation order; wire contract)
- `docs/features/active/2026-07-07-attendee-propose-new-time-130/user-story.md` (AC-1..AC-9; scenarios)
- `docs/features/active/2026-07-07-attendee-propose-new-time-130/issue.md` (mode marker)
- Research: `docs/features/active/2026-07-07-attendee-propose-new-time-130/research/2026-07-07T09-45-attendee-propose-new-time.research.md` (authoritative file-change list, seam map, Graph tentativelyAccept contract, D5 matrix, rejected alternatives)

## Global Constraints (apply to every task)

- **Diff scope (confined):** production adds `src/OpenClaw.Core/CloudGraph/GraphHostAdapterClient.ProposeNewTime.cs` and `src/OpenClaw.Core/Agent/Runtime/SchedulingWorker.ProposeNewTime.cs`; production modifies `src/OpenClaw.HostAdapter.Contracts/IHostAdapterClient.cs`, `src/OpenClaw.Core/HostAdapterHttpClient.cs`, `src/OpenClaw.Core/Agent/Contracts/ISchedulingService.cs`, `src/OpenClaw.Core/Agent/Runtime/HostAdapterSchedulingService.cs`, `src/OpenClaw.Core/Agent/Runtime/SchedulingWorker.Pipeline.cs`, `src/OpenClaw.Core/Agent/Contracts/ActionAuditResultCode.cs`, `src/OpenClaw.Core/Agent/SentActionKey.cs`; test changes confined to `tests/OpenClaw.Core.Tests/**` — test ADDs: `tests/OpenClaw.Core.Tests/CloudGraph/GraphHostAdapterClientProposeNewTimeTests.cs`, `tests/OpenClaw.Core.Tests/HostAdapterHttpClientProposeNewTimeTests.cs`, `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeTests.cs`, `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeIntentPropertyTests.cs`, `tests/OpenClaw.Core.Tests/Agent/Runtime/HostAdapterSchedulingServiceProposeNewTimeTests.cs`, plus the optional 500-line-cap split siblings `tests/OpenClaw.Core.Tests/CloudGraph/GraphHostAdapterClientProposeNewTimeErrorTests.cs` (P1-T4 fallback) and `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeEdgeTests.cs` (P3-T4 fallback); zero test MODIFIES — `tests/OpenClaw.Core.Tests/HostAdapterHttpClientTests.cs` (616 lines, pre-existing 500-line-cap violation, verified 2026-07-07) MUST remain unmodified: the member-11 local-adapter negative test is a test ADD in the new sibling file (F18 precedent: `HostAdapterHttpClientRescheduleTests.cs`); existing loose Moq mocks of `IHostAdapterClient`/`ISchedulingService` absorb the new members automatically, so no pre-existing test file changes; plus this feature folder. Zero changes to `src/OpenClaw.Core/Program.cs` (no DI change), `src/OpenClaw.Core/Agent/Runtime/SchedulingWorker.cs` (no ctor change), `src/OpenClaw.Core/Agent/Contracts/AgentPolicyOptions.cs`, `src/OpenClaw.Core/Agent/CalendarWritePolicy.cs`, `src/OpenClaw.Core/appsettings.json`, `src/OpenClaw.Core/Agent/OneOnOneMoveGuard.cs`, `src/OpenClaw.Core/Agent/Models/NormalizedMeetingContext.cs`, `src/OpenClaw.Core/Agent/Contracts/ActionAuditRecord.cs`, `src/OpenClaw.Core/Agent/Contracts/PurviewActivityLogProjection.cs` (documented fallback covers the new action type), `src/OpenClaw.Core/CloudGraph/GraphRequestExecutor.cs`, `src/OpenClaw.Core/CloudGraph/SendOnBehalfAuthorizer.cs`, and `quality-tiers.yml`. No new dependencies. No schema/migration changes.
- **Toolchain loop (per phase batch):** `csharpier format .` then `csharpier check .` (global tool 1.3.0; not `dotnet csharpier` — no local tool manifest), `dotnet build OpenClaw.MailBridge.sln -warnaserror` (analyzers + nullable as errors; zero warnings), `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` (includes the NetArchTest architecture-boundary suites). Restart the loop from formatting if any step fails or changes files; a phase is complete only when all steps pass in a single pass.
- **Fail-closed invariant:** null/missing event, organizer-owned message (`IsOrganizer == true`), `AllowNewTimeProposals == false`, missing original times, empty event id, zero proposed slots, gate off, local-backend `NOT_SUPPORTED`, and any failure envelope or exception all result in no Graph write and no dedupe row. Any ambiguity resolves to "no write". The attendee path never reads or writes `series_moves` in any branch — no move guard, no blocked result code, zero `ISeriesMoveHistory.RecordMoveAsync` calls.
- **Determinism:** all time via injected `TimeProvider` (`FakeTimeProvider` in tests); `DateTime.Now`/`DateTime.UtcNow` banned; no `Thread.Sleep`/`Task.Delay`/wall-clock waits in tests; retry-exhaustion tests advance simulated time; no live Graph calls anywhere — all HTTP through the shared `FakeHttpHandler` with base address `https://graph.example.test/v1.0/`; no temporary files.
- **Test stack:** MSTest + FluentAssertions + Moq (+ CsCheck for property tests, `MockBehavior.Strict` `IAppTokenProvider`), per repository-actual convention noted in the research (not the xUnit/NSubstitute wording in `.claude/rules/csharp.md`).
- **File size:** every new or modified production and test file <= 500 lines.
- **Architecture (No-COM):** pure decision logic (intent computation, ActingFlags snapshot) stays host-neutral; the Graph HTTP call lives only in the new `CloudGraph` partial behind `IHostAdapterClient`; the worker partial references no `CloudGraph` type and reaches Graph only through `ISchedulingService`; no domain-to-adapter reference; existing NetArchTest boundary tests must pass unmodified.
- **Evidence:** all evidence artifacts under `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/<kind>/` (canonical scheme per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`; non-canonical `artifacts/` evidence paths are prohibited). Raw command intermediates (TRX, coverage XML, build logs) go to `artifacts/csharp/`. `<ts>` in artifact names is the ISO-8601 `yyyy-MM-ddTHH-mm` execution timestamp.

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture and Policy Compliance

- [x] [P0-T1] Read the policy documents in the Required References order (items 1-5) and write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read
  - Acceptance: artifact exists with all three fields populated before any Phase 1 work begins
- [x] [P0-T2] Verify full-feature document preconditions: `docs/features/active/2026-07-07-attendee-propose-new-time-130/issue.md` contains `- Work Mode: full-feature`, and `spec.md` + `user-story.md` exist in the feature folder with `## Acceptance Criteria` sections listing AC-1..AC-9; record the check in `evidence/baseline/phase0-instructions-read.md` (append a `Mode Verification:` section)
  - Acceptance: appended section names the three files, the mode marker value, and a pass/fail verdict; fail closed (stop and report) on any mismatch
- [x] [P0-T3] Capture the C# formatting baseline: run `csharpier check .` from repo root and write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/baseline/csharp-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`
  - Acceptance: artifact exists with all four fields; `Output Summary:` states pass/fail and any offending file count
- [x] [P0-T4] Capture the C# build/analyzer/nullable baseline: run `dotnet build OpenClaw.MailBridge.sln -warnaserror` and write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/baseline/csharp-build.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`
  - Acceptance: artifact exists with all four fields; `Output Summary:` records warning/error counts (expected 0/0 on clean baseline)
- [x] [P0-T5] Capture the C# test, architecture-boundary, and coverage baseline: run `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` (this run includes the NetArchTest boundary suites), copy raw TRX/coverage output under `artifacts/csharp/`, and write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/baseline/csharp-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric baseline line-coverage percent, numeric baseline branch-coverage percent, and total pass/fail test counts
  - Acceptance: artifact exists with all four fields and numeric coverage headline values (no placeholders); raw intermediates present under `artifacts/csharp/`

### Phase 1 — Adapter Seam: ProposeNewMeetingTimeAsync (IHostAdapterClient member 11)

- [x] [P1-T1] Update `src/OpenClaw.HostAdapter.Contracts/IHostAdapterClient.cs`: add the additive member `Task<ApiEnvelope<object?>> ProposeNewMeetingTimeAsync(string bridgeId, DateTimeOffset proposedStartUtc, DateTimeOffset proposedEndUtc, string? requestId = null, CancellationToken cancellationToken = default)` with XML docs stating the Graph wire route (`POST /users/{principal}/events/{id}/tentativelyAccept`), the 202-Accepted-no-body semantics (`ok: true, data: null` per the `SendMailAsync` precedent), the body scope (exactly `sendResponse: true` and `proposedNewTime`; the body structurally cannot carry `comment` or top-level `start`/`end`/`body`/`subject`/`attendees`), and the local-backend fail-closed `NOT_SUPPORTED` behavior
  - Acceptance: file compiles once both implementations (P1-T2, P1-T3) exist, <= 500 lines; no existing member signature changed
- [x] [P1-T2] Create `src/OpenClaw.Core/CloudGraph/GraphHostAdapterClient.ProposeNewTime.cs`: implement `ProposeNewMeetingTimeAsync` as `POST users/{Principal}/events/{Uri.EscapeDataString(bridgeId)}/tentativelyAccept` through the shared `GraphRequestExecutor` via `executor.ExecuteAsync<object?>(..., _ => null, requestId, ct)` (inheriting bearer-token acquisition, `client-request-id` propagation from `requestId`, 429/502/503/504 retry with `Retry-After` precedence via injected `TimeProvider`, and the D5 error matrix: 400 -> `INVALID_REQUEST`, 401/403 -> `UNAUTHORIZED` with Graph `error.code` passthrough to `BridgeErrorCode`, 404 -> `NOT_FOUND`, 429 -> `THROTTLED`, 502/503/504 -> `TRANSPORT_FAILURE`); request body serialized camelCase via `GraphRequestExecutor.JsonOptions` with exactly two top-level properties — `sendResponse` hardcoded `true` and `proposedNewTime` containing exactly `start` and `end` dateTimeTimeZone pairs with `dateTime` rendered from the UTC instant using the invariant seconds-precision `"s"` format and `timeZone` = `"UTC"` (mirror the `SchedulingDateTime` helper precedent in `GraphHostAdapterClient.Calendar.cs`); no `comment` property; no `Prefer` headers; a 202 response with an empty body maps to `ok: true, data: null` (no fabricated data)
  - Acceptance: file compiles, <= 500 lines, namespace/partial matches the `GraphHostAdapterClient.RescheduleEvent.cs`/`GraphHostAdapterClient.SendMail.cs` precedent; no change to `GraphRequestExecutor.cs`
- [x] [P1-T3] Update `src/OpenClaw.Core/HostAdapterHttpClient.cs`: implement `ProposeNewMeetingTimeAsync` as a fail-closed synthesized failure envelope — `ApiError` code `NOT_SUPPORTED`, `Retryable: false`, message stating the local HostAdapter backend has no meeting-response route and attendee propose-new-time requires the Graph adapter — performing no HTTP I/O and no token acquisition, mirroring the member-10 `UpdateEventTimesAsync` implementation shape (lines 153-184 precedent)
  - Acceptance: file compiles, <= 500 lines; the method issues zero `HttpClient` invocations
- [x] [P1-T4] Create `tests/OpenClaw.Core.Tests/CloudGraph/GraphHostAdapterClientProposeNewTimeTests.cs`: mocked-Graph contract suite using the established `FakeHttpHandler` pattern (`MockBehavior.Strict` `IAppTokenProvider`, `FakeTimeProvider`, base address `https://graph.example.test/v1.0/`) covering: (a) method `POST` and URL-escaped principal + event-id route with the `/tentativelyAccept` suffix (assert `AbsolutePath` per the reschedule test precedent); (b) headers — bearer auth, `client-request-id` equal to the supplied `requestId`, `Content-Type: application/json`, no `Prefer` header; (c) exact body shape via structural `JsonDocument` assertions — top level contains exactly `sendResponse` (value `true`) and `proposedNewTime`; `proposedNewTime` contains exactly `start` and `end` dateTimeTimeZone pairs with `timeZone == "UTC"` and seconds-precision `dateTime`; no other properties present, in particular no `comment` and no top-level `start`, `end`, `body`, `subject`, or `attendees` (absent-property guardrail); (d) 202 Accepted with empty body maps to `ApiEnvelope<object?>` `ok: true, data: null` (the `GraphHostAdapterClientSendMailTests.cs` `Accepted()` helper precedent); (e) D5 error samples — 400 -> `INVALID_REQUEST`, 403 with Graph `error.code` `ErrorAccessDenied` -> `UNAUTHORIZED` with `BridgeErrorCode` passthrough, 404 -> `NOT_FOUND`, all non-retryable; (f) 429 retry exhaustion driven by `FakeTimeProvider.Advance` -> `THROTTLED` with `Retry-After` precedence; if the suite approaches the 500-line cap, split the D5 error samples (e)-(f) into a sibling file `tests/OpenClaw.Core.Tests/CloudGraph/GraphHostAdapterClientProposeNewTimeErrorTests.cs`
  - Acceptance: all new tests pass; each of (a)-(f) has at least one named test; each resulting test file <= 500 lines
- [x] [P1-T5] Create `tests/OpenClaw.Core.Tests/HostAdapterHttpClientProposeNewTimeTests.cs` (new sibling file mirroring the existing `HostAdapterHttpClientRescheduleTests.cs` split; references the shared `FakeHttpHandler` defined in `HostAdapterHttpClientTests.cs`): `ProposeNewMeetingTimeAsync` on the local Stage-0 adapter returns `Ok == false` with `Error.Code == "NOT_SUPPORTED"` and `Error.Retryable == false`, with the mocked handler invoked zero times and zero token acquisitions
  - Acceptance: both assertions (envelope contract and zero HTTP invocations) pass in named tests; new file <= 500 lines; `HostAdapterHttpClientTests.cs` remains unmodified
- [x] [P1-T6] Run the mandatory C# toolchain loop for the Phase 1 batch (`csharpier format .`, `csharpier check .`, `dotnet build OpenClaw.MailBridge.sln -warnaserror`, `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`), restarting from formatting on any failure or file change
  - Acceptance: all steps exit 0 in a single uninterrupted pass; existing architecture-boundary suites pass unmodified

### Phase 2 — Service Seam: ProposeNewMeetingTimeAsync (ISchedulingService)

- [x] [P2-T1] Update `src/OpenClaw.Core/Agent/Contracts/ISchedulingService.cs`: add the additive member `Task ProposeNewMeetingTimeAsync(string eventId, DateTimeOffset proposedStartUtc, DateTimeOffset proposedEndUtc, string? correlationId = null, CancellationToken ct = default)` with XML docs mirroring the `RescheduleEventAsync` seam shape (returns `Task`, not a DTO — the wire response has no body and the worker already holds the times it audits)
  - Acceptance: file compiles once P2-T2 exists, <= 500 lines; no existing member changed
- [x] [P2-T2] Update `src/OpenClaw.Core/Agent/Runtime/HostAdapterSchedulingService.cs`: implement `ProposeNewMeetingTimeAsync` mirroring `RescheduleEventAsync` (lines 149-178 precedent) — guard-clause the event id (`ArgumentException.ThrowIfNullOrWhiteSpace`), delegate to `hostAdapterClient.ProposeNewMeetingTimeAsync(eventId, proposedStartUtc, proposedEndUtc, requestId: correlationId, ct)`, and on a non-`Ok` envelope throw `InvalidOperationException($"Attendee propose-new-time failed: {code}: {message}")`; client exceptions propagate unwrapped
  - Acceptance: file compiles, <= 500 lines; no change to any existing method body
- [x] [P2-T3] Create `tests/OpenClaw.Core.Tests/Agent/Runtime/HostAdapterSchedulingServiceProposeNewTimeTests.cs` (new sibling file mirroring `HostAdapterSchedulingServiceRescheduleTests.cs`; the 480-line `HostAdapterSchedulingServiceTests.cs` is not extended): unit cases — delegation passes `eventId`/`proposedStartUtc`/`proposedEndUtc` through to `ProposeNewMeetingTimeAsync` unchanged; `correlationId` forwards as `requestId`; a non-`Ok` envelope throws `InvalidOperationException` whose message contains the error code and message; a null/empty event id fails fast without calling the client
  - Acceptance: all four new named tests pass; new file <= 500 lines; `HostAdapterSchedulingServiceTests.cs` remains unmodified
- [x] [P2-T4] Run the mandatory C# toolchain loop for the Phase 2 batch (same four commands as P1-T6), restarting from formatting on any failure or file change
  - Acceptance: all steps exit 0 in a single uninterrupted pass

### Phase 3 — Worker Orchestration: SchedulingWorker.ProposeNewTime

- [x] [P3-T1] Update `src/OpenClaw.Core/Agent/Contracts/ActionAuditResultCode.cs` (append consts `ProposedNewTime = "proposed_new_time"`, `ProposeNewTimeFailed = "propose_new_time_failed"`, `ProposeNewTimeDisabled = "propose_new_time_disabled"`; `DedupeSkipped` is reused; no blocked code is added — the attendee path has no move guard) and `src/OpenClaw.Core/Agent/SentActionKey.cs` (append const `AttendeeProposeNewTime = "attendee-propose-new-time"`, colon-free per the `Build` distinctness remark)
  - Acceptance: both files compile, <= 500 lines each; no existing constant value changed
- [x] [P3-T2] Create `src/OpenClaw.Core/Agent/Runtime/SchedulingWorker.ProposeNewTime.cs` structured on the `SchedulingWorker.Reschedule.cs` precedent: (a) `internal readonly record struct ProposeNewTimeIntent(string EventId, DateTimeOffset OriginalStartUtc, DateTimeOffset OriginalEndUtc, DateTimeOffset ProposedStartUtc, DateTimeOffset ProposedEndUtc)` plus pure `internal static ComputeProposeNewTimeIntent(NormalizedMeetingContext context, SchedulingEventDto? meetingEvent, IReadOnlyList<CandidateSlot> slots)` — an intent exists iff all of: `meetingEvent` non-null, `context.IsOrganizer == false`, `context.AllowNewTimeProposals == true`, `meetingEvent.Start`/`End` non-null, `context.EventId` non-empty, `slots.Count > 0`; proposed start = `slots[0].Start`; duration preserved from the original event (`End - Start`); any missing precondition -> `null`; (b) pure `internal static BuildProposeNewTimeActingFlags(options)` producing `CalendarWriteEnabled=<bool>;EnableAttendeeProposeNewTime=<bool>` (neither `BuildActingFlags` nor `BuildRescheduleActingFlags` is widened); (c) private `BuildProposeNewTimeAuditRecord(...)` mirroring `BuildRescheduleAuditRecord` — populates `EventId`, `OriginalStartUtc`/`OriginalEndUtc`/`NewStartUtc`/`NewEndUtc` (Original* = event's current times, New* = proposed times), the correlation id, action type `SentActionKey.AttendeeProposeNewTime`, and the propose ActingFlags snapshot; (d) `EvaluateProposeNewTimeAsync(messageId, context, meetingEvent, slots, ct)` (no `priority` parameter) orchestrating the spec's exact evaluation order — intent computation (no intent -> return silently, no audit row), then `CalendarWritePolicy.AttendeeProposeNewTimeAllowed` gate (off -> log the intended proposal original->proposed at Information and audit `propose_new_time_disabled` with the four time columns populated; no Graph call, no write-path token acquisition, no dedupe row), then dedupe (`SentActionKey.Build(mailbox, messageId, SentActionKey.AttendeeProposeNewTime)`; hit -> audit `dedupe_skipped`, return), then write (`schedulingService.ProposeNewMeetingTimeAsync(intent.EventId, intent.ProposedStartUtc, intent.ProposedEndUtc, correlationId, ct)`; on exception excluding `OperationCanceledException` audit `propose_new_time_failed` with `ErrorDetail` durable before rethrow, no dedupe row), then post-write bookkeeping in order — audit `proposed_new_time`, then `sentActionStore.RecordAsync(dedupeKey, timeProvider.GetUtcNow(), ct)`; one GUID correlation id per evaluation forwarded as the adapter request id; audit writes via the existing `WriteAuditSafelyAsync`; no move-guard step, no `ISeriesMoveHistory` reference, no `series_moves` interaction in any branch
  - Acceptance: file compiles, <= 500 lines, host-neutral (no `CloudGraph` reference, no `ISeriesMoveHistory` usage); evaluation order matches spec Behavior steps 1-5 exactly
- [x] [P3-T3] Update `src/OpenClaw.Core/Agent/Runtime/SchedulingWorker.Pipeline.cs`: add exactly one call `await EvaluateProposeNewTimeAsync(messageId, context, meetingEvent, slots, cancellationToken).ConfigureAwait(false);` in `ProposeAndActAsync` immediately after the existing F18 `EvaluateRescheduleAsync` call (no pipeline-level `if (context.IsOrganizer)` branching — mutual exclusivity comes from the intent predicates); no other pipeline stage (hydrate/normalize/triage/classify/send/reschedule) modified
  - Acceptance: file compiles, <= 500 lines; `git diff` of the file shows only the single added call
- [x] [P3-T4] Create `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeTests.cs`: worker unit suite (MSTest + Moq + `FakeTimeProvider`, reusing the `SchedulingWorkerRescheduleTests` internal harness — `Options`/`Message`/`OneOnOneEvent(isOrganizer: false)`/`Service`/`CandidateSource`) covering: (a) the four-row gate truth table — only `CalendarWriteEnabled=true` + `EnableAttendeeProposeNewTime=true` produces exactly one `ProposeNewMeetingTimeAsync` call; the other three rows produce zero service calls, zero write-path token acquisitions, zero dedupe records, and a `propose_new_time_disabled` audit; (b) dry-run detail — the `propose_new_time_disabled` audit carries all four time columns and the propose ActingFlags snapshot `CalendarWriteEnabled=<bool>;EnableAttendeeProposeNewTime=<bool>`; (c) success — exactly one `proposed_new_time` audit (action type `attendee-propose-new-time`, four time columns, correlation id), then the dedupe record, in that order, with zero `ISeriesMoveHistory.RecordMoveAsync` calls; (d) failure — service throw yields a `propose_new_time_failed` audit with `ErrorDetail` before the exception propagates, and no dedupe record and zero `RecordMoveAsync` calls; (e) dedupe hit — second evaluation of the same message audits `dedupe_skipped` and issues no service call; (f) eligibility fail-closed matrix — null event, organizer-owned event (`IsOrganizer == true`), `AllowNewTimeProposals == false`, missing `Start`/`End`, empty `EventId`, and zero slots each produce no audit row and no service call; (g) mutual exclusivity in both directions — an organizer-owned message triggers only the F18 reschedule evaluation (zero `ProposeNewMeetingTimeAsync` calls) and an attendee message triggers only the F19 propose evaluation (zero `RescheduleEventAsync` calls); (h) path isolation — an evaluated send and an evaluated F18 reschedule in the same run still persist their unmodified `BuildActingFlags`/`BuildRescheduleActingFlags` strings byte-identical to pre-F19; if the file approaches the 500-line cap, split rows (e)-(h) into `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeEdgeTests.cs`
  - Acceptance: all new tests pass; each of (a)-(h) has at least one named test; each test file <= 500 lines
- [x] [P3-T5] Create `tests/OpenClaw.Core.Tests/Agent/Runtime/SchedulingWorkerProposeNewTimeIntentPropertyTests.cs`: CsCheck property tests satisfying the T1 >= 1-property-per-new-pure-function obligation — (1) duration preservation: for all valid original intervals and proposed slots, `ProposedEnd - ProposedStart == OriginalEnd - OriginalStart`; (2) eligibility fail-closed monotonicity: from any eligible input, setting `IsOrganizer` true, setting `AllowNewTimeProposals` false, removing the event, the times, the event id, or all slots never yields an intent; (3) flags-snapshot round-trip: for all boolean pairs the `BuildProposeNewTimeActingFlags` string parses back to the input pair
  - Acceptance: all three properties pass with reproducible seeds (CsCheck default reporting); file <= 500 lines
- [x] [P3-T6] Verify the regression surface of the pre-existing paths: confirm via `git diff` that no pre-existing test file under `tests/OpenClaw.Core.Tests/` is modified, that `SchedulingWorker.cs`, `SchedulingWorker.Audit.cs`, `SchedulingWorker.Reschedule.cs`, and their `BuildActingFlags`/`BuildRescheduleActingFlags` are textually unmodified, and that the full test run includes passing send-path and F18 reschedule-path audit/dedupe suites; write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/regression-testing/existing-path-regression-surface.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the verified files
  - Acceptance: artifact exists with all four fields; zero modifications to pre-existing test files and the named production files
- [x] [P3-T7] Run the mandatory C# toolchain loop for the Phase 3 batch (same four commands as P1-T6), restarting from formatting on any failure or file change
  - Acceptance: all steps exit 0 in a single uninterrupted pass; architecture-boundary suites pass with no domain-to-adapter reference introduced

### Phase 4 — Live-Verification Runbook Confirmation and Human-Interaction Record

- [x] [P4-T1] Verify the already-authored runbook `docs/features/active/2026-07-07-attendee-propose-new-time-130/runbooks/attendee-propose-new-time-live-verification.runbook.md` exists and covers, in order: (1) confirming the `Calendars.ReadWrite` application-permission grant and tenant-admin consent (shared with F18; may already be granted); (2) enabling `OpenClaw__AgentPolicy__CalendarWriteEnabled` and `OpenClaw__AgentPolicy__EnableAttendeeProposeNewTime` in a real deployment; (3) observing a live two-mailbox proposal — the 202 response, the `proposed_new_time` audit row with the four time columns, the dedupe row, and the proposal arriving on the organizer's `attendee.proposedNewTime`; (4) disabling the flags after verification (the global kill switch shuts the path off independently); record the verification result (sections found, pass/fail per section) in `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/other/runbook-verification.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`
  - Acceptance: runbook exists at the exact path with all four content areas; verification artifact exists with all four fields; fail closed (report remediation-required) if any content area is missing
- [x] [P4-T2] Write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/other/human-interaction-record.<ts>.md` documenting the live-tenant verification `human_interaction` requirement for the orchestrator checkpoint: requirement description (live verification cannot be automated — no Azure/Exchange credentials in this environment or CI), `response: "exception"`, and `runbook_path: docs/features/active/2026-07-07-attendee-propose-new-time-130/runbooks/attendee-propose-new-time-live-verification.runbook.md`, satisfying the `.claude/rules/orchestrator-state.md` invariant that an `exception` carries a non-empty `runbook_path`
  - Acceptance: artifact exists with `Timestamp:`, the requirement text, the `response` value, and the `runbook_path`; the orchestrator (not this plan's executor) records the entry in `artifacts/orchestration/orchestrator-state.json`

### Phase 5 — Final QA, Coverage Comparison, AC Check-off, and Reconciliation

- [x] [P5-T1] Verify the 500-line cap: measure line counts of every new/modified production file (the 2 adds and 7 modifies listed in Global Constraints) and every new test file under `tests/OpenClaw.Core.Tests/` — `CloudGraph/GraphHostAdapterClientProposeNewTimeTests.cs`, `HostAdapterHttpClientProposeNewTimeTests.cs`, `Agent/Runtime/SchedulingWorkerProposeNewTimeTests.cs`, `Agent/Runtime/SchedulingWorkerProposeNewTimeIntentPropertyTests.cs`, `Agent/Runtime/HostAdapterSchedulingServiceProposeNewTimeTests.cs`, and, when created under their split fallbacks, `CloudGraph/GraphHostAdapterClientProposeNewTimeErrorTests.cs` and `Agent/Runtime/SchedulingWorkerProposeNewTimeEdgeTests.cs` (the unmodified `HostAdapterHttpClientTests.cs` is excluded — its pre-existing 616-line violation is out of scope and the file is not touched); write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/file-size-cap.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (per-file line-count table and maximum)
  - Acceptance: artifact exists; every listed file <= 500 lines
- [x] [P5-T2] Verify test hygiene: search the touched test files for banned APIs and live-endpoint usage (patterns: `Thread.Sleep`, `Task.Delay(`, `DateTime.UtcNow`, `DateTime.Now`, `GetTempFileName`, `GetTempPath`, `File.Write`, `HttpClientHandler`, `graph.microsoft.com`); write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/test-hygiene.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`
  - Acceptance: artifact exists; zero violations (or each match individually justified as a non-executing literal)
- [x] [P5-T3] Final QA formatting gate: run `csharpier format .` then `csharpier check .`; write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/csharp-format.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; if `format` changed any file, record the change and restart the Phase 5 QA loop from this task
  - Acceptance: artifact exists; `csharpier check .` exits 0
- [x] [P5-T4] Final QA build gate (lint + nullable + analyzers): run `dotnet build OpenClaw.MailBridge.sln -warnaserror`; write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/csharp-build.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`; on failure, remediate and restart the Phase 5 QA loop from P5-T3
  - Acceptance: artifact exists; build exits 0 with zero warnings/errors
- [x] [P5-T5] Final QA test + coverage gate (includes architecture-boundary, unit, property, and contract suites): run `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`; copy raw TRX/coverage to `artifacts/csharp/`; write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/csharp-test-coverage.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change line and branch coverage percents and pass/fail counts; on failure, remediate and restart the Phase 5 QA loop from P5-T3
  - Acceptance: artifact exists with numeric coverage values (no placeholders); all tests pass; architecture-boundary suites pass unmodified
- [x] [P5-T6] Produce the coverage-comparison artifact: compare the P0-T5 baseline against the P5-T5 post-change coverage and compute changed/new-code coverage for `GraphHostAdapterClient.ProposeNewTime.cs`, `SchedulingWorker.ProposeNewTime.cs`, and the changed lines of the 7 modified production files from the coverage XML; write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/qa-gates/coverage-comparison.<ts>.md` reporting baseline coverage, post-change coverage, and new/changed-code coverage, with a threshold verdict (line >= 85%, branch >= 75%, no regression on changed lines)
  - Acceptance: artifact exists with all three numeric sections; verdict PASS only when all thresholds hold — otherwise the plan outcome is remediation-required, never PASS
- [x] [P5-T7] Verify zero production-code drift outside the allowed diff scope: run `git diff --name-only` against the branch base and confirm the changed-file set is confined to the Global Constraints diff scope, with zero changes to the enumerated prohibited files (`Program.cs`, `SchedulingWorker.cs`, `AgentPolicyOptions.cs`, `CalendarWritePolicy.cs`, `appsettings.json`, `OneOnOneMoveGuard.cs`, `NormalizedMeetingContext.cs`, `ActionAuditRecord.cs`, `PurviewActivityLogProjection.cs`, `GraphRequestExecutor.cs`, `SendOnBehalfAuthorizer.cs`, `quality-tiers.yml`, `tests/OpenClaw.Core.Tests/HostAdapterHttpClientTests.cs`); write `docs/features/active/2026-07-07-attendee-propose-new-time-130/evidence/other/diff-scope-verification.<ts>.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the full changed-file set
  - Acceptance: artifact exists; changed-file set matches the Global Constraints diff scope exactly (any deviation documented and justified or remediated)
- [x] [P5-T8] Verify AC-1 (gate truth table) against the P3-T4(a) truth-table tests and check off the AC-1 checkbox in both `docs/features/active/2026-07-07-attendee-propose-new-time-130/spec.md` and `docs/features/active/2026-07-07-attendee-propose-new-time-130/user-story.md`
  - Acceptance: named passing tests cited; AC-1 checked `[x]` in both files
- [x] [P5-T9] Verify AC-2 (flag-off no-behavior-change: `propose_new_time_disabled` audit with four time columns, no Graph/dedupe writes, existing paths' `ActingFlags` byte-identical) against the P3-T4(a)/(b)/(h) tests and the P3-T6 regression-surface artifact and check off the AC-2 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: named passing tests and the regression-surface artifact cited; AC-2 checked `[x]` in both files
- [x] [P5-T10] Verify AC-3 (eligibility fail-closed matrix: organizer-owned, proposals disallowed, missing times, empty event id, zero slots each yield silent no-intent) against the P3-T4(f) tests and the P3-T5 monotonicity property and check off the AC-3 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: named passing tests cited; AC-3 checked `[x]` in both files
- [x] [P5-T11] Verify AC-4 (successful write: wire shape, correlation id, `proposed_new_time` audit, dedupe record, zero `RecordMoveAsync`) against the P1-T4(a)-(d) contract tests and the P3-T4(c) worker tests and check off the AC-4 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: named passing tests cited; AC-4 checked `[x]` in both files
- [x] [P5-T12] Verify AC-5 (fail-closed on adapter/Graph error: D5 matrix with `error.code` passthrough, 429/5xx retry exhaustion, `propose_new_time_failed` durable before rethrow with no dedupe row, local `NOT_SUPPORTED` with zero I/O and zero token acquisitions) against the P1-T4(e)-(f), P1-T5, P2-T3, and P3-T4(d) tests and check off the AC-5 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: named passing tests cited; AC-5 checked `[x]` in both files
- [x] [P5-T13] Verify AC-6 (idempotency: re-evaluation after success audits `dedupe_skipped` with dedupe key `{mailbox}:{messageId}:attendee-propose-new-time` and no Graph request) against the P3-T4(e) tests and check off the AC-6 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: named passing tests cited; AC-6 checked `[x]` in both files
- [x] [P5-T14] Verify AC-7 (mocked-Graph contract suite exists with the required coverage areas: method/URL/headers, exact body with absent-property guardrail, 202-empty-body mapping, D5 samples, 429 exhaustion) against `tests/OpenClaw.Core.Tests/CloudGraph/GraphHostAdapterClientProposeNewTimeTests.cs` (P1-T4, plus its split sibling if created) and check off the AC-7 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: file exists with named tests for each required area; AC-7 checked `[x]` in both files
- [x] [P5-T15] Verify AC-8 (quality gates: coverage thresholds, >= 1 CsCheck property per new pure function, architecture boundaries unmodified, mutual-exclusivity tests in both directions) against the P5-T5/P5-T6 coverage artifacts, the P3-T5 property tests, the P3-T4(g) exclusivity tests, and the green architecture-boundary suites and check off the AC-8 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: numeric coverage verdict PASS, all three properties passing, exclusivity tests passing, boundary suites green; AC-8 checked `[x]` in both files
- [x] [P5-T16] Verify AC-9 (runbook exists; `human_interaction` exception record with `runbook_path` produced for the orchestrator) against the P4-T1 runbook verification and P4-T2 record and check off the AC-9 checkbox in both `spec.md` and `user-story.md`
  - Acceptance: both artifacts exist at their exact paths; AC-9 checked `[x]` in both files
- [x] [P5-T17] Reconcile the plan checklist against evidence on disk: mark every completed task, confirm every evidence-producing task's artifact exists with complete schema fields, verify each acceptance criterion in the Acceptance Criteria Mapping table below traces to a completed task, and record the reconciliation summary (task-to-artifact table plus AC verdict column) at the bottom of this plan's Open Questions / Notes section
  - Acceptance: no checked task lacks its named artifact; every AC row has a completed implementing/verifying task; any gap flips the outcome to INCOMPLETE

## Acceptance Criteria Mapping

Spec and user-story AC (identical AC-1..AC-9 sets; both files are check-off targets) map to plan tasks as follows:

| AC | Summary | Implementing / verifying tasks |
|---|---|---|
| AC-1 | Gate truth table: exactly one POST on true/true; zero writes and zero write-path token acquisitions on the other three rows | P3-T2, P3-T4 (a), P5-T8 |
| AC-2 | Flag-off no-behavior-change: `propose_new_time_disabled` audit with four time columns; no Graph/dedupe writes; send path and F18 path `ActingFlags` byte-identical | P3-T2, P3-T3, P3-T4 (a)(b)(h), P3-T6, P5-T9 |
| AC-3 | Eligibility fail-closed matrix: organizer-owned, proposals disallowed, missing times, empty event id, zero slots -> silent no-intent | P3-T2, P3-T4 (f), P3-T5, P5-T10 |
| AC-4 | Successful write: wire shape (bearer, `client-request-id`, exact `sendResponse` + `proposedNewTime` body), `proposed_new_time` audit, dedupe record, zero `RecordMoveAsync` | P1-T2, P1-T4 (a)-(d), P3-T2, P3-T4 (c), P5-T11 |
| AC-5 | Fail-closed errors: D5 matrix with `error.code` passthrough, 429/5xx retry exhaustion, `propose_new_time_failed` with no dedupe row, local `NOT_SUPPORTED` zero-I/O | P1-T2, P1-T3, P1-T4 (e)-(f), P1-T5, P2-T2, P2-T3, P3-T4 (d), P5-T12 |
| AC-6 | Idempotency: re-evaluation after success audits `dedupe_skipped`, no Graph request | P3-T2, P3-T4 (e), P5-T13 |
| AC-7 | Mocked-Graph contract suite exists (`FakeHttpHandler`; method/URL/headers, exact body incl. absent-property guardrail, 202-empty-body mapping, D5 samples, 429 exhaustion under `FakeTimeProvider`) | P1-T4, P5-T14 |
| AC-8 | Quality gates: line >= 85% / branch >= 75%, no changed-line regression, >= 1 property per new pure function, architecture boundaries hold, mutual exclusivity asserted both directions | P0-T5, P3-T4 (g), P3-T5, P1-T6, P2-T4, P3-T7, P5-T1..P5-T6, P5-T15 |
| AC-9 | Live-verification runbook exists; `human_interaction` exception with `runbook_path` recorded for the orchestrator | P4-T1, P4-T2, P5-T16 |

## Test Plan

- **Contract (host-service boundary, mocked Graph):** `GraphHostAdapterClientProposeNewTimeTests.cs` — POST method/route with `/tentativelyAccept` suffix, headers, exact two-property body with absent-property guardrail (`JsonDocument` structural assertions; no `comment`, no top-level `start`/`end`/`body`/`subject`/`attendees`), 202-empty-body -> `ok: true, data: null`, D5 error samples with `BridgeErrorCode` passthrough, 429 exhaustion under `FakeTimeProvider` (P1-T4, split fallback `GraphHostAdapterClientProposeNewTimeErrorTests.cs`).
- **Adapter smoke negative:** local Stage-0 `NOT_SUPPORTED` fail-closed with zero HTTP invocations, in the new sibling file `HostAdapterHttpClientProposeNewTimeTests.cs` — a test ADD; `HostAdapterHttpClientTests.cs` (616 lines, pre-existing cap violation) remains unmodified (P1-T5).
- **Service seam:** delegation, correlation-id forwarding, failure-throw contract, id guard clause, in the new sibling file `HostAdapterSchedulingServiceProposeNewTimeTests.cs` (P2-T3).
- **Worker unit:** four-row gate truth table, dry-run detail, success ordering (audit -> dedupe record), failure fail-closed, dedupe skip, eligibility fail-closed matrix, mutual exclusivity both directions, send/F18 path `ActingFlags` isolation, zero `RecordMoveAsync` in every branch (P3-T4, split fallback `SchedulingWorkerProposeNewTimeEdgeTests.cs`).
- **Property-based (CsCheck, T1 obligation):** duration preservation, eligibility fail-closed monotonicity, flags-snapshot round-trip (P3-T5).
- **Architecture:** existing NetArchTest boundary suites pass unmodified — the new partial stays in `CloudGraph`; the worker partial stays host-neutral (P1-T6, P3-T7, P5-T5).
- **Regression:** zero pre-existing test-file modifications; `SchedulingWorker.cs`, `SchedulingWorker.Audit.cs`, `SchedulingWorker.Reschedule.cs`, and both existing ActingFlags builders textually unmodified (P3-T6).
- **Integration:** deliberately none in CI — live-tenant verification is the recorded `human_interaction` exception (P4-T1, P4-T2).
- **Coverage evidence:** baseline `evidence/baseline/csharp-test-coverage.<ts>.md` (P0-T5); post-change `evidence/qa-gates/csharp-test-coverage.<ts>.md` (P5-T5); comparison `evidence/qa-gates/coverage-comparison.<ts>.md` (P5-T6). Raw intermediates in `artifacts/csharp/`.

## Open Questions / Notes

- **CSharpier command form:** this repo uses the global `csharpier` 1.3.0 executable (`csharpier format .` / `csharpier check .`); there is no local tool manifest, so the `dotnet csharpier` driver form supplied in the delegation prompt is replaced with the repository-actual command form (F18 plan precedent).
- **`HostAdapterHttpClientTests.cs` deviation from the delegation prompt:** the delegation listed this file as a test MODIFY; it is a pre-existing 500-line-cap violation (616 lines, verified 2026-07-07) that F18 preflight already ruled must remain unmodified. The member-11 `NOT_SUPPORTED` test is therefore a test ADD in the new sibling `HostAdapterHttpClientProposeNewTimeTests.cs`, mirroring the F18 `HostAdapterHttpClientRescheduleTests.cs` split.
- **`HostAdapterSchedulingServiceTests.cs` not extended:** at 480 lines the file is near the cap; F19 service-seam tests go directly into the new sibling `HostAdapterSchedulingServiceProposeNewTimeTests.cs`, mirroring the F18 executed split (`HostAdapterSchedulingServiceRescheduleTests.cs`).
- **Test stack note:** MSTest + FluentAssertions + Moq + CsCheck + `FakeTimeProvider` + `FakeHttpHandler` is the repository's actual stack (per the research and F13/F15/F18 precedent), notwithstanding `.claude/rules/csharp.md`'s xUnit/NSubstitute wording.
- **Mutation testing (AC-8 adjacent):** Stryker.NET mutation score >= 75% on the changed T1 surface runs in the pre-merge/nightly pipeline per `.claude/rules/general-code-change.md`; it is not a task in this per-commit plan. The truth-table and fail-closed branches are the mutation-sensitive surface, so P3-T4 asserts both the action taken and the actions not taken (zero Graph calls, zero token acquisitions, zero `RecordMoveAsync` calls).
- **Purview projection:** the optional explicit `MapActionType` case for `attendee-propose-new-time` is out of scope; the documented fallback covers the new action type without a change (research Section 3.4, F18 precedent).
- **`SendOnBehalfAuthorizer` non-interaction:** the tentativelyAccept POST targets the principal's own event under app-only `Calendars.ReadWrite`; no mailbox representation occurs, so the allowlist is intentionally not consulted (spec Non-Goals). Reviewers must not flag its absence on this path.
- **No move guard / no blocked code (explicit decision):** the attendee path performs no calendar move — the organizer applies the change only by separately accepting the proposal. Consulting or recording the F18 move budget would corrupt `series_moves` history for future organizer reschedules; idempotency is bounded by the per-message dedupe key. The F19 state machine is a strict subset of F18's (intent -> gate -> dedupe -> write -> bookkeeping).
- **Additive dry-run output:** today's pipeline computes no propose-new-time intent, so the dry-run log line and `propose_new_time_disabled` audit rows are new, additive output; "no behavior change" (AC-2) is scoped to outbound side effects — no Graph request, no write-path token acquisition, no dedupe row — plus byte-identity of the existing send path's and F18 path's persisted `ActingFlags`.
- **Runbook already authored:** `runbooks/attendee-propose-new-time-live-verification.runbook.md` exists in the feature folder; P4-T1 is a verification task (content-area check), not an authoring task.
- **Orchestrator checkpoint:** the `human_interaction` requirement (P4-T2) is recorded in `artifacts/orchestration/orchestrator-state.json` by the orchestrator; the executor produces the evidence record only.
- **Plan-path continuity:** this file (`plan.2026-07-07T05-21.md`) is the single canonical plan path; all preflight revisions update it in place with no timestamped siblings.

## Reconciliation Summary (P5-T17, 2026-07-07T05-56)

All tasks P0-T1..P5-T17 are checked `[x]`. Final QC toolchain passed in a single pass:
csharpier check exit 0; `dotnet build -warnaserror` 0 warnings/0 errors; `dotnet test` 1377
passed / 0 failed / 5 skipped (expected COM/publish host-bound); NetArchTest boundary suites
green and unmodified. OpenClaw.Core coverage line 99.29% / branch 92.28% (baseline 99.27% /
92.24%, no regression); new files line 100%, branch 100% / 93.75%.

### Task-to-artifact reconciliation (evidence-producing tasks)

| Task | Artifact (exists, schema-complete) |
|---|---|
| P0-T1/T2 | evidence/baseline/phase0-instructions-read.md |
| P0-T3 | evidence/baseline/csharp-format.2026-07-07T05-56.md |
| P0-T4 | evidence/baseline/csharp-build.2026-07-07T05-56.md |
| P0-T5 | evidence/baseline/csharp-test-coverage.2026-07-07T05-56.md |
| P3-T6 | evidence/regression-testing/existing-path-regression-surface.2026-07-07T05-56.md |
| P4-T1 | evidence/other/runbook-verification.2026-07-07T05-56.md |
| P4-T2 | evidence/other/human-interaction-record.2026-07-07T05-56.md |
| P5-T1 | evidence/qa-gates/file-size-cap.2026-07-07T05-56.md |
| P5-T2 | evidence/qa-gates/test-hygiene.2026-07-07T05-56.md |
| P5-T3 | evidence/qa-gates/csharp-format.2026-07-07T05-56.md |
| P5-T4 | evidence/qa-gates/csharp-build.2026-07-07T05-56.md |
| P5-T5 | evidence/qa-gates/csharp-test-coverage.2026-07-07T05-56.md |
| P5-T6 | evidence/qa-gates/coverage-comparison.2026-07-07T05-56.md |
| P5-T7 | evidence/other/diff-scope-verification.2026-07-07T05-56.md |

### Acceptance-criteria verdict (checked `[x]` in both spec.md and user-story.md)

| AC | Implementing/verifying tasks | Verdict |
|---|---|---|
| AC-1 | P3-T2, P3-T4(a), P5-T8 | PASS |
| AC-2 | P3-T2/T3, P3-T4(a)(b)(h), P3-T6, P5-T9 | PASS |
| AC-3 | P3-T2, P3-T4(f), P3-T5, P5-T10 | PASS |
| AC-4 | P1-T2, P1-T4(a)-(d), P3-T2, P3-T4(c), P5-T11 | PASS |
| AC-5 | P1-T2/T3, P1-T4(e)-(f), P1-T5, P2-T2/T3, P3-T4(d), P5-T12 | PASS |
| AC-6 | P3-T2, P3-T4(e), P5-T13 | PASS |
| AC-7 | P1-T4, P5-T14 | PASS |
| AC-8 | P0-T5, P3-T4(g), P3-T5, P1-T6, P2-T4, P3-T7, P5-T1..T6, P5-T15 | PASS |
| AC-9 | P4-T1, P4-T2, P5-T16 | PASS |

No checked task lacks its named artifact; every AC row traces to a completed task. Outcome:
COMPLETE. No commit or PR was created by the executor (per directive; orchestrator handles both).
