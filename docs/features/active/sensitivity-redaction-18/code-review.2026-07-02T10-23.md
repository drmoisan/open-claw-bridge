# Code Review: sensitivity-redaction (#18, co-delivers #20) — Remediation Cycle 1 Re-audit (R4)

**Review Date:** 2026-07-02
**Branch:** `feature/sensitivity-redaction-18` @ `82504ff12a8ccda9ac64d0535356769c8f1b01fa`
**Base:** `main` @ merge-base `8c969f1a6e96120dd95f835a289c8b185abee202`
**Scope:** Full branch diff (4 production C# files, 10 test C# files, docs, feature evidence). The remediation cycle 1 delta (`d267c66..82504ff`) is test-only and was reviewed line-by-line; the production files are unchanged since the prior review (`code-review.2026-07-02T09-45.md`), whose design and correctness findings are re-affirmed rather than repeated.

## Executive Summary

Remediation cycle 1 delivers exactly what the remediation inputs required, with no production changes and no scope creep. The new `OutlookScannerSensitivityNormalizationEdgeTests.cs` closes the Blocking coverage gap the right way — through the real scan pipeline rather than by poking the pure transform: sensitive meeting messages (`IPM.Schedule.Meeting.Request`, both Sensitivity 2 and 3 via `DataRow`) are asserted for the full Group A redaction disposition plus `ItemKind == "meeting"`, the meeting-variant `BridgeId` encoding, retained `MeetingMessageType`, and zero protected-member accesses. The null-`MessageClass` and `Attachments`-short-circuit tests target the two remaining uncovered conditions precisely, and the two hard never-ingest tests (`ThrowOnProtectedAccess = true` for both item kinds) convert the previously dead test-double capability into the strongest available form of the never-ingest guarantee: the sensitive path completes fully redacted even when every protected COM getter throws.

The new `OutlookScannerRedactionInvariantTests.cs` resolves the Major T2 property-test-density finding via the directed option (b): deterministic, fully enumerated invariant suites. The `IsSensitive` test asserts the equivalence `IsSensitive(n) == (n is 2 or 3)` over an 11-value domain (exhaustive 0..3, null, and six out-of-range boundary representatives including `int.MinValue`/`int.MaxValue`), with the domain-sampling rationale documented in a comment. The transform matrices assert the three invariants a property test would generate — exact-protected-set transformation, complete mechanical-field preservation (all 11 message / all 16 event fields), and idempotence via structural equivalence — across 4-variant input matrices that include the already-redacted-input case (variant `IsRedacted: true`), which meaningfully exercises idempotence rather than restating the basic disposition. Assertions carry the failing variant's `BridgeId` in `because` messages, so matrix failures are self-identifying.

Test-double changes are minimal and additive (`MeetingType`, `Attachments`, nullable `MessageClass` with the `"IPM.Note"` default retained), so all pre-existing tests compile and pass unchanged. No new dependencies, no suppressions, no temp files, no wall-clock reads. All 660 runnable tests pass at head; the new file is at 100% line / 100% branch (reviewer re-measured).

No Blocking or Major findings remain. The findings table below records only carried-forward Minor/Info observations, none of which block merge.

Typed-Python review: not applicable — no Python files changed on this branch.

## Findings Table

| Severity | File | Location | Finding | Recommendation | Rationale | Evidence |
|---|---|---|---|---|---|---|
| Resolved (was Blocking) | tests/OpenClaw.MailBridge.Tests/OutlookScannerSensitivityNormalizationEdgeTests.cs | whole file | Prior finding: no test scanned a sensitive meeting-typed message; new-file branch coverage 71.43% < 75%. Now covered end-to-end: meeting-kind redaction (2/3), null-`MessageClass` fallback, `Attachments` short-circuit; `OutlookScanner.Redaction.cs` re-measured at 100% branch (14/14). | None — closed. | Uniform new-code branch gate (quality-tiers.md) now met with margin. | Reviewer cobertura: `evidence/qa-gates/coverage-review.2026-07-02T10-23.md` (line 63: 6/6; line 170: 2/2) |
| Resolved (was Major) | tests/OpenClaw.MailBridge.Tests/OutlookScannerRedactionInvariantTests.cs | whole file | Prior finding: T2 property-test density unmet for `IsSensitive`/`RedactMessage`/`RedactEvent`. Resolved via directed option (b): full-domain/parameterized invariant + idempotence suites with a dated decision record deferring CsCheck introduction. | When a repo-wide property-testing harness lands (tracked separately per the decision record), convert these invariant suites as first candidates. | Dependency-minimization policy (general-code-change.md) legitimately blocks a mid-remediation package addition; the delivered tests verify the same invariants deterministically. | `evidence/other/property-test-decision.2026-07-02T10-07.md`; 7/7 invariant tests pass at head |
| Resolved (was Minor) | tests/OpenClaw.MailBridge.Tests/SensitivityRedactionTestDoubles.cs | `ThrowOnProtectedAccess` | Prior finding: the throw-on-access capability was dead test-support code. Now exercised by two hard never-ingest tests (mail Sensitivity 2, appointment Sensitivity 3). | None — closed. | The throwing configuration upgrades the never-ingest evidence from "no access recorded" to "any access would have failed the scan." | `Sensitive_mail_scan_with_throwing_protected_members_should_stay_redacted`, `Sensitive_event_scan_with_throwing_protected_members_should_stay_redacted` |
| Minor (carried, no action this cycle) | src/OpenClaw.MailBridge/OutlookScanner.Redaction.cs | `NormalizeSensitiveMessage` (lines 55-91), `BuildSensitiveEventDto` (lines 110-157) | The sensitive builders duplicate the mechanical-field COM reads of their unredacted counterparts; a future mechanical-field addition must be made in two places or the DTOs silently diverge. | When a next mechanical field lands, extract a shared mechanical-fields reader consumed by both builders. Explicitly deferred by the remediation inputs ("recorded for future refactoring pressure only"). | Deliberate never-ingest trade-off, documented in the file's XML doc; retention tests (scanner-level and now matrix-level) act as the drift alarm. | Redaction.cs vs OutlookScanner.cs and GraphFields.cs builders; remediation-inputs Optional section |
| Minor (carried, no action this cycle) | src/OpenClaw.MailBridge/OutlookScanner.Redaction.cs | line 141 (`IsOrganizer: responseStatus == 1`) | The `IsOrganizer` derivation is duplicated from `BuildEventDto` (GraphFields.cs) with no cross-referencing comment. | Fold into the shared extraction above or add a one-line twin-site comment at next touch. | Encodes a semantic mapping (olResponseOrganizer = 1) at two sites. | Redaction.cs line 141; GraphFields.cs line 80 |
| Info | tests/OpenClaw.MailBridge.Tests/OutlookScannerRedactionInvariantTests.cs | matrix builders | The message matrix includes a variant with `IsRedacted: true` and `MeetingMessageType: 3` on an `IPM.Schedule.Meeting.Canceled` class; the event matrix includes populated-Categories and already-null variants. The matrices exercise input combinations the scanner itself cannot produce (e.g., redacted-input-with-populated-subject is impossible post-normalization), which is correct for a pure-function invariant suite: the transform's contract is defined over all DTOs, not only scanner-reachable ones. | None — this is the intended strength of contract-level testing. | Pure functions are specified over their full input type; testing beyond scanner-reachable states is what distinguishes these from the existing example tests. | Matrix definitions, lines 58-156 and 217-339 |
| Info | tests/OpenClaw.MailBridge.Tests/OutlookScannerSensitivityNormalizationEdgeTests.cs | `AssertNoProtectedAccess` / inline event assertion | The never-ingest assertion helper is duplicated once inline for the appointment double (different double type, no shared interface). Acceptable at current scale; a shared assertion extension would only pay off if a third double kind appears. | None this cycle. | Two occurrences of a 6-line assertion do not justify new test-support abstraction (simplicity-first). | Lines 115-121 vs 213-217 |

## Design and Correctness Review (narrative, remediation delta)

- **Right level of test.** The remediation inputs required scanner-level coverage, not transform-level: the edge tests drive `ScanInboxAsync`/`ScanCalendarAsync` through the fake COM graph, so the previously unexecuted branches are exercised in the exact production path (`IsMeetingItem` type-name probe, `MessageClass` fallback, `GetOptionalBool` or-chain, `MeetingType` read gated on `isMeeting`).
- **BridgeId meeting-variant assertion.** `dto.BridgeId.Should().Be(BridgeIdCodec.MessageId("entry-sensitive-mail", true))` pins the meeting flag's effect on id encoding, which none of the prior tests distinguished from the mail variant.
- **Idempotence is load-bearing.** `Redact(Redact(x)) == Redact(x)` via `BeEquivalentTo` (structural, correctly chosen because `EventDto.Categories` is an array member with referential record equality) protects the cache-rewrite path: an already-redacted row re-scanned and re-normalized must not change shape.
- **No assertion weakening, no production drift.** The delta modifies one existing file (`SensitivityRedactionTestDoubles.cs`) additively; `git diff --name-only d267c66..82504ff` contains zero `src/` paths; no existing test was modified or removed; the spec/issue AC text is byte-identical with only checkbox state and the dated re-verification sub-bullet changed, matching the remediation plan's binding constraints.

## Toolchain Verification (reviewer, at head `82504ff`)

| Stage | Command | Result |
|---|---|---|
| Format | `csharpier check .` | EXIT 0, 204 files |
| Lint/type | `dotnet build OpenClaw.MailBridge.sln` | 0 warnings, 0 errors |
| Architecture | NetArchTest filter run | 2/2 pass |
| Tests + coverage | `dotnet test ... --collect:"XPlat Code Coverage"` | 660 pass / 0 fail / 5 env-gated skips |
| Per-file coverage | reviewer cobertura parse | New file 100% line / 100% branch — PASS |

## Verdict

**Approve.** The remediation is complete, minimal, and verified: both prior findings (and the bundled Minor) are closed with reviewer-independent evidence at the current head. Remaining observations are carried-forward Minor/Info items explicitly deferred by the remediation inputs; none block merge.
