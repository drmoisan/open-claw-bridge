# sqlite-advisory-nu1903 (Plan — Revised, Option B: SQLitePCLRaw 3.x native override)

- **Issue:** #92
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-01T20-30
- **Status:** Draft
- **Version:** 0.3
- **Work Mode:** minor-audit
- **Tier:** T1 (OpenClaw.Core and OpenClaw.MailBridge data layer)
- **Supersedes:** `plan.2026-07-01T19-46.md` (invalidated premise: "SQLitePCLRaw.lib.e_sqlite3 >= 2.1.10 clears GHSA-2m69-gcr7-jv3q" was proven false during execution — NuGet audit flags the entire 2.1.x line incl. 2.1.11, and no Microsoft.Data.Sqlite 8/9/10.x release transitively clears it; only the SQLitePCLRaw 3.x line clears it).

**Requirements source:** `docs/features/active/sqlite-advisory-nu1903-92/issue.md` is the sole requirements source. The `## Acceptance Criteria` section (AC-1..AC-7) is the only acceptance-criteria source for this minor-audit. `spec.md` and `user-story.md` are not required and must not exist in this feature folder.

**Fail-closed evidence rule:** This plan includes explicit baseline, targeted-verification, and end-state evidence tasks with numeric coverage capture. If any required baseline artifact, QA artifact, coverage-comparison artifact, or the AC-4 runtime-verification artifact is missing or contains placeholders, the verdict is BLOCKED or INCOMPLETE, never PASS.

**AC-7 HARD-GATE rule:** This is an unsupported package combination (SQLitePCLRaw 3.x native provider against a Microsoft.Data.Sqlite core built for the 2.x provider). If (a) `dotnet restore` cannot produce a coherent graph without an unresolvable version conflict, OR (b) the SQLite-backed cache/DB tests fail at runtime because the native `e_sqlite3` 3.x provider does not load or the SQLitePCLRaw 3.x / Microsoft.Data.Sqlite core mismatch breaks the cache open/read/write paths, the work STOPS. The executor writes the AC-7 stop artifact and surfaces to the operator. Forcing the combination and silent fallback to advisory suppression are both prohibited.

**Evidence location invariant:** All evidence artifacts resolve to `docs/features/active/sqlite-advisory-nu1903-92/evidence/<kind>/` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. Non-canonical `artifacts/` evidence paths are forbidden and are rejected in favor of the canonical path.

**Scope facts confirmed during planning:**
- `Microsoft.Data.Sqlite` is pinned per-csproj at `8.0.11` in both `src/OpenClaw.Core/OpenClaw.Core.csproj` (line 17) and `src/OpenClaw.MailBridge/OpenClaw.MailBridge.csproj` (line 23). No `Directory.Packages.props` or `Directory.Build.props` exists, so the edits are made in both `.csproj` files directly (not centrally).
- No `.config/dotnet-tools.json` (local tool manifest) exists in this repository. `dotnet tool restore` has no manifest to restore. CSharpier is invoked via the global subcommand form `csharpier check .` / `csharpier format .` (CSharpier 1.x). Do not use `dotnet csharpier` or `dotnet tool restore`.
- Solution file: `OpenClaw.MailBridge.sln`. Coverage runsettings: `mailbridge.runsettings` (cobertura, excludes `[*.Tests]*`).
- Target frameworks: `net10.0` (Core) and `net10.0-windows` (MailBridge). SDK pinned to `10.0.201` in `global.json`.
- Test framework is MSTest; all test invocations use framework-agnostic `dotnet test` commands.
- Prior execution proved: the SQLitePCLRaw 3.x line (`SQLitePCLRaw.lib.e_sqlite3` 3.50.3 via `SQLitePCLRaw.bundle_e_sqlite3` 3.0.0) clears NU1903 (0 occurrences) against a scratch project. The exact minimal working package set for the repo (whether `bundle_e_sqlite3` 3.0.0 alone suffices, or `SQLitePCLRaw.core` 3.x and/or a `Microsoft.Data.Sqlite` bump are also required to produce a coherent restore graph) is resolved during execution in P1-T1 and recorded.

**Reused baselines (Phase 0):** The prior execution captured valid baselines at `2026-07-01T19-46` for the NU1903 fail state, dependency versions, CSharpier state, and test+coverage state. These are reused rather than recaptured because the working tree was reverted to the identical baseline (`Microsoft.Data.Sqlite` 8.0.11 in both csproj, transitive `SQLitePCLRaw.lib.e_sqlite3` 2.1.6). Phase 0 below re-reads policy and validates that each reused baseline artifact is present and schema-complete; it recaptures only if an artifact is absent or the tree no longer matches the reverted baseline.

---

### Phase 0 — Policy Read & Baseline Validation (Reuse)

- [x] [P0-T1] Read policy files in the required order and record the read evidence: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/architecture-boundaries.md`. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/phase0-instructions-read.2026-07-01T20-30.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
  - Acceptance: the artifact exists with `Timestamp:`, `Policy Order:`, and all six files listed as read. Supports AC-6.

- [x] [P0-T2] Validate the reused baseline dependency-version artifact: confirm `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-dependency-versions.md` exists and records `Microsoft.Data.Sqlite 8.0.11` in both csproj and transitive `SQLitePCLRaw.lib.e_sqlite3 2.1.6`, and confirm the working tree still matches (`git diff --stat` on both csproj shows no change). Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-dependency-reuse-check.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (the git verification command), `EXIT_CODE:`, and `Output Summary:` (confirming reuse validity or, if the tree diverged, recording a recapture). If the artifact is absent or the tree diverged, recapture via `dotnet list OpenClaw.MailBridge.sln package --include-transitive` and record the current values.
  - Acceptance: the reuse-check artifact confirms `Microsoft.Data.Sqlite 8.0.11` (both csproj) and transitive `SQLitePCLRaw.lib.e_sqlite3 2.1.6`, or records a fresh recapture with the same fields. Supports AC-1.

- [x] [P0-T3] Validate the reused baseline NU1903 fail-state artifact: confirm `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-nu1903-build.md` exists with a non-zero `EXIT_CODE:` and an `Output Summary:` quoting the `NU1903` GHSA-2m69-gcr7-jv3q error for `SQLitePCLRaw.lib.e_sqlite3` 2.1.6. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-nu1903-reuse-check.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (validation or, if absent, `dotnet build OpenClaw.MailBridge.sln -c Release /warnaserror`), `EXIT_CODE:`, and `Output Summary:`. If the artifact is absent, recapture the failing build.
  - Acceptance: the reuse-check artifact confirms the fail-before NU1903 state (non-zero exit quoting GHSA-2m69-gcr7-jv3q) or records a fresh recapture. Establishes the fail-before state for AC-2.

- [x] [P0-T4] Validate the reused baseline CSharpier artifact: confirm `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-csharpier.md` exists with `Command: csharpier check .` and its result. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-csharpier-reuse-check.2026-07-01T20-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:`. If the artifact is absent, recapture with `csharpier check .`.
  - Acceptance: the reuse-check artifact confirms the baseline CSharpier state or records a fresh recapture, with all four fields populated. Supports AC-6.

- [x] [P0-T5] Validate the reused baseline test+coverage artifact: confirm `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-test-coverage.md` exists and records `587 passed, 5 skipped, 0 failed` and pooled coverage `line 90.73%`, `branch 79.31%`. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/baseline/baseline-test-coverage-reuse-check.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (validation or, if absent, `dotnet test OpenClaw.MailBridge.sln -c Release --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`), `EXIT_CODE:`, and `Output Summary:` including the numeric baseline line% (90.73%) and branch% (79.31%) and the passed/failed/skipped counts. If the artifact is absent, recapture.
  - Acceptance: the reuse-check artifact records numeric baseline line% (90.73%) and branch% (79.31%) and the test counts, or records a fresh recapture. These are the pre-change baseline figures for the P2-T7 delta comparison. Supports AC-4.

### Phase 1 — SQLitePCLRaw 3.x Native Override (Option B)

- [x] [P1-T1] Resolve the exact minimal working package set that forces transitive `SQLitePCLRaw.lib.e_sqlite3` to the 3.x line (`>= 3.50.3`) with a coherent restore graph. Starting from a direct `SQLitePCLRaw.bundle_e_sqlite3` `3.0.0` reference, determine whether that alone suffices or whether a companion `SQLitePCLRaw.core` 3.x reference and/or a `Microsoft.Data.Sqlite` bump is also required for `dotnet restore` to succeed without an unresolvable version conflict. Perform resolution against a scratch probe (not the repo working tree) and record the resolved set. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/other/resolved-3x-package-set.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (the resolution/probe commands, e.g. `dotnet restore` and `dotnet list <probe>.csproj package --include-transitive`), `EXIT_CODE:`, and `Output Summary:` (the exact package IDs+versions in the resolved set and the confirmed transitive `SQLitePCLRaw.lib.e_sqlite3` version `>= 3.50.3`).
  - Acceptance: the artifact records one exact minimal package set (each package ID with pinned version) that resolves the transitive `SQLitePCLRaw.lib.e_sqlite3` to `>= 3.50.3` with `EXIT_CODE: 0` on restore. If no coherent graph exists, do not record a set — proceed to the AC-7 HARD GATE (P1-T5). Supports AC-1, AC-7.

- [x] [P1-T2] Edit `src/OpenClaw.Core/OpenClaw.Core.csproj` to add the resolved P1-T1 package set (the direct `SQLitePCLRaw.bundle_e_sqlite3` 3.x reference plus any companion package, and the `Microsoft.Data.Sqlite` version only if P1-T1 requires a bump) into the existing `<ItemGroup>` at lines 16-18. Make only the additions/edits the P1-T1 resolved set requires; introduce no `NoWarn`, `NuGetAuditMode`, or `NuGetAuditSuppress`.
  - Acceptance: `src/OpenClaw.Core/OpenClaw.Core.csproj` contains exactly the P1-T1 resolved SQLitePCLRaw 3.x package reference(s) at the resolved versions; no advisory-suppression attribute is present; no other line changed. Supports AC-1, AC-3, AC-5.

- [x] [P1-T3] Edit `src/OpenClaw.MailBridge/OpenClaw.MailBridge.csproj` to add the identical P1-T1 package set into the existing `<ItemGroup>` at lines 22-30, using the exact same package IDs and version strings as P1-T2 (lockstep). Make only the additions/edits the P1-T1 resolved set requires; introduce no `NoWarn`, `NuGetAuditMode`, or `NuGetAuditSuppress`.
  - Acceptance: `src/OpenClaw.MailBridge/OpenClaw.MailBridge.csproj` contains the same SQLitePCLRaw 3.x package IDs and identical version strings as `src/OpenClaw.Core/OpenClaw.Core.csproj`; no advisory-suppression attribute is present. Supports AC-1, AC-3.

- [x] [P1-T4] Verify no advisory suppression was introduced and the diff is scoped: run `git diff` on both csproj and grep the full diff for `NU1903`, `NoWarn`, `NuGetAuditMode`, and `NuGetAuditSuppress`. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/other/no-suppression-check.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (the git diff and grep commands), `EXIT_CODE:`, and `Output Summary:` (zero suppression occurrences; diff limited to the SQLitePCLRaw 3.x package additions in the two csproj plus any P1-T1-required `Microsoft.Data.Sqlite` bump).
  - Acceptance: the artifact confirms zero new `NoWarn NU1903` / `NuGetAuditMode` / `NuGetAuditSuppress`, and the diff is limited to the two csproj SQLitePCLRaw 3.x additions (plus any justified P1-T1 `Microsoft.Data.Sqlite` bump). Supports AC-3, AC-5.

- [x] [P1-T5] AC-7 HARD GATE — restore-graph coherence: run `dotnet restore OpenClaw.MailBridge.sln` after the P1-T2/P1-T3 edits. If restore fails with an unresolvable version conflict attributable to the SQLitePCLRaw 3.x / Microsoft.Data.Sqlite core mismatch (for example NU1605/NU1107 or an unsatisfiable downgrade), STOP: revert P1-T2/P1-T3, write `docs/features/active/sqlite-advisory-nu1903-92/evidence/other/ac7-stop-restore-conflict.2026-07-01T20-30.md` with `Timestamp:`, `Command: dotnet restore OpenClaw.MailBridge.sln`, `EXIT_CODE:` (non-zero), and `Output Summary:` (the exact conflict diagnostic and the reverted tree state), and halt for operator decision. Do not force resolution and do not add suppression. If restore succeeds, record `EXIT_CODE: 0` and continue.
  - Acceptance: either (a) `dotnet restore` succeeds (`EXIT_CODE: 0`) and execution continues, or (b) the AC-7 stop artifact records the unresolvable conflict, the tree is reverted to baseline, and execution halts. Supports AC-7.

### Phase 2 — Runtime Verification, Targeted Verification & Final QC Loop

- [x] [P2-T1] Targeted verification of NU1903 clearance: run `dotnet build OpenClaw.MailBridge.sln -c Release /warnaserror` and write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/targeted-nu1903-cleared.2026-07-01T20-30.md` with `Timestamp:`, `Command: dotnet build OpenClaw.MailBridge.sln -c Release /warnaserror`, `EXIT_CODE:` (expected 0), and `Output Summary:` confirming 0 `NU1903` occurrences and no new `NUxxxx` advisory. This command must execute; `SKIPPED` is not a valid outcome.
  - Acceptance: the artifact records `EXIT_CODE: 0` and `Output Summary:` states 0 `NU1903` and no new `NUxxxx`. Supports AC-2, AC-6.

- [x] [P2-T2] AC-4 RUNTIME VERIFICATION (unsupported-combo hard check) — SQLite-backed provider load and cache open/read/write: run the SQLite-backed cache/DB tests that exercise the native `e_sqlite3` provider at runtime in both suites via `dotnet test tests/OpenClaw.Core.Tests/OpenClaw.Core.Tests.csproj -c Release --settings mailbridge.runsettings --filter "FullyQualifiedName~Sqlite|FullyQualifiedName~Cache|FullyQualifiedName~Db"` and `dotnet test tests/OpenClaw.MailBridge.Tests/OpenClaw.MailBridge.Tests.csproj -c Release --settings mailbridge.runsettings --filter "FullyQualifiedName~Sqlite|FullyQualifiedName~Cache|FullyQualifiedName~Db"` (the executor resolves the exact test-class/namespace filter from the actual SQLite-backed test names in each suite, and if a precise filter cannot be constructed, runs the full suite for that project so the SQLite-backed tests are exercised). Confirm the native `e_sqlite3` 3.x provider loads (no `DllNotFoundException` / `SQLitePCLRaw` provider-init failure / `Microsoft.Data.Sqlite` core-mismatch exception) and the cache open/read/write paths pass. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/ac4-runtime-sqlite-provider.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (both filtered test commands), `EXIT_CODE:` (expected 0 for each), and `Output Summary:` (the SQLite-backed test names executed, pass counts, and an explicit statement that the native e_sqlite3 3.x provider loaded and the open/read/write paths passed at runtime). This command must execute; `SKIPPED` is not valid.
  - Acceptance: both filtered runs report `EXIT_CODE: 0` with all SQLite-backed cache/DB tests passing and the `Output Summary:` explicitly confirming the native `e_sqlite3` 3.x provider loaded and cache open/read/write passed at runtime. If any SQLite-backed test fails due to provider-load or core-mismatch, do not continue — proceed to the AC-7 runtime HARD GATE (P2-T3). Supports AC-4, AC-7.

- [x] [P2-T3] AC-7 HARD GATE — runtime mismatch: if any SQLite-backed test in P2-T2 fails at runtime because the native `e_sqlite3` 3.x provider does not load or the SQLitePCLRaw 3.x / Microsoft.Data.Sqlite core mismatch breaks the cache open/read/write paths, STOP: write `docs/features/active/sqlite-advisory-nu1903-92/evidence/other/ac7-stop-runtime-mismatch.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (the failing test command), `EXIT_CODE:` (non-zero), and `Output Summary:` (the exact runtime exception, e.g. provider-init failure or core-mismatch, and the affected test names), and halt for operator decision. Do not force it and do not fall back to advisory suppression. If P2-T2 passed (native provider loaded, cache paths green), record `RUNTIME GATE PASSED` in this artifact and continue.
  - Acceptance: either (a) P2-T2 passed and this artifact records `RUNTIME GATE PASSED`, or (b) the AC-7 runtime-stop artifact records the exact provider-load/core-mismatch failure and execution halts with no suppression applied. Supports AC-7.

- [x] [P2-T4] Final QC — Formatting: run `csharpier format .` then `csharpier check .`; write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/final-csharpier.2026-07-01T20-30.md` with `Timestamp:`, `Command: csharpier check .`, `EXIT_CODE:`, and `Output Summary:`. If `csharpier format .` changes any file, restart the final QC loop from this task.
  - Acceptance: `csharpier check .` reports `EXIT_CODE: 0` with no unformatted files. Supports AC-6.

- [x] [P2-T5] Final QC — Lint/analyzers and nullable/type-check: run `dotnet build OpenClaw.MailBridge.sln -c Release /warnaserror` (the build under warnings-as-errors serves as the analyzer + nullable gate); write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/final-build-analyzers.2026-07-01T20-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` (0 analyzer warnings, 0 nullable warnings, 0 `NUxxxx` advisories). If this step changes files, restart the final QC loop from P2-T4.
  - Acceptance: the artifact records `EXIT_CODE: 0` with 0 analyzer/nullable warnings and 0 `NUxxxx`. Supports AC-2, AC-6.

- [x] [P2-T6] Final QC — Architecture-boundary verification: verify the `ProjectReference` graph in both edited csproj is unchanged and still conforms to `.claude/rules/architecture-boundaries.md` (the SQLitePCLRaw 3.x override adds no new `ProjectReference` and pulls no Outlook COM into `OpenClaw.Core`). Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/final-architecture.2026-07-01T20-30.md` with `Timestamp:`, `Command:` (e.g. `git diff` on `ProjectReference` lines plus a dependency-closure check), `EXIT_CODE:`, and `Output Summary:`.
  - Acceptance: the artifact confirms 0 architecture-boundary violations and that no new `ProjectReference` was introduced by the override. Supports AC-6.

- [x] [P2-T7] Final QC — Full test suite with coverage: run `dotnet test OpenClaw.MailBridge.sln -c Release --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`; write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/final-test-coverage.2026-07-01T20-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including numeric post-change pooled line coverage %, branch coverage %, and passed/failed/skipped test counts across all three test assemblies. This command must execute; `SKIPPED` is not valid.
  - Acceptance: `EXIT_CODE: 0`; all tests pass (no new failures vs. the 587 passed / 5 skipped baseline); line coverage `>= 85%` and branch coverage `>= 75%` recorded numerically. Supports AC-4, AC-6.

- [x] [P2-T8] Coverage delta / no-changed-line-regression verification: compare the reused baseline coverage (P0-T5: line 90.73%, branch 79.31%) against post-change coverage (P2-T7) and confirm no regression on changed lines. Write `docs/features/active/sqlite-advisory-nu1903-92/evidence/qa-gates/coverage-delta.2026-07-01T20-30.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` reporting baseline line%/branch% (90.73% / 79.31%), post-change line%/branch%, and changed-line coverage. Because the change is package-reference-only (no product-code change expected), the changed-line surface is the csproj edits; if AC-5 forces a narrowly-scoped product-code adjustment, its changed lines must be covered. If required numeric coverage is unavailable, the outcome is remediation-required, not PASS.
  - Acceptance: the artifact reports baseline, post-change, and changed-line coverage with no regression on the edited lines; line `>= 85%` and branch `>= 75%` maintained. Supports AC-4, AC-5.

### Phase 3 — End-State Evidence & Status

- [x] [P3-T1] Write the end-state evidence artifact `docs/features/active/sqlite-advisory-nu1903-92/evidence/other/end-state.2026-07-01T20-30.md` summarizing: the resolved SQLitePCLRaw 3.x package set added to both csproj (in lockstep), the resulting transitive `SQLitePCLRaw.lib.e_sqlite3` version (`>= 3.50.3`), the cleared NU1903 build (`EXIT_CODE: 0`), the AC-4 runtime provider-load confirmation, no suppression introduced, and the final coverage numbers. Include `Timestamp:` and a mapping table from AC-1..AC-7 to the concrete evidence artifact paths that satisfy each.
  - Acceptance: the artifact exists, maps every AC-1..AC-7 to a concrete evidence path, and records the resolved 3.x package set, cleared-advisory state, and runtime-verification result. Supports AC-1..AC-7.

- [x] [P3-T2] Update the issue.md evidence checklist and mirror: mark `baseline`, `targeted verification`, and `end-state` complete in `docs/features/active/sqlite-advisory-nu1903-92/issue.md`, and write the mirror `docs/features/active/sqlite-advisory-nu1903-92/evidence/issue-updates/issue-92.2026-07-01T20-30.md` with `Timestamp:`, the exact updated text, and `PostedAs: body` (or `PostedAs: unknown` if not posted to GitHub).
  - Acceptance: the issue.md Evidence Checklist boxes are checked and the issue-update mirror exists with the required fields. Supports AC-6.
