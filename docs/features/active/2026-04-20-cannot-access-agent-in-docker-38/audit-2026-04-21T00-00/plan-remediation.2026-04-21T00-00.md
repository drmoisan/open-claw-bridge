# 2026-04-20-cannot-access-agent-in-docker (Remediation Plan)

- **Issue:** #38
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-04-21T00-00
- **Status:** Draft
- **Version:** 1.1 (remediation cycle following 1.0 plan at `plan.2026-04-20T09-21.md`)
- **Work Mode:** full-bug
- **Budget Path:** large (3 production PowerShell files modified: `scripts/Invoke-OpenClawAgentOnboarding.ps1` for R2, `scripts/Invoke-OpenClawContainerPathValidation.ps1` for R3, and `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` for R3 module thread-through). Routed per `powershell-change-budget-router` contract (>2 production files → large).

**Fail-closed evidence rule:** Every evidence-producing task declares its expected artifact path. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing or incomplete, the audit verdict is BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path in each evidence-producing task. Do not mark evidence-backed work complete without the artifact on disk. Each command-step artifact must include the four required fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.

**Policy precedence (read in this order):** `CLAUDE.md` (standing) → `.claude/rules/general-code-change.md` → `.claude/rules/general-unit-test.md` → `.claude/rules/powershell.md` → `.claude/rules/tonality.md`.

**Sources of truth (this remediation cycle):**

- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/remediation-inputs.2026-04-21T00-00.md` (authoritative remediation input).
- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/code-review.2026-04-21T00-00.md` (severities, locations).
- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/policy-audit.2026-04-21T00-00.md` (policy violations).
- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/feature-audit.2026-04-21T00-00.md` (AC state: 14 PASS / 1 DEFERRED).
- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/spec.md` (original AC list, unchanged by this cycle).
- `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/plan.2026-04-20T09-21.md` (original delivery plan, completed).

**Scope locks (orchestrator-adjudicated):**

- **R1 (Blocker)** — Split `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` so every resulting `*.Tests.ps1` file is `<= 500` lines. Preserve all 17 existing `It` blocks (additions allowed for R2/R3 parameter coverage; deletions are not). Pester pass/fail outcomes must be unchanged for the preserved 17 tests.
- **R2 (High)** — Resolve via **Option B**: add `-OnboardBinaryPath [string]` parameter to `scripts/Invoke-OpenClawAgentOnboarding.ps1` with default `'dist/index.js'`. Document the override in `README.md`, `docs/mailbridge-runbook.md`, and `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md`. Add Pester tests for default and overridden paths.
- **R3 (High)** — Resolve via **Option B**: expose `-DashboardAuthPath [string]` on `scripts/Invoke-OpenClawContainerPathValidation.ps1` (default `'/auth/verify'`). Thread the value through to the existing `Invoke-OpenClawDashboardAuthProbe -AuthPath` call. Add Pester test for an overridden path. Add a code comment at the default-value line in `OpenClawContainerValidation.psm1:351` citing the followups entry. Document the override in `docs/mailbridge-runbook.md`.
- **Non-remediation items (medium/low/info)** — Out of scope. Append each to `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/followups.md` with severity, location, and one-line rationale.

**Working-tree assumption:** Phase 0 begins at `HEAD = 92d7ee69dcd66f126d7a957e3071f00dd5373ed3` on branch `bug/cannot-access-agent-in-docker-38` with no uncommitted changes.

---

### Phase 0 — Remediation baseline

- [x] [P0-T1] Read `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, and `.claude/rules/tonality.md`. Record the read order to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/phase0-instructions-read.md` with required fields `Timestamp:`, `Policy Order:`, and an explicit list of files read.
- [x] [P0-T2] Verify the working tree is clean: run `git status --short` and `git rev-parse HEAD`. Confirm the HEAD SHA equals `92d7ee69dcd66f126d7a957e3071f00dd5373ed3` and that `git status --short` reports no modified or untracked files. Capture both command outputs to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/phase0-pre-remediation-status.txt`. If HEAD differs or the tree is dirty, halt and escalate to the orchestrator.
- [x] [P0-T3] Record remediation baseline metadata: branch name (`git rev-parse --abbrev-ref HEAD`), HEAD SHA, ISO-8601 timestamp, original-baseline reference path (`artifacts/evidence/2026-04-20T09-21-issue-38/baseline/`). Write to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/baseline-metadata.txt` with required fields `Timestamp:`, `Branch:`, `HEAD:`, `OriginalBaselineRoot:`.
- [x] [P0-T4] Capture remediation-baseline PowerShell format state by running `mcp__drmCopilotExtension__run_poshqc_format`. Store the output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/2026-04-21T00-00-poshqc-format.md` with required fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Expected `EXIT_CODE: 0` with no file rewrites (matches the post-original-implementation clean state).
- [x] [P0-T5] Capture remediation-baseline PSScriptAnalyzer state by running `mcp__drmCopilotExtension__run_poshqc_analyze`. Store the output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/2026-04-21T00-00-poshqc-analyze.md` with the four required fields. Expected `EXIT_CODE: 0` and zero diagnostics.
- [x] [P0-T6] Capture remediation-baseline Pester test + coverage state by running `mcp__drmCopilotExtension__run_poshqc_test` in coverage mode. Store the output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/2026-04-21T00-00-poshqc-test.md` with the four required fields. `Output Summary:` must record numeric values for repository-wide coverage and per-file coverage for `scripts/Invoke-OpenClawAgentOnboarding.ps1`, `scripts/Invoke-OpenClawContainerPathValidation.ps1`, and `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`. Expected baseline values (carried from original final): repo-wide `86.97%`, onboarding `98.55%`, validation `90.28%`, module `94.63%`, Pester `97/97 PASS`.
- [x] [P0-T7] Record the line count of the file targeted by R1: `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1).Count`. Write the result to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/2026-04-21T00-00-r1-pre-split-line-count.md` with the four required fields. Expected value `742`.

### Phase 1 — R1 test-file split

- [x] [P1-T1] Inventory the 17 `It` blocks in `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` and assign each to one of five resulting test files plus one shared fixture module. Write the partition plan to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-partition-plan.md`. Required `It` → file mapping:
  - `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` (top-level orchestration; remains the entry test file): `'returns expected when all container endpoints match their validation contracts'`, `'returns unexpected when readiness diagnostics report a degraded dependency'`, `'identifies the container aspect when an expected container is missing'`, `'captures request failures as endpoint diagnostics without throwing'`, `'emits JSON when requested'`.
  - `tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1`: `'AgentReadyz probe reports IsExpected when /readyz returns HTTP 200'`, `'AgentReadyz probe reports Unexpected when /readyz returns HTTP 503'`, `'AgentReadyz probe reports Unexpected when /readyz is unreachable'`.
  - `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1`: `'HostAdapterInContainer probe reports IsExpected when docker exec returns 200'`, `'HostAdapterInContainer probe reports Unexpected when docker exec returns non-200'`, `'HostAdapterInContainer probe reports Unexpected when docker exec exits non-zero'`.
  - `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1`: `'GatewayTokenPresence probe reports IsExpected when .env has OPENCLAW_GATEWAY_TOKEN with non-empty value'`, `'GatewayTokenPresence probe reports Unexpected when .env omits OPENCLAW_GATEWAY_TOKEN'` (the Phase 2 expect-fail test).
  - `tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1`: `'DashboardAuth probe reports Unexpected when OPENCLAW_GATEWAY_TOKEN is empty'` (Phase 2 expect-fail), `'DashboardAuth probe reports IsExpected when POST with stored token returns 200'`, `'DashboardAuth probe reports Unexpected when POST returns 401'`, `'DashboardAuth probe reports Unexpected when response body is non-JSON / malformed'`.
  Acceptance criterion: every named `It` block is assigned to exactly one resulting file; the sum across files equals 17. Record the partition plan with required fields `Timestamp:`, `SourceFile:`, `SourceLineCount:`, `Partition:` (table of file → list of `It` titles).
- [x] [P1-T2] Create `tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1` containing the shared `BeforeAll` module-import logic, the `BeforeEach` setup of `$script:RequestedUris` / `$script:DockerRequests` / `$script:ScriptPath` and the `Invoke-FakeDocker` global function shim, plus the `AfterEach` teardown. Export the helper functions/setup actions so each split test file can dot-source or import the fixture. The fixture module file must be `<= 500` lines. Verify by `(Get-Content tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1).Count -le 500`. Record the line count to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-fixtures-line-count.md` with the four required fields.
- [x] [P1-T3] Create `tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1` containing the three `AgentReadyz` `It` blocks listed in the P1-T1 partition. Use the shared fixture module from P1-T2 for `BeforeAll`/`BeforeEach`/`AfterEach`. Preserve every assertion exactly as written in the source file (no behavioral edits). Verify by `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1).Count -le 500`. Record the line count to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-readyz-line-count.md` with the four required fields.
- [x] [P1-T4] Create `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1` containing the three `HostAdapterInContainer` `It` blocks listed in the P1-T1 partition. Use the shared fixture module. Preserve assertions exactly. Verify by `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1).Count -le 500`. Record the line count to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-hostadapter-line-count.md` with the four required fields.
- [x] [P1-T5] Create `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1` containing the two `GatewayTokenPresence` `It` blocks listed in the P1-T1 partition (one positive, one Phase 2 expect-fail). Use the shared fixture module. Preserve assertions exactly. Verify by `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1).Count -le 500`. Record the line count to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-tokenpresence-line-count.md` with the four required fields.
- [x] [P1-T6] Create `tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1` containing the four `DashboardAuth` `It` blocks listed in the P1-T1 partition. Use the shared fixture module. Preserve assertions exactly. Verify by `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1).Count -le 500`. Record the line count to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-dashboardauth-line-count.md` with the four required fields.
- [x] [P1-T7] Edit `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` in place: remove every `It` block now owned by P1-T3 through P1-T6 (twelve total: three `AgentReadyz`, three `HostAdapterInContainer`, two `GatewayTokenPresence`, four `DashboardAuth`). Retain only the five orchestration `It` blocks named in P1-T1. Switch the `BeforeAll`/`BeforeEach`/`AfterEach` setup to use the shared fixture module from P1-T2. Verify the resulting file is `<= 500` lines and contains exactly five `It` blocks: `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1).Count -le 500` and `(Select-String -Path tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1 -Pattern '^\s*It ' | Measure-Object).Count -eq 5`. Record both verifications to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-orchestration-line-count.md` with the four required fields.
- [x] [P1-T8] Run `mcp__drmCopilotExtension__run_poshqc_format`. If any file is rewritten, restart the loop from this task. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-poshqc-format.md` with the four required fields. Expected `EXIT_CODE: 0` with no rewrites.
- [x] [P1-T9] Run `mcp__drmCopilotExtension__run_poshqc_analyze`. If diagnostics are reported, fix them and restart from P1-T8. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-poshqc-analyze.md` with the four required fields. Expected `EXIT_CODE: 0` and zero diagnostics.
- [x] [P1-T10] Run `mcp__drmCopilotExtension__run_poshqc_test` in coverage mode. All tests in the split files plus all preserved tests in the orchestration file plus all `Invoke-OpenClawAgentOnboarding.Tests.ps1` tests must pass. The test count owned by `Invoke-OpenClawContainerPathValidation*.Tests.ps1` (orchestration + four split files) must equal 17 with no deletions. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/2026-04-21T00-00-r1-poshqc-test.md` with the four required fields. `Output Summary:` must include the per-file test counts across the five split files and confirm `97/97 PASS` overall.

### Phase 2 — R2 parameterize onboarding binary path

- [x] [P2-T1] Edit `scripts/Invoke-OpenClawAgentOnboarding.ps1` to add a new `-OnboardBinaryPath [string]` parameter to the script-level `param()` block at line 42-57. The parameter must declare `[Parameter(Mandatory = $false)]` and default value `'dist/index.js'`. Update the `.PARAMETER` help block in the script header to document the new parameter (purpose: allow operators to override the upstream binary path per upstream release). Verify by `Select-String -Path scripts/Invoke-OpenClawAgentOnboarding.ps1 -Pattern '\[string\]\$OnboardBinaryPath = ''dist/index.js'''` returning exactly one match.
- [x] [P2-T2] Edit `scripts/Invoke-OpenClawAgentOnboarding.ps1` `Invoke-OpenClawOnboardCommand` function (line 113-152) to accept a new `[Parameter(Mandatory = $true)][string]$OnboardBinaryPath` parameter and substitute the hard-coded `'dist/index.js'` literal at line 131 with `$OnboardBinaryPath`. Update the call site in the `try` block at line 214 (`Invoke-OpenClawOnboardCommand -DockerPath $DockerPath -ComposeFiles $ComposeFiles -AnthropicApiKeyPlaintext $apiKeyPlain`) to pass `-OnboardBinaryPath $OnboardBinaryPath`. Verify by `Select-String -Path scripts/Invoke-OpenClawAgentOnboarding.ps1 -Pattern 'dist/index\.js'` returning exactly one match (the script-level default value), not the inline literal in the helper function.
- [x] [P2-T3] Verify the file size cap on `scripts/Invoke-OpenClawAgentOnboarding.ps1`: `(Get-Content scripts/Invoke-OpenClawAgentOnboarding.ps1).Count -le 500`. Record to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r2/2026-04-21T00-00-r2-onboarding-line-count.md` with the four required fields. Expected count `<= 230` (current `221` plus parameter and help additions).
- [x] [P2-T4] Add a new Pester `It` block to `tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1` titled `Onboarding script invokes docker with the default OnboardBinaryPath when -OnboardBinaryPath is not supplied`. Arrange: mock the docker shim with the existing `Invoke-FakeDocker` pattern, supply a SecureString API key, and capture the docker argument list. Act: invoke the script with no `-OnboardBinaryPath`. Assert: the captured argument list contains `'dist/index.js'` immediately following `'openclaw-agent'` and immediately before `'onboard'`. Use `Should -Contain` or explicit index assertion. The test must not introduce temporary files, mutable globals beyond the existing `$Global:__OpenClawOnboardingTestWrittenContent`, or external dependencies.
- [x] [P2-T5] Add a second new Pester `It` block to `tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1` titled `Onboarding script substitutes -OnboardBinaryPath into docker arguments when explicitly supplied`. Arrange: same docker shim. Act: invoke the script with `-OnboardBinaryPath 'openclaw.mjs'`. Assert: the captured argument list contains `'openclaw.mjs'` and does not contain `'dist/index.js'`. The test must not introduce temporary files, mutable globals beyond the existing pattern, or external dependencies.
- [x] [P2-T6] Verify the file size cap on `tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1` after additions: `(Get-Content tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1).Count -le 500`. Record to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r2/2026-04-21T00-00-r2-onboarding-tests-line-count.md` with the four required fields. Expected count `<= 280` (current `207` plus two new tests).
- [x] [P2-T7] Edit `README.md` Manage-the-OpenClaw-Agent section (currently around line 275-280) to document the new `-OnboardBinaryPath` parameter. Add a one-paragraph note immediately after the existing onboarding-script invocation example explaining when an operator should override the default (upstream release that renames the binary path). Verify by `Select-String -Path README.md -Pattern '-OnboardBinaryPath'` returning at least one match.
- [x] [P2-T8] Edit `docs/mailbridge-runbook.md` to document the new `-OnboardBinaryPath` parameter alongside the onboarding script reference. Add a one-paragraph note (or table row) describing the override and the default value. Verify by `Select-String -Path docs/mailbridge-runbook.md -Pattern '-OnboardBinaryPath'` returning at least one match.
- [x] [P2-T9] Edit `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md` Operator-upgrade-steps section to mention the optional `-OnboardBinaryPath` override and reference the runbook for the override rationale. Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern '-OnboardBinaryPath'` returning at least one match.

### Phase 3 — R3 parameterize dashboard-auth path

- [x] [P3-T1] Edit `scripts/Invoke-OpenClawContainerPathValidation.ps1` to add a new `-DashboardAuthPath [string]` parameter to the script-level `param()` block at line 18-30. The parameter must declare default value `'/auth/verify'` and follow the existing parameter pattern. Update the script-level synopsis or comment header to document the new parameter. Verify by `Select-String -Path scripts/Invoke-OpenClawContainerPathValidation.ps1 -Pattern '\[string\]\$DashboardAuthPath = ''/auth/verify'''` returning exactly one match.
- [x] [P3-T2] Edit `scripts/Invoke-OpenClawContainerPathValidation.ps1` line 219 (the `Invoke-OpenClawDashboardAuthProbe` call) to pass `-AuthPath $DashboardAuthPath` so the script's parameter value is threaded through to the existing module probe. Verify by `Select-String -Path scripts/Invoke-OpenClawContainerPathValidation.ps1 -Pattern '-AuthPath \$DashboardAuthPath'` returning exactly one match.
- [x] [P3-T3] Edit `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1` line 351 to add an inline code comment immediately above the `[string]$AuthPath = '/auth/verify'` parameter line. The comment must read (single-line acceptable): `# Default '/auth/verify' is unverified against upstream config; tracked as a manual pre-release verification gate in docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/followups.md`. Verify by `Select-String -Path scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1 -Pattern 'tracked as a manual pre-release verification gate'` returning exactly one match.
- [x] [P3-T4] Verify the file size cap on `scripts/Invoke-OpenClawContainerPathValidation.ps1`: `(Get-Content scripts/Invoke-OpenClawContainerPathValidation.ps1).Count -le 500`. Record to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r3/2026-04-21T00-00-r3-validation-line-count.md` with the four required fields. Expected count `<= 270` (current `264` plus parameter declaration and call-site arg).
- [x] [P3-T5] Verify the file size cap on `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`: `(Get-Content scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1).Count -le 500`. Record to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r3/2026-04-21T00-00-r3-module-line-count.md` with the four required fields. Expected count `<= 415` (current `410` plus one comment line).
- [x] [P3-T6] Add a new Pester `It` block to `tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1` (created in P1-T6) titled `DashboardAuth probe POSTs to overridden -DashboardAuthPath when supplied to the script`. Arrange: mock `Invoke-WebRequest` (module-scoped) and capture the requested `Uri`; mock `.env` to contain a non-empty token; supply the docker shim used by other DashboardAuth tests. Act: invoke `scripts/Invoke-OpenClawContainerPathValidation.ps1 -DashboardAuthPath '/api/auth/verify' -PassThru`. Assert: the captured URI list contains `'http://127.0.0.1:18789/api/auth/verify'` and does not contain `'http://127.0.0.1:18789/auth/verify'`. The test must not introduce temporary files or external dependencies.
- [x] [P3-T7] Verify the file size cap on `tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1` after the P3-T6 addition: `(Get-Content tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1).Count -le 500`. Record to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r3/2026-04-21T00-00-r3-dashboardauth-tests-line-count.md` with the four required fields.
- [x] [P3-T8] Edit `docs/mailbridge-runbook.md` to document the new `-DashboardAuthPath` parameter alongside the validation-script reference. Add a one-paragraph note (or table row) describing the override scenario (operator's upstream gateway exposes a non-default auth path) and the default value `/auth/verify`. Verify by `Select-String -Path docs/mailbridge-runbook.md -Pattern '-DashboardAuthPath'` returning at least one match.

### Phase 4 — Followups append

- [x] [P4-T1] Append to `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/followups.md` a new top-level section `## 4. Non-remediation findings from 2026-04-21 review` containing one bullet per item from `remediation-inputs.2026-04-21T00-00.md` §Non-Remediation Items. Each bullet must include severity (carried from the code review), file path + line range (carried from the code review), and a one-line rationale describing why the item is deferred. The required nine bullets correspond to:
  - Medium — `scripts/Invoke-OpenClawAgentOnboarding.ps1:113-152, 212-221` — Anthropic API key plaintext window on docker CLI argv (upstream constraint; not introduced by this branch).
  - Medium — `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1:61-72, 95-138` — `Get-OpenClawContentPreview` whitespace-only body conflated with absent body.
  - Medium — `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1:211-230, 309-336` — `Test-OpenClawGatewayTokenPresence` does not strip surrounding quotes from `.env` values.
  - Medium — `tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1:1-10` — script-scope `SuppressMessageAttribute` should be narrowed to the specific `It`/`BeforeEach` blocks.
  - Medium — `scripts/Invoke-OpenClawAgentOnboarding.ps1:41, 169, 199-221` — `SupportsShouldProcess` advertised at script level but not honored for the `docker compose run` side effect.
  - Low — `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1:268-302` — `Invoke-OpenClawHostAdapterInContainerProbe` `curl -w "%{http_code}"` format-string brittleness; add malformed-output regression test.
  - Low — `scripts/Invoke-OpenClawContainerPathValidation.ps1:33-39` — stale-module-version warning path when `OpenClawContainerValidation` is already loaded.
  - Info — `.env.example` (token block) — add a `# Do not surround the value in quotes; docker compose expands the raw value.` comment.
  - Info — `scripts/Invoke-OpenClawAgentOnboarding.ps1:160` — token-extraction regex does not strip outer quotes from quoted upstream output.

  Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/followups.md -Pattern '## 4\. Non-remediation findings from 2026-04-21 review'` returning exactly one match and `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/followups.md -Pattern '^- '` returning at least nine matches inside the new section.

### Phase 5 — Final QA gate

- [x] [P5-T1] Run `mcp__drmCopilotExtension__run_poshqc_format`. If any file is rewritten, restart the loop from this task. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-poshqc-format.md` with the four required fields. Expected `EXIT_CODE: 0` with no rewrites.
- [x] [P5-T2] Run `mcp__drmCopilotExtension__run_poshqc_analyze`. If diagnostics are reported, fix them and restart from P5-T1. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-poshqc-analyze.md` with the four required fields. Expected `EXIT_CODE: 0` and zero diagnostics.
- [x] [P5-T3] Run `mcp__drmCopilotExtension__run_poshqc_test` in coverage mode. All tests must pass. Store output to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-poshqc-test.md` with the four required fields. `Output Summary:` must include numeric values for repository-wide coverage and per-file coverage for `scripts/Invoke-OpenClawContainerPathValidation.ps1`, `scripts/Invoke-OpenClawAgentOnboarding.ps1`, and `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`.
- [x] [P5-T4] Verify the Pester test count: total tests must equal `97 + N` where `N = 3` (two additive tests from P2-T4/P2-T5 plus one from P3-T6). Expected total `100/100 PASS`. The 17 tests originally in `Invoke-OpenClawContainerPathValidation.Tests.ps1` must be preserved across the five split test files (one orchestration + four probe-group files) plus one additive DashboardAuth test from P3-T6 = 18 tests in that group. The 7 tests in `Invoke-OpenClawAgentOnboarding.Tests.ps1` must be preserved plus two additive tests from P2-T4/P2-T5 = 9 tests. Combined feature tests: 27 (was 24) plus 73 unchanged tests across the rest of the repo = 100. Record verification to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-test-count-verification.md` with the four required fields plus `OriginalCount:`, `AdditiveCount:`, `FinalCount:`, `Verdict:`.
- [x] [P5-T5] Compare Phase 0 remediation baseline coverage (`artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/2026-04-21T00-00-poshqc-test.md`) and original Phase 0 baseline coverage (`artifacts/evidence/2026-04-20T09-21-issue-38/baseline/2026-04-20T09-21-poshqc-test.md`) against Phase 5 final coverage (`artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-poshqc-test.md`). Write the comparison to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-coverage-comparison.md` with required fields: `RemediationBaselineRepoCoverage:`, `OriginalBaselineRepoCoverage:`, `FinalRepoCoverage:`, `PerFileCoverage:` (table for `Invoke-OpenClawAgentOnboarding.ps1`, `Invoke-OpenClawContainerPathValidation.ps1`, `OpenClawContainerValidation.psm1`), `ThresholdRepo: >= 80%`, `ThresholdNewCode: >= 90%`, `Verdict:`. Fail the task if repo-wide coverage drops below `80%` or any of the three target files drops below `90%`.
- [x] [P5-T6] Verify the R1 split outcome: for each of the five test files in the `Invoke-OpenClawContainerPathValidation*.Tests.ps1` family, run `(Get-Content <file>).Count -le 500`. The five files are: `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1`, `tests/scripts/Invoke-OpenClawContainerPathValidation.Readyz.Tests.ps1`, `tests/scripts/Invoke-OpenClawContainerPathValidation.HostAdapter.Tests.ps1`, `tests/scripts/Invoke-OpenClawContainerPathValidation.TokenPresence.Tests.ps1`, `tests/scripts/Invoke-OpenClawContainerPathValidation.DashboardAuth.Tests.ps1`. Also verify the fixture module: `(Get-Content tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1).Count -le 500`. Record all six counts to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-r1-final-split-line-counts.md` with the four required fields plus a per-file count table and `Verdict:` (PASS/FAIL based on whether every count is `<= 500`).
- [x] [P5-T7] Verify the file-size cap on the modified production files: `(Get-Content scripts/Invoke-OpenClawAgentOnboarding.ps1).Count -le 500`, `(Get-Content scripts/Invoke-OpenClawContainerPathValidation.ps1).Count -le 500`, `(Get-Content scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1).Count -le 500`, and `(Get-Content tests/scripts/Invoke-OpenClawAgentOnboarding.Tests.ps1).Count -le 500`. Record all four counts to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-final-production-line-counts.md` with the four required fields plus a per-file count table and `Verdict:`.
- [x] [P5-T8] Verify the spec acceptance-criteria checklist is unchanged: `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/spec.md -Pattern '^\s*-\s*\[x\]'` count must equal 14 and `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/spec.md -Pattern '^\s*-\s*\[ \]'` count inside the §Acceptance Criteria section must equal 1 (the deferred AC-15). Record both counts to `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-ac-checklist-no-regression.md` with the four required fields plus `CheckedAcCount:`, `DeferredAcCount:`, and `Verdict:` (PASS only when checked equals 14 and deferred equals 1).
- [x] [P5-T9] Store an end-state remediation summary at `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/2026-04-21T00-00-remediation-summary.md` that links every Phase 5 artifact, lists `EXIT_CODE: 0` for each toolchain step, lists each R-item (R1, R2, R3) with its delivering tasks and final verdict (`PASS` if all evidence artifacts exist with passing fields), and confirms the AC checklist regression status from P5-T8. Required fields: `Timestamp:`, `R1Verdict:`, `R2Verdict:`, `R3Verdict:`, `FollowupsAppendVerdict:`, `AcRegressionVerdict:`, `OverallVerdict:`.

### Phase 6 — PR-notes refresh

- [x] [P6-T1] Edit `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md` Summary section to add one sentence describing the R1 test-file split (validation test file split into five `*.Tests.ps1` files plus one shared fixture module under `tests/scripts/fixtures/`). Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern 'tests/scripts/fixtures/OpenClawContainerValidation.Fixtures.psm1'` returning at least one match.
- [x] [P6-T2] Edit the same `pr-notes.md` Summary section to add one sentence describing the R2 (`-OnboardBinaryPath`) and R3 (`-DashboardAuthPath`) parameterizations as additive, non-breaking parameters with documented defaults. Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern '-OnboardBinaryPath' | Measure-Object` returning at least one match (covered already by P2-T9; this task ensures both parameter names appear in the Summary section, not only the upgrade-steps section) and `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern '-DashboardAuthPath'` returning at least one match.
- [x] [P6-T3] Edit the `pr-notes.md` Breaking-changes section to explicitly state that the breaking-change scope is unchanged from the original PR: only the removal of the hard-coded `OPENCLAW_GATEWAY_TOKEN` default in `docker-compose.yml` and the removal of `OPENCLAW_AGENT_WORKSPACE` from `.env.example` are operator-visible breaking changes. The new `-OnboardBinaryPath` and `-DashboardAuthPath` parameters are additive (defaults preserved). Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern 'breaking-change scope is unchanged'` returning at least one match.
- [x] [P6-T4] Edit the `pr-notes.md` Validation-evidence-pointers section to append the three remediation-evidence root paths: `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/`, `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r1/`, `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r2/`, `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/r3/`, and `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/final/`. Also add the path of this remediation plan: `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/plan-remediation.2026-04-21T00-00.md`. Verify by `Select-String -Path docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/pr-notes.md -Pattern '2026-04-21T00-00-issue-38-remediation/final/'` returning at least one match.

---

## Remediation Coverage Matrix

Mapping the three explicit remediation items and the followups append to plan tasks:

| Item | Description | Delivering tasks | Verifying tasks |
|---|---|---|---|
| R1 | Split `tests/scripts/Invoke-OpenClawContainerPathValidation.Tests.ps1` so every resulting `*.Tests.ps1` file is `<= 500` lines; preserve 17 tests | P1-T1, P1-T2, P1-T3, P1-T4, P1-T5, P1-T6, P1-T7 | P1-T8, P1-T9, P1-T10, P5-T6 |
| R2 | Parameterize onboarding binary path with `-OnboardBinaryPath` (default `dist/index.js`); document and test default + override | P2-T1, P2-T2, P2-T4, P2-T5, P2-T7, P2-T8, P2-T9 | P2-T3, P2-T6, P5-T3, P5-T4 |
| R3 | Parameterize dashboard-auth path with `-DashboardAuthPath` (default `/auth/verify`); thread through to module probe; add code comment citing followups; test override | P3-T1, P3-T2, P3-T3, P3-T6, P3-T8 | P3-T4, P3-T5, P3-T7, P5-T3, P5-T4 |
| Followups | Append nine non-remediation items to `followups.md` | P4-T1 | P4-T1 (self-verifying via grep) |
| AC no-regression | Confirm spec checklist remains 14 PASS / 1 DEFERRED | (no implementation tasks; reviewer-only) | P5-T8 |

## Acceptance-Criteria Impact

This remediation cycle changes no AC outcomes. The `spec.md §Acceptance Criteria` checklist must remain 14 `[x]` and 1 `[ ]` (AC-15 deferred). P5-T8 verifies this invariant. R2 and R3 are additive parameter introductions with default values matching the prior hard-coded values, so AC-1 (onboarding behavior) and AC-6 (validation script behavior) continue to hold under default invocation.

## Out-of-Scope (do not do in this cycle)

- No real-docker smoke run for R2 (orchestrator rejected Option A; the executor environment does not guarantee Docker Desktop with `host.docker.internal` networking).
- No upstream-path verification call for R3 (orchestrator rejected Option A; verification is tracked as a manual pre-release gate via the followups citation in the code comment).
- No remediation of medium/low/info findings beyond their bullet append to `followups.md`.
- No edits to `.claude/rules/` or `.github/instructions/` policy files.
- No edits to `spec.md` or `issue.md` (AC list is frozen for this cycle).

---

## Preflight

- **Spec coverage:** Remediation items R1, R2, R3 each map to delivering and verifying tasks per the matrix above. AC list unchanged; P5-T8 verifies no regression. Followups append covered by P4-T1.
- **Toolchain (PowerShell):** Remediation baseline capture at P0-T4 (format), P0-T5 (analyze), P0-T6 (test+coverage). Per-phase toolchain pass-through during R1 at P1-T8/P1-T9/P1-T10. Final QA loop at P5-T1 (format), P5-T2 (analyze), P5-T3 (test+coverage). Coverage comparison at P5-T5. Type-check step intentionally omitted per `.claude/rules/powershell.md`.
- **Evidence:** Baseline artifacts declared under `artifacts/evidence/2026-04-21T00-00-issue-38-remediation/baseline/`. Per-R artifacts under `.../r1/`, `.../r2/`, `.../r3/`. Final artifacts under `.../final/`. Every evidence-producing task names its artifact path and the four required fields (`Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`) are required on every command-step artifact. Coverage artifacts (P0-T6, P5-T3, P5-T5) record numeric repo-wide and per-file coverage values.
- **Budget:** `large` path selected. Production-file count = 3 (`scripts/Invoke-OpenClawAgentOnboarding.ps1`, `scripts/Invoke-OpenClawContainerPathValidation.ps1`, `scripts/powershell/modules/OpenClawContainerValidation/OpenClawContainerValidation.psm1`). Routed per `powershell-change-budget-router` contract. Note: the orchestrator brief estimated `small` likely; the planner adjudicates `large` because R3 requires editing both the script and the module to add and thread the `-DashboardAuthPath` parameter and to add the followups comment.
- **File-size cap (500 lines):** R1 split verifications at P1-T2 through P1-T7 and the final aggregate verification at P5-T6. Production-file caps verified at P2-T3 (onboarding script), P3-T4 (validation script), P3-T5 (module), and aggregated at P5-T7.
- **Test additions:** Two additive `It` blocks for R2 (P2-T4, P2-T5) and one additive `It` block for R3 (P3-T6). No deletions. Original 17 + 7 = 24 feature tests preserved; final feature test count is 27. Repository-wide test count moves from 97 to 100 (P5-T4 verifies).
- **Policy compliance:**
  - Tonality — plan prose is factual and measured; no humor, hyperbole, or decorative metaphor.
  - Unit-test policy — additive tests use Pester mocks for docker and `Invoke-WebRequest`; no temporary files; no external services; deterministic inputs; Arrange-Act-Assert structure preserved.
  - General-code-change — 500-line cap enforced on every resulting file (production and test); no silent catches added; new parameters use keyword-style with explicit defaults; no breaking changes to existing parameter contracts.
  - PowerShell — `[Parameter(Mandatory = $false)]` on both new parameters; defaults preserved at original hard-coded values; advanced-function pattern unchanged.
  - Evidence-and-timestamp — ISO-8601 `2026-04-21T00-00` applied throughout; canonical `artifacts/evidence/<timestamp>-issue-38-remediation/` rooting with `baseline/`, `r1/`, `r2/`, `r3/`, and `final/` subpaths.
- **Plan-path continuity:** This plan is written to the exact path `docs/features/active/2026-04-20-cannot-access-agent-in-docker-38/plan-remediation.2026-04-21T00-00.md` and will be updated in place across any preflight revision cycles. The original delivery plan at `plan.2026-04-20T09-21.md` remains untouched.
- **Open questions:**
  - None blocking. The orchestrator scope locks adjudicate Option B for R2 and R3 and resolve all option-selection ambiguity from `remediation-inputs.2026-04-21T00-00.md`.

PREFLIGHT: ALL CLEAR
