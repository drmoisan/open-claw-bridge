# Remediation Plan — Issue #73 — Cycle 1

- **Canonical issue:** #73
- **Feature folder:** docs/features/active/mailbridge-messagedto-resolved-fields-73
- **Work Mode:** full-bug (spec.md present; user-story.md not required)
- **Plan timestamp:** 2026-06-14T09-00
- **Authoritative input:** docs/features/active/mailbridge-messagedto-resolved-fields-73/remediation-inputs.2026-06-14T09-00.md
- **Base / merge-base:** main / be2ddbf6559febc4ddfcf14a098025d96647f772
- **Head at cycle entry:** 9658ee76b2a997287aa36a6d22f4a353f8bb9890

## Scope

Two operator-approved blocking findings. The approach is fixed; do not re-litigate.

- **RF-1** — `src/OpenClaw.MailBridge/ComMessageSource.cs` new-file coverage 80.1% line / 60.9% branch is below the uniform thresholds (line >= 85%, branch >= 75%). Add MSTest unit tests for every reachable fallback/branch via the existing reflection-based fakes; isolate the irreducible live-COM-only members and apply a narrow, justified `[ExcludeFromCodeCoverage]` to only those members. Do not lower any threshold.
- **RF-2** — `src/OpenClaw.Core/CoreCacheRepository.cs` is 699 lines, exceeding the 500-line cap. Moving only the message members (~187 lines) leaves ~512 lines, still over the cap, so the message extraction alone cannot meet the RF-2 exit condition. Extract the message read/write/upsert/parameter-binding members into a new behavior-preserving partial `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`, AND extract the event CRUD members (the event upsert/list/get/parameter-binding/reader analogues of the message members) into a second new partial `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. Same partial class, same members, same signatures, no logic change. Leave the shared readers (`ReadString`/`ReadDateTimeOffset`/`ReadNullableInt`/`ReadBoolean`/`ToDbValue`/`CountAsync`), `GetCountsAsync` (queries both tables), the constructor/lifecycle, and schema/migration coordination in the base file. The RF-2 exit condition requires ALL THREE: `CoreCacheRepository.cs <= 500`, `CoreCacheRepository.Messages.cs <= 500`, `CoreCacheRepository.Events.cs <= 500`; prove zero regression by running the full test suite after the moves. Line-count arithmetic: 699 − ~187 (messages) − ~241 (events) ≈ 271 lines in the base file; each new partial (~197 and ~251 with header/usings) is well under 500.

Phase order ends each phase green: RF-2 (behavior-preserving extraction, proven by full test run) precedes RF-1 (added tests + narrow exclusions). The final phase runs the full seven-stage toolchain and proves all numeric exit conditions and AC-01..AC-11.

## Evidence Locations (canonical, non-overridable)

All evidence artifacts MUST be written under `docs/features/active/mailbridge-messagedto-resolved-fields-73/evidence/<kind>/`:

- Baseline: `evidence/remediation-baseline/`
- Regression / per-phase test runs: `evidence/regression-testing/`
- Final QA gates and coverage: `evidence/qa-gates/`
- Other (file-size, suppression scan, AC matrix): `evidence/other/`

Any non-canonical path supplied by a caller is rejected and replaced with the canonical path; the override is recorded as `EVIDENCE_LOCATION_OVERRIDE_REJECTED: <supplied> replaced with <canonical>`.

## Per-task toolchain gate (applies to every implementation task)

Each implementation task must end with this gate, in this exact order, and the task is complete only when every stage passes in a single pass with no file changes:

1. Format: `csharpier format .`
2. Build / lint: `dotnet build OpenClaw.MailBridge.sln -c Debug -p:EnableNETAnalyzers=true -p:EnforceCodeStyleInBuild=true`
3. Nullable / type-check: `dotnet build OpenClaw.MailBridge.sln -c Debug -p:TreatWarningsAsErrors=true`
4. Architecture check: verify project-reference and COM-confinement boundaries per `.claude/rules/architecture-boundaries.md` (COM remains confined to `OpenClaw.MailBridge`).
5. Test + coverage: `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`

Use the global CSharpier subcommand form only (`csharpier format .` / `csharpier check .`). Never use `dotnet csharpier`. If any stage fails or changes files, restart from stage 1.

---

### Phase 0 — Policy reading and baseline capture

- [x] [P0-T1] Read the policy files in order and record an evidence artifact at `docs/features/active/mailbridge-messagedto-resolved-fields-73/evidence/other/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read: `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md`, `.claude/rules/architecture-boundaries.md`, `.claude/rules/quality-tiers.md`. The artifact MUST note that no root `CLAUDE.md` exists in this repository; standing instructions are delivered via `.claude/rules/`. Acceptance: artifact exists with the five rule files listed.
- [x] [P0-T2] Capture the baseline format state. Run `csharpier check .` and write `evidence/remediation-baseline/baseline-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; summary states pass/fail and count of files needing formatting.
- [x] [P0-T3] Capture the baseline build/lint state. Run `dotnet build OpenClaw.MailBridge.sln -c Debug -p:EnableNETAnalyzers=true -p:EnforceCodeStyleInBuild=true` and write `evidence/remediation-baseline/baseline-build-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (warning count, error count). Acceptance: artifact exists with all four fields.
- [x] [P0-T4] Capture the baseline nullable/type-check state. Run `dotnet build OpenClaw.MailBridge.sln -c Debug -p:TreatWarningsAsErrors=true` and write `evidence/remediation-baseline/baseline-nullable.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields.
- [x] [P0-T5] Capture the baseline test + coverage state. Run `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` and write `evidence/remediation-baseline/baseline-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. The summary MUST record numeric values: total passed/failed/skipped, per-project aggregate line/branch coverage, and the parsed `ComMessageSource.cs` new-file line/branch coverage (expected near 80.1% / 60.9%). Acceptance: artifact exists with all four fields and the named numeric coverage headlines (no placeholders).
- [x] [P0-T6] Capture the baseline file-size state. Record line counts for `src/OpenClaw.Core/CoreCacheRepository.cs` (expected 699), `src/OpenClaw.Core/CoreCacheRepository.Schema.cs`, `src/OpenClaw.MailBridge/ComMessageSource.cs`, and `tests/OpenClaw.MailBridge.Tests/ComMessageSourceTests.cs` to `evidence/remediation-baseline/baseline-file-size.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields and the four numeric line counts; confirms `CoreCacheRepository.cs > 500`.

---

### Phase 1 — RF-2: Behavior-preserving partial extraction (CoreCacheRepository.Messages.cs + CoreCacheRepository.Events.cs)

Two behavior-preserving partial splits are required so that all three files fall under the 500-line cap. The message members and the event CRUD members each move verbatim (not copied) into a dedicated partial, leaving the shared readers (`ReadString`/`ReadDateTimeOffset`/`ReadNullableInt`/`ReadBoolean`/`ToDbValue`/`CountAsync`), `GetCountsAsync` (queries both tables), the bridge-status/cursor/ingest-run members, and the constructor/lifecycle/schema-migration coordination in `CoreCacheRepository.cs`. No signature, accessibility, or body change.

Line-count arithmetic (verified against the current 699-line file): message members total ~187 lines (`UpsertMessagesAsync` 84-139, `ListMessagesAsync` 263-289, `GetMessageAsync` 291-300, `AddMessageParameters` 431-483, `ReadMessagesAsync` 591-601, `ReadMessage` 615-638); event CRUD members total ~241 lines (`UpsertEventsAsync` 141-203, `ListEventsAsync` 302-323, `GetEventAsync` 325-334, `AddEventParameters` 485-554, `CategoriesToDbValue` 556-561, `ReadCategories` 563-583, `ReadEventsAsync` 603-613, `ReadEvent` 640-669). After both moves the base file is 699 − ~187 − ~241 ≈ 271 lines; `CoreCacheRepository.Messages.cs` is ~197 lines (members + header/usings) and `CoreCacheRepository.Events.cs` is ~251 lines (members + header/usings). All three are provably <= 500. The event-only JSON helpers `CategoriesToDbValue` and `ReadCategories` use `System.Text.Json`, so the Events partial carries the `System.Text.Json` using and the base file no longer needs it.

- [x] [P1-T1] Create the new partial file `src/OpenClaw.Core/CoreCacheRepository.Messages.cs` with the same `using` directives required by the moved members (`System.Text.Json` is not needed by the message members; include `Microsoft.Data.Sqlite` and `OpenClaw.MailBridge.Contracts.Models`), file-scoped `namespace OpenClaw.Core;`, an `internal sealed partial class CoreCacheRepository` declaration, and a class-level `<summary>` XML doc describing it as the message persistence partial split from `CoreCacheRepository.cs` to satisfy the 500-line cap (cite issue #73 RF-2), following the convention established by `CoreCacheRepository.Schema.cs`. Acceptance: file compiles as an empty partial body after subsequent moves; partial declaration matches the existing class modifiers exactly. Run the full per-task toolchain gate.
- [x] [P1-T2] Move the message upsert member `UpsertMessagesAsync` (currently `src/OpenClaw.Core/CoreCacheRepository.cs` lines 84-139) verbatim from `CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`. No body or signature change. Acceptance: the member exists only in the new partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T3] Move the message query members `ListMessagesAsync` (lines 263-289) and `GetMessageAsync` (lines 291-300) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`. No body or signature change. Acceptance: both members exist only in the new partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T4] Move the message parameter-binding member `AddMessageParameters` (lines 431-483) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`. No body or signature change. Acceptance: the member exists only in the new partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T5] Move the message read members `ReadMessagesAsync` (lines 591-601) and `ReadMessage` (lines 615-638) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`. Leave the shared reader helpers (`ReadString`, `ReadDateTimeOffset`, `ReadNullableInt`, `ReadBoolean`, `ToDbValue`, `CountAsync`) and `GetCountsAsync` in `CoreCacheRepository.cs` because event and status members also call them; the partial relies on them via the shared partial class. No body or signature change. Acceptance: both members exist only in the new partial; build succeeds with no duplicate-definition or missing-reference errors. Run the full per-task toolchain gate.
- [x] [P1-T6] Create the second new partial file `src/OpenClaw.Core/CoreCacheRepository.Events.cs` with the `using` directives required by the moved event members (`System.Text.Json` IS needed here because `CategoriesToDbValue`/`ReadCategories` use it; include `Microsoft.Data.Sqlite` and `OpenClaw.MailBridge.Contracts.Models`), file-scoped `namespace OpenClaw.Core;`, an `internal sealed partial class CoreCacheRepository` declaration, and a class-level `<summary>` XML doc describing it as the event persistence partial split from `CoreCacheRepository.cs` to satisfy the 500-line cap (cite issue #73 RF-2), following the convention established by `CoreCacheRepository.Schema.cs` and `CoreCacheRepository.Messages.cs`. Acceptance: file compiles as an empty partial body after subsequent moves; partial declaration matches the existing class modifiers exactly. Run the full per-task toolchain gate.
- [x] [P1-T7] Move the event upsert member `UpsertEventsAsync` (lines 141-203) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. No body or signature change. Acceptance: the member exists only in the Events partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T8] Move the event query members `ListEventsAsync` (lines 302-323) and `GetEventAsync` (lines 325-334) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. No body or signature change. Acceptance: both members exist only in the Events partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T9] Move the event parameter-binding member `AddEventParameters` (lines 485-554) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. No body or signature change. Acceptance: the member exists only in the Events partial; build succeeds. Run the full per-task toolchain gate.
- [x] [P1-T10] Move the event JSON helpers `CategoriesToDbValue` (lines 556-561) and `ReadCategories` (lines 563-583) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. These are the only members that require `System.Text.Json`; once moved, remove the now-unused `using System.Text.Json;` from `CoreCacheRepository.cs` if no remaining member uses it. No body or signature change. Acceptance: both members exist only in the Events partial; build succeeds with no unused-using warning in the base file. Run the full per-task toolchain gate.
- [x] [P1-T11] Move the event read members `ReadEventsAsync` (lines 603-613) and `ReadEvent` (lines 640-669) verbatim from `src/OpenClaw.Core/CoreCacheRepository.cs` into `src/OpenClaw.Core/CoreCacheRepository.Events.cs`. Leave the shared reader helpers (`ReadString`, `ReadDateTimeOffset`, `ReadNullableInt`, `ReadBoolean`, `ToDbValue`, `CountAsync`) and `GetCountsAsync` in `CoreCacheRepository.cs`; the Events partial relies on them via the shared partial class. No body or signature change. Acceptance: both members exist only in the Events partial; build succeeds with no duplicate-definition or missing-reference errors. Run the full per-task toolchain gate.
- [x] [P1-T12] Verify file-size exit for RF-2. Record line counts for `src/OpenClaw.Core/CoreCacheRepository.cs`, `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`, and `src/OpenClaw.Core/CoreCacheRepository.Events.cs` to `evidence/regression-testing/rf2-file-size.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact shows ALL THREE of `CoreCacheRepository.cs <= 500`, `CoreCacheRepository.Messages.cs <= 500`, AND `CoreCacheRepository.Events.cs <= 500` (expected base ~271, Messages ~197, Events ~251); confirm no other touched file exceeds 500 lines.
- [x] [P1-T13] Prove no regression from the extraction. Run `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` and write `evidence/regression-testing/rf2-post-extraction-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording numeric passed/failed/skipped and per-project aggregate coverage. Acceptance: 0 failed; passed count >= the Phase 0 baseline passed count; no public API or behavior change; artifact has all four fields with numeric values.

---

### Phase 2 — RF-1: Coverage tests and narrow COM-only exclusions (ComMessageSource.cs)

The SMTP-resolution and recipient members in `src/OpenClaw.MailBridge/ComMessageSource.cs` read COM members through reflection helpers (`OutlookComHelpers.GetOptionalMemberValue`, `GetOptionalString`, `GetOptionalInt`, `InvokeMember`). Reflection-readable fakes (the existing `FakeAdapterMailItem`/`FakeAdapterMeetingItem` pattern with `FakeComActiveObject` in `tests/OpenClaw.MailBridge.Tests/ComMessageSourceTests.cs`) can therefore drive every branch except the two hard live-COM behaviors: the `PropertyAccessor.GetProperty(PR_SMTP_ADDRESS)` MAPI call and `AddressEntry.GetExchangeUser().PrimarySmtpAddress`. Tests are added to `tests/OpenClaw.MailBridge.Tests/ComMessageSourceTests.cs` (Moq + FluentAssertions, deterministic, no temp files, no live COM). If that file would exceed 500 lines, add a sibling `tests/OpenClaw.MailBridge.Tests/ComMessageSourceResolutionTests.cs`; keep every test file <= 500 lines.

- [x] [P2-T1] Add tests for the sender SMTP fallback path `ResolveSenderSmtp` (`ComMessageSource.cs` lines 99-121): cover (a) resolved address returned from the address-entry chain, (b) the catch path falling through to the raw `SenderEmailAddress`, and (c) null/whitespace `SenderEmailAddress` yielding null. Use reflection-readable fakes that expose `Sender`, `AddressEntry`, and `Address`/`SenderEmailAddress`. Acceptance: lines 105-120 including the catch (111-114) and the final return (120) are exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T2] Add tests for the from / on-behalf-of path `ResolveFromSmtp` and `ResolveOnBehalfOfSmtp` (`ComMessageSource.cs` lines 128-177): cover (a) empty `SentOnBehalfOfEmailAddress` delegating to `ResolveSenderSmtp` (line 136), (b) an on-behalf-of value that already looks like SMTP returned directly (lines 170-172), (c) a non-SMTP on-behalf-of value resolved via the address-entry chain (line 175), and (d) the chain returning empty so the raw on-behalf-of value is normalized and returned (line 176). Acceptance: branches at lines 134-136, 170-172, and 175-176 are exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T3] Add tests for the address-entry resolution chain `ResolveAddressEntrySmtp` (`ComMessageSource.cs` lines 185-224): cover (a) null sender returning null (lines 187-190), (b) the PropertyAccessor branch returning a value (lines 197-200) via a fake exposing a reflection-invokable `GetProperty`, (c) the ExchangeUser branch returning a value (lines 203-207) via a fake exposing a reflection-invokable `GetExchangeUser`, (d) the `Sender.Address` SMTP-shape branch (lines 209-213), and (e) the entry-address fallback selection (lines 215-218) for both SMTP-shaped and non-SMTP entry addresses. Acceptance: each branch at lines 198-200, 204-207, 210-213, and 215-218 is exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T4] Add tests for `ResolveViaPropertyAccessor` (`ComMessageSource.cs` lines 230-264) and `ResolveViaExchangeUser` (lines 270-298) reachable surface: cover (a) null `addressEntry` returning null, (b) null `PropertyAccessor`/`exchangeUser` member returning null, and (c) the catch path returning null when the reflection-invoked member throws. Use fakes whose `PropertyAccessor`/`GetExchangeUser` members are present-or-absent and whose invoked methods throw on demand. Acceptance: the null-guard branches (232-235, 244-247, 272-275, 281-284) and catch branches (256-259, 290-293) are exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T5] Add tests for the recipient enumeration paths `EnsureRecipients`, `ToRecipients`, and `CcRecipients` (`ComMessageSource.cs` lines 61-90): cover (a) first access populating both lists, (b) repeat access short-circuiting via the cached non-null lists (line 82-85), and (c) To/Cc projection from a fake recipient collection (To=type 1, Cc=type 2). Acceptance: the cache short-circuit branch (82-85) and both list getters (61-78) are exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T6] Add tests for the pure helpers `LooksLikeSmtp` (`ComMessageSource.cs` lines 304-307) and `NormalizeAddress` (lines 312-313) plus `MeetingMessageType` for an ordinary-mail item (line 57-58, isMeeting=false yields null) and a meeting item. Acceptance: `LooksLikeSmtp` true/false branches (with `@`, with `/o=` prefix, null/whitespace) and `NormalizeAddress` empty-vs-trimmed branches are exercised; tests pass. Run the full per-task toolchain gate.
- [x] [P2-T7] Isolate the irreducible live-COM-only behavior into the smallest possible members and apply a narrow `[ExcludeFromCodeCoverage]` with an in-code justification comment naming the live-COM dependency, to ONLY those members. The candidates are the hard MAPI/Exchange COM calls that cannot execute without live Outlook: the `PropertyAccessor.GetProperty(PR_SMTP_ADDRESS)` invocation in `ResolveViaPropertyAccessor` and the `GetExchangeUser().PrimarySmtpAddress` invocation in `ResolveViaExchangeUser`. If a member is fully reachable via reflection fakes (as P2-T1..P2-T6 establish), it MUST NOT be excluded. Extract only the unavoidable live-COM line(s) into a minimal private member if needed to keep the surrounding null-guard/catch branches measured. Do not lower any coverage threshold. Acceptance: `[ExcludeFromCodeCoverage]` appears only on the COM-interop-only member(s) with an in-code justification comment; no exclusion on any unit-test-reachable member; build clean with no new analyzer/nullable suppressions. Run the full per-task toolchain gate.
- [x] [P2-T8] Verify the RF-1 coverage exit on the testable surface. Run `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`, parse the cobertura report for `ComMessageSource.cs`, and write `evidence/regression-testing/rf1-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording baseline line/branch (80.1% / 60.9%), post-change line/branch, and a statement that the excluded members are limited to the COM-interop-only members from P2-T7. Acceptance: `ComMessageSource.cs` measured line coverage >= 85% AND branch coverage >= 75% on the testable surface; 0 failed tests; artifact has all four fields with numeric values.

---

### Phase 3 — Final QA loop and exit verification

Run the full seven-stage toolchain in order; if any stage fails or changes files, restart from stage 1. Each command-bearing task MUST execute its command and record evidence; `SKIPPED` is not a passing outcome.

- [x] [P3-T1] Formatting. Run `csharpier check .` and write `evidence/qa-gates/final-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0; no files require formatting.
- [x] [P3-T2] Linting / static analysis. Run `dotnet build OpenClaw.MailBridge.sln -c Debug -p:EnableNETAnalyzers=true -p:EnforceCodeStyleInBuild=true` and write `evidence/qa-gates/final-build-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (0 warnings, 0 errors). Acceptance: EXIT_CODE 0; 0 analyzer errors.
- [x] [P3-T3] Type checking (nullable). Run `dotnet build OpenClaw.MailBridge.sln -c Debug -p:TreatWarningsAsErrors=true` and write `evidence/qa-gates/final-nullable.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: EXIT_CODE 0; 0 nullable warnings.
- [x] [P3-T4] Architecture-boundary check. Verify project-reference edges and COM confinement per `.claude/rules/architecture-boundaries.md` (Outlook COM remains only in `OpenClaw.MailBridge`; `OpenClaw.Core` reaches no COM closure; no new cross-project references introduced by the extraction). Write `evidence/qa-gates/final-architecture.md` with `Timestamp:`, `Command:` (the verification performed), `EXIT_CODE:`, `Output Summary:`. Acceptance: 0 boundary violations.
- [x] [P3-T5] Tests + coverage. Run `dotnet test OpenClaw.MailBridge.sln -c Debug --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` and write `evidence/qa-gates/final-test-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` recording numeric passed/failed/skipped, per-project aggregate line/branch coverage, and the parsed `ComMessageSource.cs` new-file line/branch coverage. Acceptance: EXIT_CODE 0; 0 failed; aggregate line >= 85% / branch >= 75%; `ComMessageSource.cs` line >= 85% / branch >= 75%.
- [x] [P3-T6] Coverage delta / threshold verification. Write `evidence/qa-gates/coverage-delta.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` reporting baseline coverage (from P0-T5), post-change coverage (from P3-T5), and new/changed-code coverage for `ComMessageSource.cs`, with an explicit PASS/FAIL against line >= 85% and branch >= 75% and a no-regression statement on changed lines. Acceptance: artifact records all three coverage values numerically and a PASS verdict; if any required value is unavailable the outcome is remediation-required, not PASS.
- [x] [P3-T7] File-size check on all touched files. Record line counts for `src/OpenClaw.Core/CoreCacheRepository.cs`, `src/OpenClaw.Core/CoreCacheRepository.Messages.cs`, `src/OpenClaw.Core/CoreCacheRepository.Events.cs`, `src/OpenClaw.MailBridge/ComMessageSource.cs`, `tests/OpenClaw.MailBridge.Tests/ComMessageSourceTests.cs`, and any new test file created in P2 to `evidence/other/final-file-size.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: every listed file <= 500 lines; `CoreCacheRepository.cs <= 500`, `CoreCacheRepository.Messages.cs <= 500`, and `CoreCacheRepository.Events.cs <= 500` explicitly confirmed.
- [x] [P3-T8] Suppression scan. Confirm no new analyzer/nullable suppressions were introduced and that the only `[ExcludeFromCodeCoverage]` additions are the narrow COM-interop-only members from P2-T7. Write `evidence/other/final-suppression-scan.md` with `Timestamp:`, `Command:` (the diff/grep performed against merge-base be2ddbf), `EXIT_CODE:`, `Output Summary:` listing each `[ExcludeFromCodeCoverage]` attribute and its justification. Acceptance: no `#pragma warning disable`, `SuppressMessage`, or `!`-null-forgiving additions beyond pre-existing; exclusions limited to the approved COM-interop-only members.
- [x] [P3-T9] AC verification matrix. Write `evidence/other/final-ac-matrix.md` with `Timestamp:` and a PASS/FAIL row for each of AC-01..AC-11, each citing the verifying test name or evidence artifact. AC-11 must cite P3-T1..P3-T8 (seven-stage pass, coverage >= 85/75, no changed-line regression, no new suppressions, no file > 500 lines). Acceptance: AC-01 through AC-11 all PASS with a concrete citation per row.
