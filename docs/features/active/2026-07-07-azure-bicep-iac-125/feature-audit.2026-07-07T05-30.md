# Feature Audit: azure-bicep-iac (#125)

**Audit Date:** 2026-07-07
**Branch:** `feature/azure-bicep-iac-125` @ `b3a252b`
**Work Mode:** `full-feature` (persisted marker `- Work Mode: full-feature` in `issue.md`)
**AC Sources:** `spec.md` (Seeded Test Conditions) and `user-story.md` (Acceptance Criteria), per `acceptance-criteria-tracking`; `issue.md`'s early-draft Acceptance Criteria list is a courtesy mirror of the same set, tracked in lockstep by the plan.

## Scope and Baseline

- Resolved base branch: `epic/openclaw-vision-integration` (origin/epic/openclaw-vision-integration; the feature branches from and merges back into this epic-integration branch, not `main`, per the caller's explicit instruction and confirmed by `git merge-base HEAD origin/epic/openclaw-vision-integration`).
- Merge-base SHA: `7a29286b687f00c6a10809efa41102c78f009c36`; branch head: `b3a252b`.
- Evidence sources: `artifacts/pr_context.summary.txt` and `artifacts/pr_context.appendix.txt` (both absent at review start and regenerated directly from git for this review — branch/head/base/merge-base, commit list, name-status, diff stat for the summary; full `git diff` for the appendix), the authoritative `git diff 7a29286..HEAD` (42 files, +1697/-1), executor evidence under `docs/features/active/2026-07-07-azure-bicep-iac-125/evidence/` (18 artifacts across `baseline/`, `qa-gates/`, `other/`), the runbook at `runbooks/azure-bicep-deployment.runbook.md`, and the reviewer's independent verification at branch head (PSScriptAnalyzer zero diagnostics on both new PowerShell files; `Invoke-Formatter` zero diffs; isolated Pester re-run of the new test file, 7/7 pass; independent confirmation that `bicep`/`az` CLI are absent locally; independent `gh run list` check for a green workflow run against the branch head).
- Note: no repo-local PR-context collector script exists in `scripts/`; both PR-context artifacts were self-generated per the accepted #119/#120 fallback (never trust a caller's "just regenerated" claim without an `ls` check first).

## Acceptance Criteria Inventory

Six checkbox criteria, present identically in `issue.md` (Acceptance Criteria, early draft), `spec.md` (Seeded Test Conditions — a 3-item subset restated as the top-level testing lens; the full 6-item AC list is not separately headed in `spec.md` beyond the Seeded Test Conditions and the Definition of Done, both unchecked planning-aid lists that mirror the same intent), and `user-story.md` (Acceptance Criteria, 6 items). All six arrived checked `[x]` in `issue.md` and `user-story.md`; `spec.md`'s 3-item Seeded Test Conditions list (a narrower restatement of AC-4/AC-5/AC-3 in test-condition form) is also fully checked `[x]`.

1. AC-1 — `deploy/azure/` contains parameterized Bicep templates that declaratively provision: (a) an Azure Container Apps environment + Container App hosting the existing `openclaw-core` container image, (b) an Azure Key Vault instance, and (c) queue infrastructure (Service Bus namespace/queue or Storage Queue) suitable for a future durable `INotificationQueue` implementation.
2. AC-2 — No secrets, connection strings, or credentials are committed; secret-shaped parameters are marked `@secure()` and sourced from Key Vault references or deployment-time parameters only.
3. AC-3 — Templates are parameterized per environment (at minimum a `main.bicep` + a `parameters.<env>.json`/`.bicepparam` pair for at least one environment).
4. AC-4 — Bicep templates pass declarative structural validation (`bicep build` and, where available, `bicep lint`/`az bicep build --stdout`) with zero errors; if the `bicep` CLI is unavailable in this environment, structural review + documented rationale substitutes and is recorded as such.
5. AC-5 — Live tenant deployment (`az deployment group create`) is explicitly out of scope for automated execution and is documented as a `human_interaction` exception with a runbook, consistent with the F11/F14/F15/F17 precedent.
6. AC-6 — No changes to `OpenClaw.Core` or `OpenClaw.Core.CloudSync` runtime behavior; this is IaC-only.

## Acceptance Criteria Evaluation

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| AC-1 | Bicep templates declaratively provision Container Apps + Key Vault + queue infrastructure | PASS | Reviewer read all four `.bicep` files: `main.bicep` orchestrates `containerApp.bicep` (Container Apps managed environment + Container App running the `containerImage` parameter, ingress `targetPort: 8081` matching `deploy/docker/openclaw-core.Dockerfile`'s `EXPOSE 8081`), `keyVault.bicep` (Key Vault with `enableRbacAuthorization: true`), and `queue.bicep` (Service Bus namespace + queue). All three modules declare the outputs `main.bicep` consumes; `main.bicep` declares all five outputs named in `spec.md`'s API/CLI Surface section, sourced one-for-one from module outputs. Executor's Phase 1/Phase 2 structural reviews (`phase1-bicep-structural-review.2026-07-07T01-40.md`, `phase2-bicep-structural-review.2026-07-07T01-50.md`) and the consolidated Phase 6 review (`final-bicep-yaml-structural-review.2026-07-07T03-00.md`) independently corroborate this against the research artifact's Requirements Mapping table (§8), which the reviewer also read and cross-checked. |
| AC-2 | No secrets/connection strings/credentials committed | PASS | Reviewer grep across all five `.bicep` files for `connectionString`/`listKeys`/`primaryConnectionString`: zero matches. `queue.bicep` outputs only `serviceBusEndpoint` (host name) and the queue name; `keyVault.bicep` declares zero secret values anywhere and uses RBAC (not access-policy) authorization. `main.dev.bicepparam`'s `containerImage` value is an explicitly-labeled placeholder (`'REPLACE_AT_DEPLOY_TIME/openclaw-core:unset'`) containing no secret-shaped content, independently confirmed against all four of the new PowerShell script's detection patterns. The new `scripts/Test-OpenClawBicepParameterSecrets.ps1` script (and its wiring into `.github/workflows/_bicep-validate.yml`) provides an automated guard against future regressions on this criterion; reviewer independently ran the mirrored Pester test in isolation (7/7 pass) confirming the scanner correctly detects a synthetic secret-shaped literal and correctly passes a clean file. **Note on the AC's literal `@secure()` wording:** no parameter in `main.bicep` is currently marked `@secure()` because no secret-shaped parameter exists yet in this feature's scope (the templates provision infrastructure but do not yet wire any credential consumption) — this is consistent with the criterion's disjunctive intent ("secret-shaped parameters are marked `@secure()` ... only," which is vacuously satisfied when no secret-shaped parameter is declared) and with `user-story.md`'s Non-Goals, which explicitly defer Key Vault/credential consumption wiring to a future feature. |
| AC-3 | Templates parameterized per environment (`main.bicep` + `.bicepparam` pair) | PASS | `deploy/azure/parameters/main.dev.bicepparam` exists with `using 'main.bicep'`, binds `environmentName = 'dev'`, and leaves `containerImage` to be supplied at deploy time (no dev-time default, since no image has been pushed to a registry from this workspace, per `spec.md`). Reviewer confirmed the `main.<env>.bicepparam` naming convention (stated in `spec.md`'s Versioning section) is genuinely extensible: `main.bicep`'s module deployment names are derived from `resourceNamePrefix`/`environmentName`, so a future `main.prod.bicepparam` would not collide with the `dev` deployment. Executor's Phase 3 structural review (`phase3-bicep-structural-review.2026-07-07T02-00.md`) corroborates. |
| AC-4 | `bicep build`/structural validation passes with zero errors; documented CLI-unavailable fallback if `bicep` is absent | PASS | Reviewer independently confirmed neither the `bicep` CLI nor the `az` CLI is installed in this local execution environment (`Get-Command bicep -ErrorAction SilentlyContinue`/`Get-Command az -ErrorAction SilentlyContinue`, both empty), matching the executor's `evidence/baseline/cli-tooling-availability.2026-07-07T01-32.md`. The executor's documented structural-review fallback (brace-balance checks, resource/parameter/output declaration verification, secret-shaped-literal greps, cross-checked against the research artifact's Requirements Mapping table) was performed across five gates (Phases 1-4 plus the consolidated Phase 6 review), all recorded PASS, and reviewer independently spot-verified the underlying file content matches every claim in those reviews. Real `bicep build` execution is correctly deferred to `.github/workflows/_bicep-validate.yml` on the `windows-latest` GitHub Actions runner, per the criterion's own explicit fallback clause — this criterion's structural-validation intent is met; whether that CI execution has actually produced a **green run against this branch's head** is a distinct, CI-gate-level requirement evaluated in the policy audit (`modified-workflow-needs-green-run`), not this AC, and is recorded there as a Blocking finding pending a `workflow_dispatch`. |
| AC-5 | Live deployment out of scope, documented as a `human_interaction` exception with a runbook | PASS | `docs/features/active/2026-07-07-azure-bicep-iac-125/runbooks/azure-bicep-deployment.runbook.md` exists with the five required sections in order (Cue, Prerequisites, Step-by-step Instructions, Verification, Source and Citation — reviewer confirmed via direct read and a `grep -n "^## "` structural check), each step carrying a dated Microsoft Learn citation (7 rows, all dated 2026 within the source table). The executor's `human-interaction-deployment-exception-note.2026-07-07T03-05.md` confirms no task in the plan invokes `az deployment group create`/`az deployment sub create` (reviewer independently grepped the diff for both strings: the only occurrences are inside the runbook's own command-example text and a documentation comment in `main.dev.bicepparam`, neither an executed command). Recording the exception in `artifacts/orchestration/orchestrator-state.json`'s `human_interaction` block (per `.claude/rules/orchestrator-state.md`'s invariants) is the orchestrator's responsibility, not this branch's files; this criterion's branch-side documentation half is complete. |
| AC-6 | No `OpenClaw.Core`/`OpenClaw.Core.CloudSync` runtime behavior change | PASS | Reviewer independently confirmed via `git diff --stat 7a29286..HEAD` that no file under `OpenClaw.Core/` or `OpenClaw.Core.CloudSync/` (or any other `.sln`-project source tree) appears anywhere in the diff. The only existing-file edit is the single documented `.github/workflows/ci.yml` wiring change (one added job referencing the new reusable workflow; the three pre-existing jobs are byte-identical per `git diff .github/workflows/ci.yml`, independently re-run by the reviewer). Matches the executor's `no-runtime-change-verification.2026-07-07T03-10.md`. |

## Summary

All six acceptance criteria evaluate PASS with independently re-verified evidence: the Bicep parameter/output contract was verified by direct file reads and cross-checked against `spec.md`'s API/CLI Surface section; the no-secrets criterion was verified by grep across all Bicep files plus an isolated re-run of the new secret-scan test (7/7 pass); the environment-parameterization criterion was verified by reading the `.bicepparam` binding and confirming the naming scheme's extensibility; the structural-validation criterion was verified by independently confirming CLI unavailability and cross-checking the executor's five structural-review gates against the actual file contents; the human-interaction/runbook criterion was verified by a full read of the runbook against its five-section contract and an independent grep confirming no live-deployment command is ever executed by this branch; and the no-runtime-change criterion was verified by an independent `git diff --stat` scan of the full branch range.

One item outside the AC set itself is recorded as a **Blocking finding in the policy audit** (`policy-audit.2026-07-07T05-30.md`, Section 8): the `modified-workflow-needs-green-run` rule fires because the diff modifies `.github/workflows/ci.yml` and adds `.github/workflows/_bicep-validate.yml`, and no workflow run with a head SHA matching `b3a252b` currently exists (`gh run list` returns zero rows for this branch as of this audit). This is a CI-gate-level procedural requirement, distinct from AC-4's structural-validation intent (which is met), and does not reflect a defect in any acceptance criterion's delivered content. It is expected to close once a `workflow_dispatch` of `ci.yml` (or `_bicep-validate.yml`) against the branch head (or the eventual PR head) produces a `success` conclusion — see `remediation-inputs.2026-07-07T05-30.md`.

The branch is additive apart from the single documented `ci.yml` edit and the `.gitignore` re-include line; the evidence-location scan is clean (all evidence under the canonical `docs/features/active/2026-07-07-azure-bicep-iac-125/evidence/<kind>/` paths); no evidence was written to any `artifacts/` sub-path. Findings from the code review: one Minor (a preview Service Bus API version — follow-up recommended, non-blocking since no live deployment occurs from this branch) and three Info observations, none of which affect any acceptance criterion's PASS verdict. **Recommendation: Go for PR contingent on closing the single Blocking, CI-gate-level finding** (green workflow run against the branch head); no acceptance criterion requires further work.

## Acceptance Criteria Check-off

All six criteria were already checked `[x]` in `issue.md` and `user-story.md`, and the corresponding three-item Seeded Test Conditions subset was already checked `[x]` in `spec.md`, at the start of this review — checked off by the plan's executor one item at a time as each corresponding task passed verification (per the plan's Phase 2/3/5/6 check-off tasks: P2-T4, P3-T4, P5-T6, P6-T6, P6-T8, P6-T9), not batched at plan end. Per the acceptance-criteria-tracking protocol, the reviewer verified each PASS verdict independently against the checked state; no new check-offs were needed and no criterion required unchecking. Criterion text was not modified in any source file.

(The unchecked boxes under `issue.md`'s "Next Step" list, `issue.md`'s "Test Conditions to Consider" list, and `spec.md`'s "Definition of Done" list are planning aids that pre-date and mirror the acceptance criteria in a different form — they were left untouched, consistent with the F11/#111 precedent for planning-aid checklists that are not the authoritative AC source.)

### Acceptance Criteria Status
- Source: `docs/features/active/2026-07-07-azure-bicep-iac-125/spec.md`, `docs/features/active/2026-07-07-azure-bicep-iac-125/user-story.md` (mirror: `issue.md`)
- Total AC items: 6 (identical set in `issue.md` and `user-story.md`; `spec.md`'s Seeded Test Conditions restate 3 of the 6 in test-condition form, also fully checked)
- Checked off (delivered): 6
- Remaining (unchecked): 0
- Items remaining: none
