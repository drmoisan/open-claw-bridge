# Feature Audit: one-on-one-move-history (#105)

**Audit Date:** 2026-07-02
**Branch:** `feature/one-on-one-move-history-105` @ `be385342eb4f5b0ccc999e15730f7450d59a19b9`
**Work Mode:** `full-feature` (persisted marker `- Work Mode: full-feature` in `issue.md`)
**AC Sources:** `spec.md` and `user-story.md` (identical AC sets; mirrored in `issue.md`)

## Scope and Baseline

- Resolved base branch: `main` (origin/main; the local `main` ref is stale per caller inputs).
- Merge-base SHA: `ee387dc8c8498148a0220bd8107b22161c5d444a`; branch head: `be385342eb4f5b0ccc999e15730f7450d59a19b9` (single commit).
- Evidence sources: `artifacts/pr_context.summary.txt` and `artifacts/pr_context.appendix.txt` (refreshed for this head — head SHA verified to match), the authoritative `git diff ee387dc..be38534` (22 files, +1834/-1; 5 production `.cs`, 3 test `.cs`), executor evidence under `docs/features/active/2026-07-02-one-on-one-move-history-105/evidence/`, and the reviewer's independent toolchain re-run at branch head (format EXIT 0; build 0/0; 774 tests passed / 0 failed / 5 env-gated skips; fresh cobertura parsed per changed file — `evidence/qa-gates/coverage-review.2026-07-02T14-35.md`).
- Note: the PR-context summary's file categorization ("Core logic changes: 0 files") is inaccurate for this branch; the audit scoped from the authoritative git diff (recorded in the policy audit's Rejected Scope Narrowing section — fourth consecutive occurrence of this summary miscategorization).

## Acceptance Criteria Inventory

Five checkbox criteria, identical in `spec.md` (lines 134-143) and `user-story.md` (lines 54-63), mirrored in `issue.md` (lines 25-34). All arrived checked `[x]` by the executor.

1. AC-1 — `series_moves` table with idempotent migration (fresh-database DDL and pre-existing-database upgrade both tested); `ISeriesMoveHistory` round-trips survive restart; duplicate records idempotent (`ON CONFLICT ... DO NOTHING`); repository partial clock-free (caller-supplied timestamps only).
2. AC-2 — Pure `OneOnOneMoveGuard` implements master Section 10.3 for `ONE_ON_ONE` exactly (allow only when moves-in-last-six-occurrences < 2 AND NOT moved-previous-week) with the D1/D2 anchor and rolling-window semantics; unit truth table (0/1/2 moves x previous-week present/absent, exactly-six boundary, -7/-8-day boundaries) and CsCheck properties including monotonicity.
3. AC-3 — Non-`ONE_ON_ONE` classifications unaffected: delegation to `MovePolicy.CanMove` unchanged, proven by regression/equivalence tests.
4. AC-4 — `ISeriesMoveHistory` in `Agent/Contracts`, DI-registered in `Program.cs` mirroring `ISentActionStore`; no Runtime behavior changes (no production call site by design — F18 seam), documented in spec D5.
5. AC-5 — Full C# toolchain passes (CSharpier -> analyzers -> nullable -> architecture -> tests); coverage thresholds hold (line >= 85%, branch >= 75%) with changed lines covered; all new files <= 500 lines; no temp files in tests.

## Acceptance Criteria Evaluation

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| AC-1 | `series_moves` persistence: idempotent migration, restart round-trip, duplicate idempotency, clock-free | PASS | Diff: `series_moves` DDL appended to `CreateTablesSql` (`Schema.cs`) and duplicated as the lazy-ensure constant in the new partial; composite `PRIMARY KEY(series_key, occurrence_start_utc)` with `ON CONFLICT ... DO NOTHING`. 12 test cases in `CoreCacheRepositorySeriesMovesTests.cs`: fresh-path `InitializeAsync` idempotency (twice, no throw, table exists); pre-existing-database upgrade (seeded pre-#105 schema gains `series_moves`); lazy schema-ensure without `InitializeAsync`; round-trip; duplicate pair leaves exactly one row (direct row-count query); restart persistence via a second repository instance over one shared in-memory database; non-UTC normalization to byte-exact "O" UTC; ordering; isolation; blank-key `ArgumentException` x3. Clock-free verified twice: executor grep scan (`evidence/other/scope-and-size-verification.2026-07-02T14-25.md` section d, zero clock reads) and reviewer banned-API scan of the added diff lines (zero matches). Reviewer full-suite run green (327/327 Core.Tests). |
| AC-2 | Pure guard implements Section 10.3 exactly with D1/D2 semantics, truth table + boundaries + CsCheck properties incl. monotonicity | PASS | `OneOnOneMoveGuard.cs` (new, 161 lines): `ComputeAnswers` anchors at `UtcDateTime.Date`, builds the six-greatest-distinct-anchors window from occurrences plus candidate (future anchors excluded), counts per move row, and evaluates the half-open `[candidateAnchor - 7d, candidateAnchor)` interval; `CanMove` returns `history.MovesInLastSixOccurrences < 2 && !history.MovedPreviousWeek` for `ONE_ON_ONE`. Reviewer verified the code against spec D1/D2 line by line. Tests: complete 3x2 truth table (6 DataRows); exactly-six boundary (seventh-most-recent move excluded); fewer-than-six window; -7d blocked / -8d allowed / -1d blocked / same-anchor not-previous-week (4 DataRows); same-UTC-day double move counts as two; future-anchor exclusion — 27 unit cases total. CsCheck: monotonicity (adding a recorded move never flips blocked to allowed, 1000 iters), window invariance, and an independent half-open-interval oracle. Reviewer-parsed coverage 100.00% line / 100.00% branch for the file. |
| AC-3 | Non-1:1 kinds unaffected; delegation to `MovePolicy.CanMove` unchanged, proven by regression/equivalence tests | PASS | Diff absence proof: `MovePolicy.cs`, `RecurringMeetingClassifier.cs`, `NormalizedMeetingContext.cs` not in the diff (reviewer grep, exit 1). Delegation code path passes identical arguments to `MovePolicy.CanMove`. Equivalence property `CanMove_NonOneOnOneKinds_DelegateToMovePolicyUnchanged` (1000 iters) compares guard vs policy for generated non-1:1 contexts across attendee sets, requesters, priorities, and histories. Regression evidence: `evidence/regression-testing/movepolicy-regression.2026-07-02T14-17.md` (17 existing MovePolicy/classifier tests pass unmodified, EXIT 0), reconfirmed inside the reviewer's 327/327 Core.Tests run. |
| AC-4 | Contract in `Agent/Contracts`, DI-registered mirroring `ISentActionStore`, no Runtime behavior changes, D5 documented | PASS | File location verified: `src/OpenClaw.Core/Agent/Contracts/ISeriesMoveHistory.cs` (namespace `OpenClaw.Core.Agent`, matching `ISentActionStore`). Diff: `Program.cs` adds `builder.Services.AddSingleton<ISeriesMoveHistory>(sp => sp.GetRequiredService<CoreCacheRepository>());` immediately after the `ISentActionStore` registration — same shape, same repository instance. No `SchedulingWorker*` or other Runtime files in the diff; production diff is exactly the five planned files (`evidence/other/scope-and-size-verification.2026-07-02T14-25.md` section a, reviewer-confirmed via `git diff --stat`). Spec D5 documents the in-scope-by-design no-consumer status and the F18 obligations. Build green (0/0), reviewer-verified. |
| AC-5 | Full toolchain; coverage thresholds with changed lines covered; 500-line cap; no temp files | PASS | Reviewer re-ran at head `be38534`: `csharpier check .` EXIT 0 (223 files); `dotnet build` 0 warnings / 0 errors (analyzers + nullable as errors); NetArchTest boundary tests inside the 327/327 Core.Tests pass; full suite 774 passed / 0 failed / 5 env-gated skips; pooled coverage 90.91% line / 80.74% branch (gates 85/75); no regression (+0.10pp/+0.12pp; HostAdapter and MailBridge counts unchanged vs baseline); instrumented changed files at 100.00% line and branch (`OneOnOneMoveGuard.cs` 52/52 + 8/8, `Schema.cs` 37/37 + 6/6, `Program.cs` 239/239); `CoreCacheRepository.SeriesMoves.cs` uninstrumented under the pre-existing async/CompilerGenerated measurement configuration and behaviorally covered by its 12 tests (documented, accepted #99/#103 disposition — policy audit Section 8); all 8 touched `.cs` files <= 500 lines (max 352, `wc -l` reviewer-verified); no temp files (in-memory shared-cache SQLite only; executor grep scan zero matches). |

## Summary

All five acceptance criteria evaluate PASS with independently re-verified evidence: the diff delivers exactly the spec's implementation scope (three new production files, two one-line-scale modifications), the guard's window arithmetic was verified against spec D1/D2 line by line and carries 100% reviewer-parsed line and branch coverage, the persistence layer is proven across fresh/upgrade/restart paths on real in-memory SQLite, the delegation contract is proven both by diff absence and by a generated-input equivalence property, and the reviewer's fresh toolchain and coverage runs reproduce the executor's numbers exactly (pooled counts 4353/4788 line, 998/1236 branch). No Blocking or Major findings; the code review records three Info observations (async instrumentation scope — pre-existing; optional `ResolveSeriesKey` property test; write-side-only key validation matching the `SentActions` precedent). Remediation is not required. Recommendation: **Go for PR**.

## Acceptance Criteria Check-off

All five criteria were already checked `[x]` by the executor in both authoritative source files (`spec.md`, `user-story.md`) and the `issue.md` mirror. Per the acceptance-criteria-tracking protocol, the reviewer verified each PASS verdict against the checked state; no new check-offs were needed and no criterion required unchecking. Criterion text was not modified.

### Acceptance Criteria Status
- Source: `docs/features/active/2026-07-02-one-on-one-move-history-105/spec.md`, `docs/features/active/2026-07-02-one-on-one-move-history-105/user-story.md` (mirror: `issue.md`)
- Total AC items: 5
- Checked off (delivered): 5
- Remaining (unchecked): 0
- Items remaining: none
