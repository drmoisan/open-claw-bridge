# Feature Audit: outbound-audit-log (#107)

**Audit Date:** 2026-07-02
**Branch:** `feature/outbound-audit-log-107` @ `c5b19de6a118aa7bd2dbf5a2df2c350ffceb4c63`
**Work Mode:** `full-feature` (persisted marker `- Work Mode: full-feature` in `issue.md`)
**AC Sources:** `spec.md` and `user-story.md` (identical AC sets; mirrored in `issue.md`)

## Scope and Baseline

- Resolved base branch: `main` (origin/main; the local `main` ref is stale per caller inputs — reviewer-confirmed `git merge-base origin/main HEAD` returns the supplied SHA).
- Merge-base SHA: `a497bf0ce25741d9c0c908521f57cef7b4442b9e`; branch head: `c5b19de6a118aa7bd2dbf5a2df2c350ffceb4c63` (single commit).
- Evidence sources: `artifacts/pr_context.summary.txt` and `artifacts/pr_context.appendix.txt` (refreshed for this head — head SHA verified to match), the authoritative `git diff a497bf0..c5b19de` (37 files, +2264/-31; 11 production `.cs`, 7 test `.cs`), executor evidence under `docs/features/active/2026-07-02-outbound-audit-log-107/evidence/`, and the reviewer's independent toolchain re-run at branch head (format EXIT 0; build 0/0; 807 tests passed / 0 failed / 5 env-gated skips; fresh cobertura parsed per changed file — `evidence/qa-gates/coverage-review.2026-07-02T15-50.md`).
- Note: the PR-context summary's file categorization ("Core logic changes: 0 files") is inaccurate for this branch; the audit scoped from the authoritative git diff (recorded in the policy audit's Rejected Scope Narrowing section — fifth consecutive occurrence of this summary miscategorization). The summary's author-asserted autoclose list (`#74`, `#75`, `#ISO-8601`) is parsing noise from spec prose; only #107 is the closing issue.

## Acceptance Criteria Inventory

Six checkbox criteria, identical in `spec.md` (lines 199-206) and `user-story.md` (lines 45-52), mirrored in `issue.md` (lines 23-30). All arrived checked `[x]` by the executor.

1. AC1 — `audit_log` table with idempotent migration (fresh-database DDL plus lazy schema-ensure on pre-existing databases); records round-trip and are queryable by message id, most recent first, and survive restart (a new repository instance on the same database reads records written by the previous instance).
2. AC2 — One audit record per Stage 0 decision point with the master §13 step 12 fields: successful send (`sent`), failed send (`send_failed` with error detail, written before the exception propagates to `ProcessMessageSafelyAsync`), dedupe hit (`dedupe_skipped`), kill-switch suppression (`send_disabled`).
3. AC3 — Audit-write failure does not break message processing: the worker logs the failure at Error and continues; an audit-write failure on the send-failure path does not mask the original send exception.
4. AC4 — Repository clock-free (recorded-at supplied by the worker from `TimeProvider`); correlation id is a worker-generated GUID per outbound-action evaluation, forwarded to the HostAdapter send call as its request id through the existing `IHostAdapterClient.SendMailAsync` `requestId` parameter (D5).
5. AC5 — Property-based round-trip test (CsCheck): `ActionAuditRecord` fields survive persistence unchanged, with timestamps normalized to UTC round-trip (O) form.
6. AC6 — Full C# toolchain passes (format -> lint -> type-check -> architecture -> test); line coverage >= 85% and branch coverage >= 75% hold with changed lines covered.

## Acceptance Criteria Evaluation

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| AC1 | `audit_log` persistence: idempotent migration, query most-recent-first, restart survival | PASS | Diff: `audit_log` DDL + `idx_audit_log_message_id` index appended to `CreateTablesSql` (`Schema.cs`) and duplicated verbatim as the lazy-ensure constant in the new `CoreCacheRepository.AuditLog.cs` partial (reviewer verified byte-identical to the spec's Data & State SQL). 22 test cases in `CoreCacheRepositoryAuditLogTests.cs`: full-field and null-optional round-trips; `ORDER BY recorded_at_utc DESC, id DESC` most-recent-first with the id tie-break pinned under identical timestamps; restart survival via a second repository instance over one shared in-memory database; `InitializeAsync` idempotency (twice, no throw, table exists); pre-existing-database upgrade (seeded pre-#107 schema gains `audit_log`); lazy schema-ensure without `InitializeAsync`; non-UTC-offset instants preserved through UTC normalization; 12 required-field guard DataRows. Executor filter run 23/23 pass (`evidence/regression-testing/repository-audit-tests.2026-07-02T15-15.md` EXIT 0); reviewer full-suite run green (360/360 Core.Tests). |
| AC2 | One record per Stage 0 decision point with master §13 step 12 fields | PASS | Diff: exactly four `WriteAuditSafelyAsync` emission sites in `ProposeAndActAsync`, mapped one-to-one onto the existing branches (reviewer verified against spec Behavior items 1-4; no fifth emission): `send_disabled` in the `!options.SendEnabled` branch; `dedupe_skipped` in the `IsRecordedAsync` hit branch; `sent` after the send and before the dedupe record; `send_failed` with `{ExceptionType}: {Message}` detail inside a filtered catch before `throw;` (durable before propagation). `ActionAuditRecord` carries the full §13 step 12 field mapping (D1, XML-doc'd). Tests: 4 decision-point tests with full field assertions incl. exact acting-flags strings, call-order proof (send -> audit -> dedupe-record), and dedupe-record-never-written-after-failure. Red/green evidence: `schedulingworker-audit-expect-fail.2026-07-02T15-26.md` (EXIT 1, 8/8 audit tests failing pre-wiring) and `schedulingworker-audit-pass-after.2026-07-02T15-30.md` (EXIT 0). |
| AC3 | Audit-write failure never breaks processing; no masking of the original send exception | PASS | Diff: all four emissions route through the single `WriteAuditSafelyAsync` helper (spec D4): narrow filtered catch (`when (exception is not OperationCanceledException)`), Error log with message id and result code, continue. Tests: `RunCycle_AuditWriteFailure_OnSuccessPath_ContinuesAndLogsError` (audit sink throws; dedupe record still written once; Error log verified via captured `Mock<ILogger>` with exception identity) and `RunCycle_AuditWriteFailure_OnFailurePath_DoesNotMaskOriginalException` (send and audit both throw; the isolation layer receives the original `InvalidOperationException("send failed")`, and the audit failure is separately Error-logged). Reviewer verified the `send_failed` write happens inside the catch with `throw;` (not `throw exception;`) so the original exception and stack propagate unreplaced. |
| AC4 | Clock-free repository; correlation id is a worker GUID forwarded as the HostAdapter request id | PASS | Clock-free: `CoreCacheRepository.AuditLog.cs` has no `TimeProvider`/clock usage (reviewer banned-API scan of added diff lines: zero matches for DateTime.Now/UtcNow; `RecordedAtUtc` is caller-supplied); the worker sources it from the injected `TimeProvider` (`BuildAuditRecord` uses `timeProvider.GetUtcNow()`), pinned by `RunCycle_SendSuccess_RecordedAtUtcEqualsFakeTimeProviderValue`. Correlation: one `Guid.NewGuid().ToString()` per outbound-action evaluation at the top of the gated block, stamped on every record for that action; `ISchedulingService.SendMailAsync` gains optional `correlationId` which `HostAdapterSchedulingService` forwards as the pre-existing `requestId:` argument (no HostAdapter/MailBridge/wire changes — reviewer verified only `src/OpenClaw.Core` production paths in the diff). Tests: `RunCycle_SendSuccess_CorrelationIdIsGuidAndMatchesForwardedValue` (GUID-parseable; equals the captured seam argument) plus the two seam tests `SendMailAsync_SuppliedCorrelationId_ForwardsVerbatimAsRequestId` / `SendMailAsync_NullCorrelationId_ForwardsNullRequestId`. |
| AC5 | CsCheck property: fields survive persistence unchanged with UTC (O) normalization | PASS | `CoreCacheRepositoryAuditLogPropertyTests.RecordAsync_GetByMessageIdAsync_RoundTripsAfterUtcNormalization` (iter 100): generated records with non-empty alphabet-constrained required fields, half-null optional strings and timestamps, and arbitrary whole-minute offsets in the valid +/-14h range on every `DateTimeOffset` field (anchored away from `DateTime` extrema so offset arithmetic cannot overflow); oracle is record equality after `ToUniversalTime()` normalization of all five timestamp fields plus an explicit offset-zero assertion; fresh in-memory database per sample; failing seed printed by CsCheck. Genuine property test on the T1 module per `quality-tiers.md`. Included in the executor's 23/23 filter run and the reviewer's green full run. |
| AC6 | Full toolchain; coverage thresholds hold with changed lines covered | PASS | Reviewer re-ran at head `c5b19de`: `csharpier check .` EXIT 0 (231 files); `dotnet build` 0 warnings / 0 errors (analyzers + nullable as errors); NetArchTest boundary tests inside the 360/360 Core.Tests pass; full suite 807 passed / 0 failed / 5 env-gated skips; pooled coverage 96.83% line / 89.96% branch (gates 85/75); no regression (+0.04pp/+0.05pp; Core 98.78% -> 98.82% line, 91.95% -> 92.05% branch); every instrumented new/changed file at 100.00% line (and 100.00% branch where branch points exist); the only partial branches on a changed file are two pre-existing unchanged ternaries verified identical (1/2) at baseline lines 233/240 in the baseline cobertura; async bodies uninstrumented under the pre-existing runsettings exclusion and behaviorally verified (policy audit Section 8, accepted #99/#103/#105 disposition); all 18 touched `.cs` files <= 500 lines (max 480); no temp files (in-memory shared-cache SQLite only). Evidence: `evidence/qa-gates/coverage-review.2026-07-02T15-50.md`. |

## Summary

All six acceptance criteria evaluate PASS with independently re-verified evidence: the diff delivers exactly the spec's implementation scope (five new production files, six modified, three new test files plus four mechanically updated), the four emission points and their ordering guarantees were verified against the spec line by line and proven by call-order and red/green evidence, the D4 resilience boundary is tested in both directions including the no-masking guarantee, the persistence layer is proven across fresh/upgrade/lazy/restart paths on real in-memory SQLite with a genuine CsCheck round-trip property, and the reviewer's fresh toolchain and coverage runs reproduce the executor's numbers exactly (pooled 96.83% line / 89.96% branch; every instrumented changed file 100% line). No Blocking or Major findings; the code review records three Info observations (async instrumentation scope — pre-existing; query-side blank-key asymmetry matching the established precedent; optional exhaustive test for the trivial `BuildActingFlags` formatter). Remediation is not required. Recommendation: **Go for PR**.

## Acceptance Criteria Check-off

All six criteria were already checked `[x]` by the executor in both authoritative source files (`spec.md`, `user-story.md`) and the `issue.md` mirror. Per the acceptance-criteria-tracking protocol, the reviewer verified each PASS verdict against the checked state; no new check-offs were needed and no criterion required unchecking. Criterion text was not modified.

### Acceptance Criteria Status
- Source: `docs/features/active/2026-07-02-outbound-audit-log-107/spec.md`, `docs/features/active/2026-07-02-outbound-audit-log-107/user-story.md` (mirror: `issue.md`)
- Total AC items: 6
- Checked off (delivered): 6
- Remaining (unchecked): 0
- Items remaining: none
