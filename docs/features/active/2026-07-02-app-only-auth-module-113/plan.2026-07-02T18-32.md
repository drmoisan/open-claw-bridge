# app-only-auth-module — Plan

- **Issue:** #113
- **Parent (optional):** none
- **Owner:** drmoisan
- **Last Updated:** 2026-07-02T18-32
- **Status:** Draft
- **Version:** 1.0
- **Work Mode:** full-feature (per `issue.md` metadata)

## Required References

- Policy reading order per `.claude/skills/policy-compliance-order/SKILL.md`:
  1. `CLAUDE.md` (auto-loaded)
  2. `.claude/rules/general-code-change.md`
  3. `.claude/rules/general-unit-test.md`
  4. `.claude/rules/csharp.md` (C# is the only language in scope)
- Requirements sources (authoritative): `docs/features/active/2026-07-02-app-only-auth-module-113/spec.md` (5 AC, design decisions D1–D9), `docs/features/active/2026-07-02-app-only-auth-module-113/user-story.md`, `docs/features/active/2026-07-02-app-only-auth-module-113/issue.md`.

**All work must comply with these policies; do not duplicate their content here.**

## Conventions Used in This Plan

- `FEATURE` = `docs/features/active/2026-07-02-app-only-auth-module-113`
- `<ts>` = actual execution timestamp in `yyyy-MM-ddTHH-mm` form (per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`).
- Evidence artifacts live only under `FEATURE/evidence/<kind>/` (`baseline`, `regression-testing`, `qa-gates`, `other`). Writing evidence under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or similar is a policy violation.
- Raw command intermediates (TRX files, Cobertura XML, tool console dumps) go under `artifacts/csharp/`; evidence markdown summaries in `FEATURE/evidence/<kind>/` reference them.
- Every command-step evidence artifact MUST contain: `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (1–20 lines). Test-step artifacts MUST include numeric line and branch coverage in `Output Summary:`.
- C# toolchain commands (exact forms):
  - Format: `csharpier format .` (write) / `csharpier check .` (verify) — global tool 1.3.0; do not use `dotnet csharpier`.
  - Build (lint + type check: analyzers + nullable): `dotnet build OpenClaw.MailBridge.sln`
  - Test with coverage (includes architecture-boundary and contract tests in suite): `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`
- No-SKIPPED rule: every command-bearing task below must execute its stated command; `EXIT_CODE: SKIPPED` is not a passing outcome.

## Scope Guard (applies to all phases)

- Production diff is confined to: one `PackageReference` line added to `src/OpenClaw.Core/OpenClaw.Core.csproj`, plus exactly nine new files under `src/OpenClaw.Core/CloudAuth/` (`IAppTokenProvider.cs`, `AppAccessToken.cs`, `CloudAuthOptions.cs`, `CloudAuthOptionsValidator.cs`, `TokenFreshness.cs`, `TokenAcquisitionException.cs`, `ClientCredentialsTokenProvider.cs`, `CredentialFactory.cs`, `CloudAuthServiceCollectionExtensions.cs`). Test diff is confined to exactly six new files under `tests/OpenClaw.Core.Tests/CloudAuth/`. Plus feature-folder docs/evidence.
- **Untouched surfaces:** `src/OpenClaw.Core/Program.cs`, `src/OpenClaw.Core/appsettings.json`, `docker-compose.yml`/Docker deployment files, and `tests/OpenClaw.Core.Tests/Agent/AgentArchitectureBoundaryTests.cs` are not modified. Nothing calls `AddCloudAuth` (opt-in only; absence of registration is the gate per spec D8).
- The only new package dependency is `Azure.Identity` in `src/OpenClaw.Core/OpenClaw.Core.csproj` (spec D1/Constraints & Risks); the test project gains no new package (`Azure.Core` flows transitively via the project reference).
- No temp files anywhere, including tests (DI binding tests use `ConfigurationBuilder.AddInMemoryCollection`). Every new file stays <= 500 lines. No secret or realistic credential material in code, tests, or logs; test constants are obviously fake (e.g., `"fake-token-value"`, all-zero GUIDs).
- No wall-clock reads, `Task.Delay`, or `Thread.Sleep`: all expiry math via injected `TimeProvider`; tests use `FakeTimeProvider`; the single-flight concurrency test gates with `TaskCompletionSource` inside the mocked credential.
- Test stack: MSTest + FluentAssertions + Moq + CsCheck + `FakeTimeProvider` + NetArchTest — all already referenced by `OpenClaw.Core.Tests` (verified in `tests/OpenClaw.Core.Tests/OpenClaw.Core.Tests.csproj`).

## Implementation Plan (Atomic Tasks)

### Phase 0 — Baseline Capture

- [x] [P0-T1] Read the policy files in required order — `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/csharp.md` — and record the read in `FEATURE/evidence/baseline/phase0-instructions-read.md`.
  - Acceptance: Artifact exists at `docs/features/active/2026-07-02-app-only-auth-module-113/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of the four files read.
- [x] [P0-T2] Capture the formatting baseline by running `csharpier check .` from the repo root and recording the result in `FEATURE/evidence/baseline/baseline-format.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command: csharpier check .`, `EXIT_CODE:`, and `Output Summary:` stating pass/fail and any file count reported.
- [x] [P0-T3] Capture the build baseline (analyzers + nullable type checking) by running `dotnet build OpenClaw.MailBridge.sln` and recording the result in `FEATURE/evidence/baseline/baseline-build.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` including warning and error counts.
- [x] [P0-T4] Capture the test-and-coverage baseline by running `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"`, storing raw TRX/Cobertura output under `artifacts/csharp/`, and recording the result in `FEATURE/evidence/baseline/baseline-test-coverage.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` containing pass/fail test counts plus numeric baseline line-coverage percent and branch-coverage percent (no placeholders); artifact names the raw Cobertura file path under `artifacts/csharp/`.
- [x] [P0-T5] Record the pre-change state of the untouched surfaces — `git rev-parse HEAD`, `git hash-object` for `src/OpenClaw.Core/Program.cs`, `src/OpenClaw.Core/appsettings.json`, `tests/OpenClaw.Core.Tests/Agent/AgentArchitectureBoundaryTests.cs`, and `docker-compose.yml`, plus verification that `src/` contains no `Azure.Identity`, `Microsoft.Identity`, or `CloudAuth` reference today (`grep` over `src/`) — into `FEATURE/evidence/baseline/baseline-untouched-surfaces.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, the commit SHA, the four file hashes with paths, and the grep commands with their empty-result confirmation, to support the AC-5 no-behavior-change verification in Phase 5.

### Phase 1 — Dependency Landing (Azure.Identity, Build-Gated)

- [x] [P1-T1] Add `<PackageReference Include="Azure.Identity" Version="<latest stable>" />` to the existing `<ItemGroup>` of package references in `src/OpenClaw.Core/OpenClaw.Core.csproj`, pinning the exact latest stable version available at implementation time (versions are inline in this repo; there is no `Directory.Packages.props`).
  - Acceptance: The csproj contains exactly one new `PackageReference` line with an explicit pinned version; no other line of the csproj changes; no package is added to `tests/OpenClaw.Core.Tests/OpenClaw.Core.Tests.csproj`. (AC-5)
- [x] [P1-T2] Verify restore and build succeed with the new dependency by running `dotnet build OpenClaw.MailBridge.sln` and recording the result in `FEATURE/evidence/other/dependency-build-gate.<ts>.md`; if restore or build fails, resolve the version pin before any Phase 2 task begins.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` naming the resolved `Azure.Identity` version and confirming 0 errors; this gate precedes all production code so restore issues surface early. (AC-5)
- [x] [P1-T3] Update the dependency record in `FEATURE/spec.md` (Constraints & Risks, "New dependency — Azure.Identity" bullet): replace "latest stable 1.x at implementation time" with the exact pinned version from P1-T1, keeping the existing dependency-policy justification text intact.
  - Acceptance: `spec.md` names the exact pinned `Azure.Identity` version and retains the justification per `.claude/rules/general-code-change.md` dependency policy; no other spec content changes. (AC-5)

### Phase 2 — Contract Types, Options, and Pure Logic (Create, Then Test)

> Create-then-test ordering: every test task in this phase compiles against types created earlier in the same phase. No `[expect-fail]` task exists in this plan because no test compiles against a pre-existing type — all tests target newly created `OpenClaw.Core.CloudAuth` types, so a fail-before run is structurally impossible (the test code would not compile before the type exists).

- [x] [P2-T1] Create `src/OpenClaw.Core/CloudAuth/IAppTokenProvider.cs` declaring `public interface IAppTokenProvider` with the single member `ValueTask<AppAccessToken> GetTokenAsync(CancellationToken cancellationToken)`, in file-scoped namespace `OpenClaw.Core.CloudAuth`, with XML docs stating the host-neutral contract (no Azure/MSAL types cross it).
  - Acceptance: File exists, <= 500 lines, contains only the interface per spec D3; namespace is `OpenClaw.Core.CloudAuth`. (AC-1)
- [x] [P2-T2] Create `src/OpenClaw.Core/CloudAuth/AppAccessToken.cs` declaring `public sealed record AppAccessToken(string Token, DateTimeOffset ExpiresOn)` with `public override string ToString() => $"AppAccessToken(ExpiresOn: {ExpiresOn:O})"` so the token value never appears in `ToString()` output, with XML docs explaining the redaction rationale.
  - Acceptance: File exists, <= 500 lines; the record overrides `ToString()` to emit the expiry only, per spec D3. (AC-1, AC-2)
- [x] [P2-T3] Create `src/OpenClaw.Core/CloudAuth/TokenAcquisitionException.cs` declaring `public sealed class TokenAcquisitionException : Exception` carrying `TenantId`, `ClientId`, and `Scope` properties and an inner exception, whose message includes tenant/client/scope context and never includes token, secret, or certificate material.
  - Acceptance: File exists, <= 500 lines; the exception exposes the three context properties, preserves the inner exception, and its message construction cannot echo credential values, per spec D7. (AC-2)
- [x] [P2-T4] Create `src/OpenClaw.Core/CloudAuth/CloudAuthOptions.cs` declaring `public sealed class CloudAuthOptions` (plain `get; set;` properties, matching the `AgentPolicyOptions` convention) with the seven properties and defaults from spec D5: `TenantId` = `""`, `ClientId` = `""`, `CertificatePath` = `""`, `ClientSecret` = `""`, `Scope` = `"https://graph.microsoft.com/.default"`, `AuthorityHost` = `"https://login.microsoftonline.com/"`, `RefreshSkewMinutes` = `5`, each with XML docs including the env binding form (`OpenClaw__CloudAuth__<Name>`).
  - Acceptance: File exists, <= 500 lines; all seven properties present with exactly the D5 defaults; no validation logic in this file. (AC-1, AC-3)
- [x] [P2-T5] Create `src/OpenClaw.Core/CloudAuth/CloudAuthOptionsValidator.cs` declaring a pure static `CloudAuthOptionsValidator.Validate(CloudAuthOptions options)` returning the full list of violation messages that: requires non-whitespace `TenantId` and `ClientId`; enforces exactly-one of `CertificatePath`/`ClientSecret` (zero configured -> error; both configured -> reject-ambiguous error); requires `Scope` to be an absolute URI ending with `/.default`; requires `AuthorityHost` to be an absolute `https` URI; requires `0 <= RefreshSkewMinutes <= 60`; and names offending keys without ever echoing configured values.
  - Acceptance: File exists, <= 500 lines; the method is static, pure (no I/O, no clock, no state), returns all violations (not first-failure), and no message interpolates a property value, per spec D5. (AC-2)
- [x] [P2-T6] Create `src/OpenClaw.Core/CloudAuth/TokenFreshness.cs` declaring the pure internal static predicate `TokenFreshness.IsFresh(AppAccessToken? token, DateTimeOffset nowUtc, TimeSpan skew)` returning `token is not null && nowUtc < token.ExpiresOn - skew`, with XML docs stating the boundary semantics (at exactly `ExpiresOn - skew` the token is stale).
  - Acceptance: File exists, <= 500 lines; predicate is pure and matches the D6 definition exactly, including staleness at the exact boundary instant. (AC-3)
- [x] [P2-T7] Create `tests/OpenClaw.Core.Tests/CloudAuth/AppAccessTokenTests.cs` (MSTest + FluentAssertions) covering: (a) `ToString()` output contains the ISO-8601 expiry and does not contain the token value; (b) string interpolation of the record does not leak the token; (c) record value semantics (equality on same Token/ExpiresOn, inequality otherwise); using an obviously fake token constant (e.g., `"fake-token-value"`).
  - Acceptance: File exists in the `tests/` mirror location, <= 500 lines; all three scenario groups asserted with Arrange–Act–Assert and descriptive names; no realistic secret material. (AC-1, AC-2)
- [x] [P2-T8] Create `tests/OpenClaw.Core.Tests/CloudAuth/CloudAuthOptionsValidatorTests.cs` (MSTest + FluentAssertions + CsCheck) covering the full validation matrix: valid certificate-only passes; valid secret-only passes; both-set rejected (reject-ambiguous); neither-set rejected; blank/whitespace `TenantId` rejected; blank/whitespace `ClientId` rejected; malformed `Scope` (non-URI, and valid URI not ending `/.default`) rejected; malformed `AuthorityHost` (non-URI, and `http` scheme) rejected; `RefreshSkewMinutes` of `-1` and `61` rejected while `0`, `5`, `60` pass; multiple simultaneous violations all reported; no violation message echoes a configured value; plus at least one CsCheck property (e.g., for arbitrary option mutations, validity implies exactly one credential source is set and violations never contain the configured secret/path strings).
  - Acceptance: File exists, <= 500 lines; every matrix row above has an explicit assertion; at least one CsCheck property targets `CloudAuthOptionsValidator.Validate` (T1 obligation); dummy values are clearly fake. (AC-2, AC-4)
- [x] [P2-T9] Create `tests/OpenClaw.Core.Tests/CloudAuth/TokenFreshnessTests.cs` (MSTest + FluentAssertions + CsCheck) covering: null token is stale; token fresh one tick before `ExpiresOn - skew`; token stale at exactly `ExpiresOn - skew`; token stale after expiry; zero skew behavior; plus at least one CsCheck property (e.g., monotonicity — if fresh at time t then fresh at every earlier time; and `IsFresh` agrees with the definitional inequality for arbitrary token/now/skew combinations).
  - Acceptance: File exists, <= 500 lines; the exact-boundary case is example-based (not property-only); at least one CsCheck property targets `TokenFreshness.IsFresh` (T1 obligation); no wall-clock reads. (AC-3, AC-4)

### Phase 3 — Provider: CredentialFactory and ClientCredentialsTokenProvider (Create, Then Test)

- [x] [P3-T1] Create `src/OpenClaw.Core/CloudAuth/CredentialFactory.cs` declaring `internal static class CredentialFactory` with a single method building the `Azure.Core.TokenCredential` from validated `CloudAuthOptions`: `ClientCertificateCredential` when `CertificatePath` is set, `ClientSecretCredential` when `ClientSecret` is set, honoring `AuthorityHost`; this is the only file in the module that instantiates `Azure.Identity` types.
  - Acceptance: File exists, <= 500 lines, class is `internal`; a repo-wide search shows `ClientCertificateCredential`/`ClientSecretCredential` appear only in this file, per spec D4/D5. (AC-1, AC-2)
- [x] [P3-T2] Create `src/OpenClaw.Core/CloudAuth/ClientCredentialsTokenProvider.cs` declaring `public sealed class ClientCredentialsTokenProvider : IAppTokenProvider` with: (a) public constructor `(CloudAuthOptions options, TimeProvider timeProvider, ILogger<ClientCredentialsTokenProvider> logger)` that runs `CloudAuthOptionsValidator.Validate` and throws an `ArgumentException`-family exception carrying all violation messages when any exist (fail-closed at construction), then builds the credential via `CredentialFactory`; (b) internal constructor `(TokenCredential credential, CloudAuthOptions options, TimeProvider timeProvider, ILogger<ClientCredentialsTokenProvider> logger)` for tests (existing `InternalsVisibleTo` covers it); (c) `GetTokenAsync` implementing the D6 cache: fast path returns the cached token when `TokenFreshness.IsFresh` per `timeProvider.GetUtcNow()` and skew `TimeSpan.FromMinutes(options.RefreshSkewMinutes)`; slow path awaits `SemaphoreSlim(1,1)` with `WaitAsync(cancellationToken)`, double-checks freshness, calls `credential.GetTokenAsync(new TokenRequestContext([options.Scope]), cancellationToken)` exactly once per staleness window, stores and returns the mapped `AppAccessToken`; (d) D7 error contract: `OperationCanceledException` propagates unwrapped; any other credential failure is rethrown as `TokenAcquisitionException` with tenant/client/scope context and inner exception, without serving a stale cached token; (e) logging: `Debug` on refresh with the new `ExpiresOn` only, `Error` on failure with non-secret context only.
  - Acceptance: File exists, <= 500 lines; both constructors present with the exact D4 signatures; no `DateTimeOffset.UtcNow`/`DateTime.UtcNow` anywhere (TimeProvider only); no Azure type appears in any public member signature; the credential call site is single and wrapped per D7. (AC-1, AC-2, AC-3)
- [x] [P3-T3] Create `tests/OpenClaw.Core.Tests/CloudAuth/ClientCredentialsTokenProviderTests.cs` (MSTest + FluentAssertions + Moq + `FakeTimeProvider`) — part 1, construction and deterministic caching: (a) public-constructor fail-closed matrix — invalid options (blank tenant, blank client, neither credential source, both credential sources, bad skew) throw at construction with all violation messages present and no configured value echoed; (b) public-constructor credential-source selection — certificate-only options and secret-only options both construct successfully (construction behavior only, no token call, per spec D4); (c) success — first `GetTokenAsync` via the internal constructor with a mocked `TokenCredential` returns the mapped token/expiry; (d) cache-hit — a second call before `ExpiresOn - skew` returns the cached token with exactly one credential invocation; (e) expiry-refresh — advancing `FakeTimeProvider` past `ExpiresOn - skew` triggers exactly one new credential call; (f) skew boundary — at exactly `ExpiresOn - skew` a refresh occurs, one tick earlier it does not; (g) default skew is 5 minutes when unspecified.
  - Acceptance: File exists, <= 500 lines; scenarios (a)–(g) each individually asserted with credential-invocation counts verified via Moq; all time movement via `FakeTimeProvider` (no sleeps, no wall clock); fake-only token constants. (AC-2, AC-3, AC-4)
- [x] [P3-T4] Extend `tests/OpenClaw.Core.Tests/CloudAuth/ClientCredentialsTokenProviderTests.cs` — part 2, concurrency, cancellation, and failure propagation: (a) single-flight — N concurrent callers (e.g., N=8) with a stale cache produce exactly one credential call, coordinated by a `TaskCompletionSource`-gated mocked credential (no timing); all callers receive the refreshed token; (b) cancellation before the semaphore wait surfaces `OperationCanceledException` unwrapped and leaves the cache unchanged; (c) cancellation during the credential call surfaces `OperationCanceledException` unwrapped and leaves the cache unchanged; (d) failure propagation — a credential failure surfaces `TokenAcquisitionException` with `TenantId`/`ClientId`/`Scope` context and the inner exception preserved; (e) the exception message contains no token, secret, or certificate value; (f) no stale token after failed refresh — with a previously cached now-stale token, a failing refresh throws and does not return the stale token; (g) recovery — a subsequent successful credential call after a failure returns a fresh token.
  - Acceptance: Same file, still <= 500 lines total (if the cap would be exceeded, split part 2 into `ClientCredentialsTokenProviderConcurrencyTests.cs` in the same folder and record the split in the task evidence); scenarios (a)–(g) each individually asserted; concurrency test is deterministic (TCS gating, no `Task.Delay`/`Thread.Sleep`). (AC-2, AC-3, AC-4)

### Phase 4 — DI Extension and Architecture-Boundary Suite (Create, Then Test)

- [x] [P4-T1] Create `src/OpenClaw.Core/CloudAuth/CloudAuthServiceCollectionExtensions.cs` declaring `public static class CloudAuthServiceCollectionExtensions` with `AddCloudAuth(this IServiceCollection services, IConfiguration configuration)` that binds `CloudAuthOptions` from section `OpenClaw:CloudAuth`, applies `.Validate(...)` delegating to `CloudAuthOptionsValidator` and `.ValidateOnStart()`, and registers `IAppTokenProvider` as a singleton `ClientCredentialsTokenProvider`; do not modify `Program.cs` or any existing registration.
  - Acceptance: File exists, <= 500 lines; no Azure type in any public signature; `git diff` shows `src/OpenClaw.Core/Program.cs` and `src/OpenClaw.Core/appsettings.json` unchanged; repo-wide search shows no caller of `AddCloudAuth` outside tests. (AC-1, AC-2, AC-5)
- [x] [P4-T2] Create `tests/OpenClaw.Core.Tests/CloudAuth/CloudAuthServiceCollectionExtensionsTests.cs` (MSTest + FluentAssertions) using `ConfigurationBuilder.AddInMemoryCollection` (no temp files) covering: (a) valid certificate-first configuration binds all `OpenClaw:CloudAuth` keys onto `CloudAuthOptions` (including defaults for omitted `Scope`/`AuthorityHost`/`RefreshSkewMinutes`); (b) `IAppTokenProvider` resolves as a singleton (same instance twice) of type `ClientCredentialsTokenProvider`; (c) invalid configuration (e.g., both credential sources set) causes options validation failure surfaced via `ValidateOnStart` semantics (resolving/starting throws `OptionsValidationException` with the validator's messages and no echoed values).
  - Acceptance: File exists, <= 500 lines; scenarios (a)–(c) individually asserted; the test host registers a `TimeProvider` and logging as needed without touching production wiring; fake-only configuration values. (AC-2, AC-4)
- [x] [P4-T3] Create `tests/OpenClaw.Core.Tests/CloudAuth/CloudAuthArchitectureBoundaryTests.cs` implementing both spec D2 assertions: (a) Agent isolation via NetArchTest — types residing in namespace `OpenClaw.Core.Agent` (including `Runtime`) must not depend on `Azure` or `Microsoft.Identity` (prefix match); (b) contract purity via a reflection walk (same helper pattern as `tests/OpenClaw.Core.Tests/Agent/AgentArchitectureBoundaryTests.cs`) — the public surface of `IAppTokenProvider`, `AppAccessToken`, `CloudAuthOptions`, `TokenAcquisitionException`, `ClientCredentialsTokenProvider`, and `CloudAuthServiceCollectionExtensions` exposes no type from an `Azure.*` or `Microsoft.Identity*` namespace (public members only; the internal test constructor is exempt).
  - Acceptance: File exists at exactly `tests/OpenClaw.Core.Tests/CloudAuth/CloudAuthArchitectureBoundaryTests.cs`, <= 500 lines; both assertions present and passing; `tests/OpenClaw.Core.Tests/Agent/AgentArchitectureBoundaryTests.cs` is unmodified (hash matches `FEATURE/evidence/baseline/baseline-untouched-surfaces.<ts>.md`). (AC-1)
- [x] [P4-T4] Run the new CloudAuth test subset with `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --filter "FullyQualifiedName~OpenClaw.Core.Tests.CloudAuth"` and record the result in `FEATURE/evidence/regression-testing/cloudauth-suite.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` listing total/passed test counts for the CloudAuth suite (all passing, none skipped). (AC-1, AC-2, AC-3, AC-4)

### Phase 5 — Scope Verification, Final QA Loop, and Coverage

- [x] [P5-T1] Verify diff-scope confinement and inert wiring: run `git diff --name-only` against the Phase 0 SHA (must list only `src/OpenClaw.Core/OpenClaw.Core.csproj`, the nine `src/OpenClaw.Core/CloudAuth/*.cs` files, the six `tests/OpenClaw.Core.Tests/CloudAuth/*.cs` files — or seven if the P3-T4 split branch was taken — plus feature docs/evidence); re-hash the untouched surfaces from P0-T5 (`Program.cs`, `appsettings.json`, `AgentArchitectureBoundaryTests.cs`, `docker-compose.yml`) and confirm they match baseline; search `src/` to confirm `Azure` appears only under `src/OpenClaw.Core/CloudAuth/` plus the csproj, and `AddCloudAuth` has no production caller; verify every new file is <= 500 lines (line counts listed); scan the six test files to confirm no realistic secret material (only clearly fake constants); record all checks in `FEATURE/evidence/other/scope-and-inertness-check.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, the exact commands, `EXIT_CODE:` per command, and `Output Summary:` confirming diff confinement, baseline-matching hashes for all four untouched surfaces, Azure containment, zero `AddCloudAuth` production callers, all file line counts <= 500, and fake-only test data. (AC-1, AC-5)

> Loop rule: if any task P5-T2..P5-T4 fails or changes files, restart from P5-T2 and repeat until all three complete cleanly in one pass. Each artifact records the final clean-pass run.

- [x] [P5-T2] Run the formatting stage — `csharpier format .` followed by `csharpier check .` — and record the final clean-pass result in `FEATURE/evidence/qa-gates/final-qa-format.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, both commands, `EXIT_CODE: 0` for `csharpier check .`, and `Output Summary:`; if `csharpier format .` changed any file, the loop restarted and the artifact reflects the final pass. (AC-5)
- [x] [P5-T3] Run the lint/type-check stage — `dotnet build OpenClaw.MailBridge.sln` (analyzers + nullable) — and record the final clean-pass result in `FEATURE/evidence/qa-gates/final-qa-build.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` including error/warning counts (0 errors). (AC-5)
- [x] [P5-T4] Run the full test stage in coverage mode — `dotnet test OpenClaw.MailBridge.sln --settings mailbridge.runsettings --collect:"XPlat Code Coverage"` (this suite includes the new and existing architecture-boundary tests) — storing raw TRX/Cobertura under `artifacts/csharp/`, and record the final clean-pass result in `FEATURE/evidence/qa-gates/final-qa-test-coverage.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, `Command:`, `EXIT_CODE: 0`, and `Output Summary:` containing pass/fail test counts and numeric post-change line-coverage and branch-coverage percents (no placeholders); artifact names the raw Cobertura file path under `artifacts/csharp/`. (AC-5)
- [x] [P5-T5] Verify coverage thresholds and no-regression by comparing the Phase 0 baseline Cobertura against the Phase 5 post-change Cobertura: report baseline line/branch percent, post-change line/branch percent, and per-file new-code coverage for each of the nine `src/OpenClaw.Core/CloudAuth/*.cs` files (every added production line covered; `IAppTokenProvider.cs` may report as interface-only per the C# coverage clarification); record the comparison in `FEATURE/evidence/qa-gates/coverage-comparison.<ts>.md`.
  - Acceptance: Artifact exists with `Timestamp:`, the comparison inputs (both Cobertura paths), and numeric values showing post-change line coverage >= 85%, branch coverage >= 75%, no reduction relative to baseline for changed lines, and covered new production code per file; if any threshold fails or a required numeric value is unavailable, outcome is remediation-required, not PASS. (AC-5)
- [x] [P5-T6] Update the acceptance-criteria checkboxes in `FEATURE/spec.md`, `FEATURE/user-story.md`, and `FEATURE/issue.md`, checking each of the five AC items only where a Phase 0–5 evidence artifact substantiates it, and cite the artifact path next to each checked item.
  - Acceptance: Every checked AC in the three documents names at least one existing evidence artifact under `FEATURE/evidence/`; no AC is checked without evidence.

## Test Plan

- Unit (new, all under `tests/OpenClaw.Core.Tests/CloudAuth/`, MSTest + FluentAssertions):
  - `AppAccessTokenTests.cs` — redacting `ToString()`, interpolation non-leak, value semantics.
  - `CloudAuthOptionsValidatorTests.cs` — full fail-closed matrix (cert-only OK, secret-only OK, both rejected, neither rejected, blank tenant/client, malformed scope/authority, out-of-range skew, multi-violation aggregation, no value echo) + CsCheck property (T1).
  - `TokenFreshnessTests.cs` — boundary examples (fresh one tick before `ExpiresOn - skew`, stale at boundary, null stale) + CsCheck monotonicity/definitional property (T1).
  - `ClientCredentialsTokenProviderTests.cs` — Moq over `Azure.Core.TokenCredential` via the internal constructor: construction fail-closed, credential-source selection, success, cache-hit, expiry-refresh, skew boundary, default-skew, single-flight (TCS-gated, N concurrent -> 1 credential call), cancellation unwrapped (pre-semaphore and in-call), `TokenAcquisitionException` wrapping with context and inner, no secret in messages, no stale token after failure, recovery after failure — all time via `FakeTimeProvider`.
  - `CloudAuthServiceCollectionExtensionsTests.cs` — in-memory configuration binding of `OpenClaw:CloudAuth` (including defaults), singleton resolution, `ValidateOnStart`-surfaced failure on invalid config.
- Architecture (new): `CloudAuthArchitectureBoundaryTests.cs` — Agent namespace Azure/MSAL-free (NetArchTest); CloudAuth public surface Azure-free (reflection walk).
- Integration / live: none — no live Entra calls anywhere; live token exchange is post-provisioning and runbook-covered (spec D9, F11 handoff package `docs/features/active/2026-07-02-exchange-rbac-scripts-111/runbooks/exchange-rbac-setup.runbook.md` Step 7); no new `human_interaction` requirement.
- Coverage evidence:
  - Baseline: `FEATURE/evidence/baseline/baseline-test-coverage.<ts>.md` (raw Cobertura in `artifacts/csharp/`)
  - Post-change: `FEATURE/evidence/qa-gates/final-qa-test-coverage.<ts>.md` (raw Cobertura in `artifacts/csharp/`)
  - Comparison: `FEATURE/evidence/qa-gates/coverage-comparison.<ts>.md`

## Open Questions / Notes

- **Expect-fail rationale (required statement):** no `[expect-fail]` tasks exist in this plan. Every test compiles against production types created earlier in the same plan (create-then-test ordering per phase); no test targets a pre-existing type whose behavior changes, so a fail-before run is structurally impossible and no fail-before evidence obligation arises from any AC.
- **Azure.Identity version pinning:** the repo has no `Directory.Packages.props`; versions are pinned inline in each csproj (verified). P1-T1 pins the exact latest stable version at implementation time and P1-T3 records it in `spec.md`.
- **Test framework reality:** `.claude/rules/csharp.md` prescribes xUnit + NSubstitute, but `tests/OpenClaw.Core.Tests/` uses MSTest + FluentAssertions + Moq; new tests follow the repository reality per spec Constraints & Risks (established precedent in prior features).
- **Mutation testing (T1):** runs in the pre-merge/nightly pipeline per `.claude/rules/quality-tiers.md`, not in this per-commit plan; the pure `TokenFreshness.IsFresh` and `CloudAuthOptionsValidator.Validate` designs (spec D5/D6) exist partly to keep mutation score >= 75% achievable there.
- **500-line contingency:** P3-T4 carries an explicit split branch (`ClientCredentialsTokenProviderConcurrencyTests.cs`) if the provider test file would exceed the cap; P5-T1's scope check accepts either shape and records which was taken.
