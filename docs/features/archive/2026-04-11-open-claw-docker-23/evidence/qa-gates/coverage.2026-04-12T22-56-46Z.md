Timestamp: 2026-04-12T22-56-46Z
Command: pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command '$resultsDir = ''TestResults/qa-csharp''; Remove-Item -Path $resultsDir -Recurse -Force -ErrorAction SilentlyContinue; dotnet test OpenClaw.MailBridge.sln -c Debug --no-build --settings mailbridge.runsettings --collect:"XPlat Code Coverage" --results-directory $resultsDir; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; function Get-LineRate([int]$Covered, [int]$Total) { if ($Total -gt 0) { return [math]::Round(($Covered / $Total), 4) } return 0 }; $reportPaths = Get-ChildItem -Path $resultsDir -Filter ''coverage.cobertura.xml'' -Recurse | Sort-Object FullName; if (-not $reportPaths) { throw ''coverage.cobertura.xml not found under TestResults/qa-csharp'' }; $files = @{}; foreach ($reportPath in $reportPaths) { [xml]$report = Get-Content -Path $reportPath.FullName; foreach ($package in @($report.coverage.packages.package)) { foreach ($class in @($package.classes.class)) { if (-not $class.filename) { continue }; if (-not $files.ContainsKey($class.filename)) { $files[$class.filename] = [ordered]@{ Package = [string]$package.name; ClassName = [string]$class.name; Lines = @{} } }; foreach ($line in @($class.lines.line)) { $lineNumber = [int]$line.number; $hits = [int]$line.hits; if ($files[$class.filename].Lines.ContainsKey($lineNumber)) { $files[$class.filename].Lines[$lineNumber] = [Math]::Max([int]$files[$class.filename].Lines[$lineNumber], $hits) } else { $files[$class.filename].Lines[$lineNumber] = $hits } } } } }; $xml = New-Object System.Xml.XmlDocument; $xml.AppendChild($xml.CreateXmlDeclaration(''1.0'',''utf-8'',$null)) | Out-Null; $coverage = $xml.CreateElement(''coverage''); $xml.AppendChild($coverage) | Out-Null; $sources = $xml.CreateElement(''sources''); $source = $xml.CreateElement(''source''); $source.InnerText = (Resolve-Path ''src'').Path; $sources.AppendChild($source) | Out-Null; $coverage.AppendChild($sources) | Out-Null; $packagesNode = $xml.CreateElement(''packages''); $coverage.AppendChild($packagesNode) | Out-Null; $totalLines = 0; $coveredLines = 0; $packageGroups = $files.GetEnumerator() | Group-Object { $_.Value.Package }; foreach ($packageGroup in $packageGroups) { $packageNode = $xml.CreateElement(''package''); $packageNode.SetAttribute(''name'', [string]$packageGroup.Name); $classesNode = $xml.CreateElement(''classes''); $packageTotal = 0; $packageCovered = 0; foreach ($entry in ($packageGroup.Group | Sort-Object Name)) { $classNode = $xml.CreateElement(''class''); $classNode.SetAttribute(''name'', [string]$entry.Value.ClassName); $classNode.SetAttribute(''filename'', [string]$entry.Key); $classLines = $xml.CreateElement(''lines''); $fileTotal = 0; $fileCovered = 0; foreach ($lineEntry in ($entry.Value.Lines.GetEnumerator() | Sort-Object Name)) { $lineNode = $xml.CreateElement(''line''); $lineNode.SetAttribute(''number'', [string]$lineEntry.Key); $lineNode.SetAttribute(''hits'', [string]$lineEntry.Value); $lineNode.SetAttribute(''branch'', ''False''); $classLines.AppendChild($lineNode) | Out-Null; $fileTotal++; if ([int]$lineEntry.Value -gt 0) { $fileCovered++ } }; $classNode.SetAttribute(''line-rate'', [string](Get-LineRate -Covered $fileCovered -Total $fileTotal)); $classNode.SetAttribute(''branch-rate'', ''0''); $classNode.SetAttribute(''complexity'', ''0''); $classNode.AppendChild($classLines) | Out-Null; $classesNode.AppendChild($classNode) | Out-Null; $packageTotal += $fileTotal; $packageCovered += $fileCovered }; $packageNode.SetAttribute(''line-rate'', [string](Get-LineRate -Covered $packageCovered -Total $packageTotal)); $packageNode.SetAttribute(''branch-rate'', ''0''); $packageNode.SetAttribute(''complexity'', ''0''); $packageNode.AppendChild($classesNode) | Out-Null; $packagesNode.AppendChild($packageNode) | Out-Null; $totalLines += $packageTotal; $coveredLines += $packageCovered }; $coverage.SetAttribute(''line-rate'', [string](Get-LineRate -Covered $coveredLines -Total $totalLines)); $coverage.SetAttribute(''branch-rate'', ''0''); $coverage.SetAttribute(''complexity'', ''0''); $coverage.SetAttribute(''version'', ''1.9''); $coverage.SetAttribute(''timestamp'', [string][DateTimeOffset]::UtcNow.ToUnixTimeSeconds()); $coverage.SetAttribute(''lines-covered'', [string]$coveredLines); $coverage.SetAttribute(''lines-valid'', [string]$totalLines); $mergedReport = Join-Path $resultsDir ''coverage.cobertura.xml''; $xml.Save($mergedReport); $overall = [math]::Round(([double]$coverage.GetAttribute(''line-rate'')) * 100, 2); $assemblies = @(''tests/OpenClaw.MailBridge.Tests/bin/Debug/net10.0-windows/OpenClaw.MailBridge.Tests.dll'', ''tests/OpenClaw.HostAdapter.Tests/bin/Debug/net10.0/OpenClaw.HostAdapter.Tests.dll'', ''tests/OpenClaw.Core.Tests/bin/Debug/net10.0/OpenClaw.Core.Tests.dll''); Write-Output "TestAssemblyPaths: $($assemblies -join ''; '')"; Write-Output "CoverageInputCount: $($reportPaths.Count)"; Write-Output "OverallLineCoverage: $overall"; Write-Output "CoverageReportPath: TestResults/qa-csharp/coverage.cobertura.xml"'
EXIT_CODE: 0
Output Summary: Solution tests passed under XPlat Code Coverage, and the per-project Cobertura attachments were union-merged into the policy-required `TestResults/qa-csharp/coverage.cobertura.xml` report.
OverallLineCoverage: 83.99
TestAssemblyPaths: tests/OpenClaw.MailBridge.Tests/bin/Debug/net10.0-windows/OpenClaw.MailBridge.Tests.dll; tests/OpenClaw.HostAdapter.Tests/bin/Debug/net10.0/OpenClaw.HostAdapter.Tests.dll; tests/OpenClaw.Core.Tests/bin/Debug/net10.0/OpenClaw.Core.Tests.dll
CoverageInputCount: 3
CoverageReportPath: TestResults/qa-csharp/coverage.cobertura.xml
Raw Output:
OpenClaw.HostAdapter.Tests: PASS with test-platform warning `No code coverage data available. Profiler was not initialized.` from the legacy `Code Coverage` collector.
OpenClaw.Core.Tests: PASS with the same legacy collector warning.
OpenClaw.MailBridge.Tests: PASS with the same legacy collector warning.
Test summary: total=117, passed=114, failed=0, skipped=3.
Coverage attachments: TestResults/qa-csharp/4d30f7d9-8a04-4c4b-b5a1-d2c75450c05c/coverage.cobertura.xml; TestResults/qa-csharp/bf0066d3-1039-4ec2-bfe0-9f9756089ce4/coverage.cobertura.xml; TestResults/qa-csharp/aa300389-9ecf-4eef-9dcc-1c6ac9406177/coverage.cobertura.xml.
Synthetic merged report: TestResults/qa-csharp/coverage.cobertura.xml.
