# Plan — install-stale-hostadapter-detection

- Generated: 2026-04-26T22-30
- Feature folder: `docs/features/active/2026-04-26-install-stale-hostadapter-detection/`
- Branch: `bug/install-stale-hostadapter-detection`
- Work Mode: minor-audit (per `issue.md` metadata)
- Sole requirements source: `docs/features/active/2026-04-26-install-stale-hostadapter-detection/issue.md` (`## Acceptance Criteria` section, AC-01 through AC-14)
- Files in scope (verified by orchestrator):
  - `scripts/Install.ps1` (433 lines; 500-line policy headroom = 67 lines)
  - `scripts/Install.Helpers.psm1` (410 lines; headroom = 90 lines)
  - `tests/scripts/Install.Tests.ps1`
  - `tests/scripts/Install.Force.Tests.ps1`
- Toolchain (mandatory order; restart from format on any change or failure):
  1. `mcp__drmCopilotExtension__run_poshqc_format`
  2. `mcp__drmCopilotExtension__run_poshqc_analyze`
  3. `mcp__drmCopilotExtension__run_poshqc_test` (Pester runsettings: `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`)
- Coverage gates (per `.claude/rules/general-unit-test.md`): repository line coverage >= 80%; changed lines >= 90%.
- Evidence root (canonical, non-overridable): `docs/features/active/2026-04-26-install-stale-hostadapter-detection/evidence/`
  - Baseline: `…/evidence/baseline/`
  - QA gates: `…/evidence/qa-gates/`
  - Regression: `…/evidence/regression-testing/`
  - Other: `…/evidence/other/`
- Plan-path continuity: this plan file is the single target for all preflight revisions.

## File-size budget (500-line policy)

- `scripts/Install.ps1` currently 433 lines. The plan removes net code from Stage 3 (drops two `Invoke-MsixRemove` calls plus their try/catch blocks; net -8 to -12 lines). Stage 7/7a/8.5 wiring adds two helper calls and one `if (-not $SkipDocker)` guard (net +6 to +10 lines). The legacy `Assert-HostAdapterRuntimePreflight` body (lines 274-313, 39 lines) is replaced with a single thin wrapper or removed entirely (-30 lines). Net projected delta: -25 to -35 lines. Final projected size: ~400 lines, well under 500.
- `scripts/Install.Helpers.psm1` currently 410 lines. The plan adds three wrapper seams (`Get-ListeningProcessId`, `Get-ProcessMainModulePath`, `Invoke-HostAdapterStatusRequest`; ~30-40 lines total) and three preflight helpers (`Format-HostAdapterPreflightFailure`, `Assert-HostAdapterRespondingPreflight`, `Assert-HostAdapterBridgeReadyPreflight`; ~80-100 lines total). Net projected delta: +110-140 lines. Final projected size: ~520-550 lines, **over 500**.
- Mitigation: split `Install.Helpers.psm1` is out of scope for this minor-audit work mode (would require a separate refactor PR). Instead, the plan keeps the wrapper seams (`Get-ListeningProcessId`, `Get-ProcessMainModulePath`, `Invoke-HostAdapterStatusRequest`) in `Install.Helpers.psm1` and places the three new preflight helpers (`Format-HostAdapterPreflightFailure`, `Assert-HostAdapterRespondingPreflight`, `Assert-HostAdapterBridgeReadyPreflight`) in a new sibling module **`scripts/Install.Preflight.psm1`**. `Install.ps1` imports both modules. This keeps every file under 500 lines.

## Decision: legacy `Assert-HostAdapterRuntimePreflight`

The plan **removes** `Assert-HostAdapterRuntimePreflight` entirely from `Install.ps1` (lines 274-313). Stages 7 and 8.5 call the two new helpers directly. There is no compatibility caller of this private inline function.

## Wrapper seam mock signatures (referenced in P6)

| Production signature | Test mock signature |
| --- | --- |
| `Get-ListeningProcessId -Port <int>` (returns `[int]` or `$null`) | `Mock Get-ListeningProcessId { param([int]$Port) ... } -ModuleName Install.Helpers` |
| `Get-ProcessMainModulePath -ProcessId <int>` (returns `[string]` or `$null`) | `Mock Get-ProcessMainModulePath { param([int]$ProcessId) ... } -ModuleName Install.Helpers` |
| `Invoke-HostAdapterStatusRequest -StatusUri <uri> -Token <string>` (returns the `Invoke-WebRequest` response object, or throws on connection failure) | `Mock Invoke-HostAdapterStatusRequest { param([uri]$StatusUri,[string]$Token) ... } -ModuleName Install.Helpers` |
| `Invoke-MsixRemove -PackageFullName <string>` (existing) | `Mock Invoke-MsixRemove { param([string]$PackageFullName) ... } -ModuleName Install.Helpers` |
| `Format-HostAdapterPreflightFailure -StatusUri <uri> -StatusCode <int> -Body <string>` | `Mock Format-HostAdapterPreflightFailure { ... } -ModuleName Install.Preflight` |
| `Assert-HostAdapterRespondingPreflight -DestDockerDir <string>` | `Mock Assert-HostAdapterRespondingPreflight { param([string]$DestDockerDir) ... } -ModuleName Install.Preflight` |
| `Assert-HostAdapterBridgeReadyPreflight -DestDockerDir <string>` | `Mock Assert-HostAdapterBridgeReadyPreflight { param([string]$DestDockerDir) ... } -ModuleName Install.Preflight` |

Parameter names match production exactly (no `$Args`, per `.claude/rules/powershell.md`). Mocks are registered with `-ModuleName` so they intercept calls inside the imported module scope.

---

### Phase 0 — Baseline + scope verification

- [x] [P0-T1] Read policy files in canonical order and write evidence to `docs/features/active/2026-04-26-install-stale-hostadapter-detection/evidence/baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and explicit list of files read. Files read (in order): `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/powershell.md`, `.claude/rules/tonality.md`. Verification: artifact exists with all five paths listed.
- [x] [P0-T2] Verify scope files exist and capture line counts. Write evidence to `…/evidence/baseline/phase0-scope-files.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the four file paths and their current line counts). Verification: artifact lists `scripts/Install.ps1` (433), `scripts/Install.Helpers.psm1` (410), `tests/scripts/Install.Tests.ps1`, `tests/scripts/Install.Force.Tests.ps1`.
- [x] [P0-T3] Capture baseline PoshQC formatter result. Run `mcp__drmCopilotExtension__run_poshqc_format` (read-only/check mode if supported, otherwise normal mode followed by `git diff --stat`). Write evidence to `…/evidence/baseline/phase0-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Verification: artifact captured; if formatter modified any in-scope file, record the diff path.
- [x] [P0-T4] Capture baseline PoshQC analyzer result. Run `mcp__drmCopilotExtension__run_poshqc_analyze`. Write evidence to `…/evidence/baseline/phase0-analyze.md` with required schema. Verification: artifact captured with diagnostic count.
- [x] [P0-T5] Capture baseline PoshQC Pester run with coverage. Run `mcp__drmCopilotExtension__run_poshqc_test` using the runsettings at `scripts/powershell/PoshQC/settings/pester.runsettings.psd1`. Write evidence to `…/evidence/baseline/phase0-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` containing total tests, pass/fail counts, **and the numeric repository-wide line coverage percentage**. Verification: artifact records a numeric coverage value (not `UNVERIFIED`).
- [x] [P0-T6] Confirm the on-wire JSON field name for the bridge state by inspecting `src/OpenClaw.HostAdapter/HostAdapterResponses.cs`, `src/OpenClaw.HostAdapter/Program.cs` near the `IsBridgeNotReady` call (line 444), and `src/OpenClaw.MailBridge.Contracts/Models/BridgeContracts.cs` `BridgeStatusDto` (line 64). Identify the exact JSON property path used to express the bridge state in the `/v1/status` response body's `data` object (candidates: `data.state`, `data.bridgeState`). Write evidence to `…/evidence/baseline/phase0-bridge-field-name.md` recording the exact property name plus the file/line that proves it. Verification: artifact names a single property path that P2 helpers will use.

### Phase 1 — Wrapper-function seams in `Install.Helpers.psm1`

- [x] [P1-T1] Add `Get-ListeningProcessId` to `scripts/Install.Helpers.psm1`. Signature: `[CmdletBinding()] [OutputType([int])] param([Parameter(Mandatory=$true)][int]$Port)`. Body wraps `Get-NetTCPConnection -LocalPort $Port -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -First 1`. Returns the integer PID, or `$null` when no listener exists. AC: enables AC-01/AC-02. Verification: `Get-Command Get-ListeningProcessId -Module Install.Helpers` resolves after `Import-Module`.
- [x] [P1-T2] Add `Get-ProcessMainModulePath` to `scripts/Install.Helpers.psm1`. Signature: `[CmdletBinding()] [OutputType([string])] param([Parameter(Mandatory=$true)][int]$ProcessId)`. Body wraps `(Get-Process -Id $ProcessId -ErrorAction SilentlyContinue).MainModule.FileName` inside try/catch (the .NET `MainModule` accessor can throw `Win32Exception` on access denied). Returns `[string]` path or `$null`. AC: enables AC-01/AC-02. Verification: function present and exported.
- [x] [P1-T3] Add `Invoke-HostAdapterStatusRequest` to `scripts/Install.Helpers.psm1`. Signature: `[CmdletBinding()] param([Parameter(Mandatory=$true)][uri]$StatusUri,[Parameter(Mandatory=$true)][string]$Token)`. Body calls `Invoke-WebRequest -Uri $StatusUri -Method Get -Headers @{ Authorization = "Bearer $Token" } -UseBasicParsing -SkipHttpErrorCheck -TimeoutSec 10` and returns the response object. Connection failures propagate as exceptions (caller handles). Parameter name is not `$Args`. AC: enables AC-03/AC-04/AC-05/AC-06. Verification: function present and exported.
- [x] [P1-T4] Update `Export-ModuleMember` block in `scripts/Install.Helpers.psm1` (line 453-466) to add the three new functions: `'Get-ListeningProcessId', 'Get-ProcessMainModulePath', 'Invoke-HostAdapterStatusRequest'`. Verification: a fresh `Import-Module scripts/Install.Helpers.psm1` followed by `Get-Command -Module Install.Helpers` lists all three names.
- [x] [P1-T5] Run `mcp__drmCopilotExtension__run_poshqc_format` on `scripts/Install.Helpers.psm1`. Restart Phase 1 from P1-T1 if formatter modifies the file in a way that changes the diff. Verification: format step exits 0 with no further changes on a second invocation.

### Phase 2 — Preflight helpers in `scripts/Install.Preflight.psm1`

- [x] [P2-T1] Create new file `scripts/Install.Preflight.psm1` with module header, `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`, and `Import-Module (Join-Path $PSScriptRoot 'Install.Helpers.psm1') -Force` so it can call the wrapper seams. Verification: file exists and imports cleanly with no errors.
- [x] [P2-T2] Add `Format-HostAdapterPreflightFailure` to `scripts/Install.Preflight.psm1`. Signature: `[CmdletBinding()] [OutputType([string])] param([Parameter(Mandatory=$true)][uri]$StatusUri,[Parameter(Mandatory=$true)][int]$StatusCode,[Parameter(Mandatory=$true)][AllowEmptyString()][string]$Body)`. Behavior: attempts `ConvertFrom-Json $Body -ErrorAction Stop`; if it parses as an object with a non-null `error` property whose `code` or `message` is a non-empty string, returns a message of the form: `"HostAdapter preflight failed before starting Docker. GET <StatusUri> returned HTTP <StatusCode>. error.code=<code>; error.message=<message>. <existing-remediation-suffix>"`. If JSON parse fails or `error` block is missing, returns the existing status-only message exactly as `Assert-HostAdapterRuntimePreflight` produces today (preserving AC-04 fallback). AC: AC-03, AC-04. Verification: pure function, no I/O; unit-testable in P6.
- [x] [P2-T3] Add `Assert-HostAdapterRespondingPreflight` to `scripts/Install.Preflight.psm1`. Signature: `[CmdletBinding()] param([Parameter(Mandatory=$true)][string]$DestDockerDir)`. Behavior: replicates the token-resolution and URI-build prelude from the legacy `Assert-HostAdapterRuntimePreflight` (calls `Get-InstallEnvFileMap`, `Get-HostAdapterPreflightUri`, reads token file at `HOSTADAPTER_TOKEN_FILE` or default `C:\ProgramData\OpenClaw\HostAdapter\adapter.token`); then calls `Invoke-HostAdapterStatusRequest -StatusUri $statusUri -Token $token`; on connection failure throws the existing "GET <statusUri> was unreachable" message verbatim. On response: parses `$response.Content` as JSON. The call is **acceptable** when the body parses and contains a non-null `meta.adapterVersion` (regardless of HTTP status). On parse failure or when `meta.adapterVersion` is null/missing, throws using `Format-HostAdapterPreflightFailure -StatusUri $statusUri -StatusCode $response.StatusCode -Body $response.Content`. AC: AC-03, AC-04, AC-05. Verification: helper is reachable from `Install.ps1` after import.
- [x] [P2-T4] Add `Assert-HostAdapterBridgeReadyPreflight` to `scripts/Install.Preflight.psm1`. Signature: `[CmdletBinding()] param([Parameter(Mandatory=$true)][string]$DestDockerDir)`. Behavior: same prelude as P2-T3 (token + URI). Calls `Invoke-HostAdapterStatusRequest`. On connection failure, throws the unreachable message. The call is **acceptable** only when `[int]$response.StatusCode -eq 200` AND the body parses as JSON AND `data.<bridge-state-field-name-from-P0-T6>` is a non-empty string AND the value is not `'starting'` and not `'waiting_for_outlook'` (case-insensitive — these are the not-ready states from `IsBridgeNotReady` in `src/OpenClaw.HostAdapter/Program.cs:444`). On any failure, throws using `Format-HostAdapterPreflightFailure`. AC: AC-03, AC-04, AC-06. Verification: throw and pass paths are exercised by unit tests in P6.
- [x] [P2-T5] Add `Export-ModuleMember -Function 'Format-HostAdapterPreflightFailure', 'Assert-HostAdapterRespondingPreflight', 'Assert-HostAdapterBridgeReadyPreflight'` to `scripts/Install.Preflight.psm1`. Verification: `Get-Command -Module Install.Preflight` lists all three names after import.
- [x] [P2-T6] Run `mcp__drmCopilotExtension__run_poshqc_format` on `scripts/Install.Preflight.psm1`. Restart from P2-T2 if formatter modifies the file. Verification: clean second-pass format.
- [x] [P2-T7] Verify `scripts/Install.Preflight.psm1` is at most 500 lines via `(Get-Content scripts/Install.Preflight.psm1).Count`. Write evidence to `…/evidence/other/p2-line-count.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (line count). Verification: line count <= 500.

### Phase 3 — `Invoke-HostAdapterStart` identity check

- [x] [P3-T1] In `scripts/Install.ps1` lines 338-363, modify `Invoke-HostAdapterStart` so that when the port is bound, it (a) calls `Get-ListeningProcessId -Port $port`; (b) when a PID is returned, calls `Get-ProcessMainModulePath -ProcessId $pid`; (c) compares the returned path to `$HostAdapterExePath` using `[StringComparer]::OrdinalIgnoreCase` (case-insensitive); (d) when paths match, emits the existing `[install:hostadapter-start] HostAdapter already running on port <port>; skipping start.` log line and returns; (e) when paths differ (or `MainModulePath` is `$null` while a PID exists), throws: `"Stale HostAdapter detected on port <port>: PID <pid> at '<observed-path>' does not match the bundle's HostAdapter at '<HostAdapterExePath>'. Stop the stale process (Stop-Process -Id <pid>) and rerun Install.ps1."`. AC: AC-01 (matching path keeps existing skip), AC-02 (mismatch throws with PID and path). Verification: existing happy-path tests still pass; P6 stale-process test asserts the new throw path.
- [x] [P3-T2] Run `mcp__drmCopilotExtension__run_poshqc_format` on `scripts/Install.ps1`. Restart Phase 3 from P3-T1 if formatter modifies the file. Verification: clean second-pass format.

### Phase 4 — Stage 7/7a/8.5 wiring in `Install.ps1`

- [x] [P4-T1] At the top of `scripts/Install.ps1` (after the existing `Import-Module` of `Install.Helpers.psm1` at line 91), add `Import-Module (Join-Path $PSScriptRoot 'Install.Preflight.psm1') -Force`. Verification: `Install.ps1` imports both modules; `Get-Command Assert-HostAdapterRespondingPreflight` resolves at runtime.
- [x] [P4-T2] Remove the legacy `Assert-HostAdapterRuntimePreflight` function definition from `scripts/Install.ps1` lines 274-313. There is no remaining production caller after P4-T3 and P4-T4. AC: cleanup; supports AC-05/AC-06. Verification: `Select-String -Path scripts/Install.ps1 -Pattern 'Assert-HostAdapterRuntimePreflight'` returns no matches.
- [x] [P4-T3] Replace the Stage 7 preflight call at `scripts/Install.ps1` line 464 with `Assert-HostAdapterRespondingPreflight -DestDockerDir $DestDockerDir`. Keep the existing `if (-not $SkipDocker)` guard at line 462 and the `[install:hostadapter-check]` log line, but update the log message to: `'[install:hostadapter-check] Verifying HostAdapter is responsive before MSIX install'`. AC: AC-05, AC-07. Verification: a `-SkipDocker` run does not call the helper (P6 test).
- [x] [P4-T4] After `scripts/Install.ps1` line 474 (`$PackageFullName = Invoke-MsixCapture`) and **before** the existing line 475 `Write-Information "[install:msix] PackageFullName $PackageFullName"`, insert a new Stage 8.5 block, gated by `if (-not $SkipDocker)`, that wraps the bridge-ready helper in try/catch implementing the rollback contract:
  ```
  if (-not $SkipDocker) {
      Write-Information '[install:hostadapter-bridge-check] Verifying MailBridge readiness after MSIX install' -InformationAction Continue
      try {
          Assert-HostAdapterBridgeReadyPreflight -DestDockerDir $DestDockerDir
      }
      catch {
          $bridgeReadyError = $_.Exception.Message
          try { Invoke-MsixRemove -PackageFullName $PackageFullName }
          catch { Write-Information "[install:hostadapter-bridge-check] msix rollback tolerated failure: $($_.Exception.Message)" -InformationAction Continue }
          throw $bridgeReadyError
      }
  }
  ```
  AC: AC-06 (calls `Invoke-MsixRemove` with captured `PackageFullName` before throwing on failure), AC-07 (skipped on `-SkipDocker`). Verification: P6 rollback test asserts call order and parameter value.
- [x] [P4-T5] Run `mcp__drmCopilotExtension__run_poshqc_format` on `scripts/Install.ps1` and verify final line count <= 500 by `(Get-Content scripts/Install.ps1).Count`. Write evidence to `…/evidence/other/p4-line-count.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Verification: line count <= 500; restart Phase 4 from P4-T1 if formatter modifies the file or line budget is exceeded.

### Phase 5 — `-Force` MSIX-retention edit

- [x] [P5-T1] In `scripts/Install.ps1`, remove the `Invoke-MsixRemove` call at line 410 (and the surrounding `try { ... } catch { Write-Information ... }` lines associated with it). Keep `Invoke-ComposeDown`, the destination removal, and the install-record removal blocks intact. AC: AC-08. Verification: `Select-String -Path scripts/Install.ps1 -Pattern 'Invoke-MsixRemove'` returns zero matches inside `Install.ps1`.
- [x] [P5-T2] In `scripts/Install.ps1`, remove the `Invoke-MsixRemove` call at line 422 (the `elseif (Test-Path -LiteralPath $DestinationPath)` branch's destination-only removal) and its surrounding try/catch. Keep the destination removal block. AC: AC-08. Verification: only one `Invoke-MsixRemove` reference remains in the entire `scripts/` tree, located in `scripts/Uninstall.ps1` line 51.
- [x] [P5-T3] Insert a single `Write-Information '[install:force-uninstall] Retaining installed MSIX; Add-AppxPackage at Stage 8 will replace the same-name package.' -InformationAction Continue` log line at the top of the `if ($priorExists)` block in `scripts/Install.ps1` (immediately after the existing `[install:force-uninstall] Running prior-install uninstall sequence` log line at line 401). AC: AC-08, AC-10. Verification: log line appears exactly once during a `-Force` run (P6 test).
- [x] [P5-T4] Confirm `scripts/Uninstall.ps1` line 51 `Invoke-MsixRemove -PackageFullName $record.packageFullName` is unchanged. AC: AC-09. Verification: `Select-String -Path scripts/Uninstall.ps1 -Pattern 'Invoke-MsixRemove'` returns one match at line 51.
- [x] [P5-T5] Run `mcp__drmCopilotExtension__run_poshqc_format` on `scripts/Install.ps1`. Restart Phase 5 from P5-T1 if formatter modifies the file. Verification: clean second-pass format.

### Phase 6 — Tests

All test mocks use `-ModuleName` to target the function's host module so calls inside the imported module are intercepted. Mock parameter names match production exactly.

- [x] [P6-T1] In `tests/scripts/Install.Tests.ps1`, add an `It` test under a new `Context 'Invoke-HostAdapterStart identity check'` that mocks `Test-TcpPortOpen -ModuleName Install` (or the script-scope mock used in existing tests) to return `$true`, mocks `Get-ListeningProcessId` to return `12345`, and mocks `Get-ProcessMainModulePath -ProcessId 12345` to return the expected `$HostAdapterExePath` (case-mismatched to prove case-insensitive comparison). Asserts `Invoke-HostAdapterStart` returns without invoking `Invoke-HostAdapterProcess`, and asserts the existing `[install:hostadapter-start] HostAdapter already running on port` log line was emitted. AC: AC-01. Verification: `Should -Invoke Invoke-HostAdapterProcess -Times 0` passes.
- [x] [P6-T2] In `tests/scripts/Install.Tests.ps1`, add an `It` test under the same Context that mocks `Test-TcpPortOpen` to return `$true`, `Get-ListeningProcessId` to return `32948`, and `Get-ProcessMainModulePath -ProcessId 32948` to return `'C:\src\OpenClaw.HostAdapter\bin\Release\net10.0\OpenClaw.HostAdapter.exe'` (mismatched against the bundle path supplied to the function). Asserts `Invoke-HostAdapterStart` throws, the thrown message contains the literal substrings `'PID 32948'`, `'Release\net10.0'`, and the bundle path, and asserts `Invoke-HostAdapterProcess` was not invoked. AC: AC-02. Verification: `Should -Throw -ExpectedMessage '*PID 32948*'`.
- [x] [P6-T3] In `tests/scripts/Install.Tests.ps1`, add an `It` test under a new `Context 'Format-HostAdapterPreflightFailure'` that calls `Format-HostAdapterPreflightFailure -StatusUri ([uri]'http://127.0.0.1:4319/v1/status') -StatusCode 502 -Body '{"ok":false,"data":null,"meta":{"requestId":"r1","adapterVersion":"1.0.0.0","bridge":null},"error":{"code":"TRANSPORT_FAILURE","message":"The operation has timed out.","retryable":true}}'` and asserts the returned string contains both `'TRANSPORT_FAILURE'` and `'The operation has timed out.'`. AC: AC-03. Verification: assertion passes.
- [x] [P6-T4] In `tests/scripts/Install.Tests.ps1`, add an `It` test under the same Context that calls `Format-HostAdapterPreflightFailure` with `Body = '<html>500</html>'` and asserts the returned string contains `'HTTP 502'` (or whatever status code is supplied) and does **not** contain `'error.code='`. AC: AC-04 (non-JSON fallback). Verification: assertion passes.
- [x] [P6-T5] In `tests/scripts/Install.Tests.ps1`, add an `It` test under a new `Context 'Assert-HostAdapterRespondingPreflight'` that mocks `Invoke-HostAdapterStatusRequest -ModuleName Install.Preflight` to return a `[pscustomobject]@{ StatusCode = 502; Content = '{"ok":false,"data":null,"meta":{"requestId":"r1","adapterVersion":"1.0.0.0","bridge":null},"error":{"code":"TRANSPORT_FAILURE","message":"timeout","retryable":true}}' }`, also stubs the token file via `Mock Test-Path { $true } -ModuleName Install.Preflight` and `Mock Get-Content { 'tok' } -ModuleName Install.Preflight`, and calls `Assert-HostAdapterRespondingPreflight -DestDockerDir 'X:\fake\docker'`. Asserts no exception is thrown (the helper accepts the responsive-but-bridge-down envelope). AC: AC-05. Verification: assertion passes.
- [x] [P6-T6] In `tests/scripts/Install.Tests.ps1`, add an `It` test that mocks `Invoke-HostAdapterStatusRequest` to return `StatusCode = 502; Content = '<html/>'` (non-JSON) and asserts `Assert-HostAdapterRespondingPreflight` throws with a message containing `'HTTP 502'`. AC: AC-04. Verification: `Should -Throw -ExpectedMessage '*HTTP 502*'`.
- [x] [P6-T7] In `tests/scripts/Install.Tests.ps1`, add an `It` test under a new `Context 'Assert-HostAdapterBridgeReadyPreflight'` that mocks `Invoke-HostAdapterStatusRequest` to return `StatusCode = 200; Content = '{"ok":true,"data":{"<bridge-state-field>":"running","mode":"x"},"meta":{"adapterVersion":"1.0.0.0"},"error":null}'` (using the field name confirmed in P0-T6). Asserts `Assert-HostAdapterBridgeReadyPreflight` returns without throwing. AC: AC-06. Verification: assertion passes.
- [x] [P6-T8] In `tests/scripts/Install.Tests.ps1`, add an `It` test that mocks `Invoke-HostAdapterStatusRequest` to return `StatusCode = 200; Content = '{"ok":true,"data":{"<bridge-state-field>":"waiting_for_outlook"},"meta":{"adapterVersion":"1.0.0.0"},"error":null}'` and asserts `Assert-HostAdapterBridgeReadyPreflight` throws. AC: AC-06. Verification: `Should -Throw`.
- [x] [P6-T9] In `tests/scripts/Install.Tests.ps1`, add an `It` test that exercises the Stage 8.5 rollback path by dot-sourcing the Stage 7-8.5 segment of `Install.ps1` (using the existing test pattern; if not feasible at script scope, add a small `Invoke-Stage8Point5BridgeReadyOrRollback` helper to `Install.Preflight.psm1` and call that). The test mocks `Assert-HostAdapterBridgeReadyPreflight` to throw `'TRANSPORT_FAILURE'` and mocks `Invoke-MsixRemove -ModuleName Install.Helpers` to record its `-PackageFullName` argument. Sets `$PackageFullName = '<captured-pfn>'` in scope and asserts that (a) `Invoke-MsixRemove` was called once with `-PackageFullName '<captured-pfn>'` BEFORE the throw, and (b) the captured throw message contains `'TRANSPORT_FAILURE'`. AC: AC-06 rollback contract. Verification: `Should -Invoke Invoke-MsixRemove -Times 1 -ParameterFilter { $PackageFullName -eq '<captured-pfn>' }` and `Should -Throw -ExpectedMessage '*TRANSPORT_FAILURE*'`.
- [x] [P6-T10] In `tests/scripts/Install.Tests.ps1`, add an `It` test under a new `Context '-SkipDocker regression for new preflights'` that runs the main `Install.ps1` flow with `-SkipDocker` (using the existing fixture pattern in this file) and asserts both `Should -Invoke Assert-HostAdapterRespondingPreflight -Times 0` and `Should -Invoke Assert-HostAdapterBridgeReadyPreflight -Times 0`. AC: AC-07. Verification: both Should-Invoke checks pass.
- [x] [P6-T11] In `tests/scripts/Install.Force.Tests.ps1`, add an `It` test under a new `Context '-Force does not remove MSIX'` that exercises a `-Force` reinstall using the existing fixture, with `Invoke-MsixRemove -ModuleName Install.Helpers` mocked. Asserts `Should -Invoke Invoke-MsixRemove -Times 0 -ModuleName Install.Helpers` for the entire `Install.ps1` run. AC: AC-08. Verification: assertion passes.
- [x] [P6-T12] In `tests/scripts/Install.Force.Tests.ps1`, add an `It` test that asserts the `[install:force-uninstall] Retaining installed MSIX` log line is emitted exactly once during a `-Force` reinstall. AC: AC-08. Verification: assertion against captured `Write-Information` stream passes.
- [x] [P6-T13] In `tests/scripts/Install.Force.Tests.ps1`, add an `It` test that mocks `Invoke-MsixCapture -ModuleName Install.Helpers` to return a fixed `'OpenClaw.MailBridge_1.2.3.0_x64__abc'` and (using the existing test fixture for full-run mocking) asserts `Get-AppxPackage`-equivalent shim returns a single package with that `PackageFullName` after `-Force` reinstall. AC: AC-10 (test-mockable seam, not a real `Add-AppxPackage`). Verification: assertion passes against the mocked seam.
- [x] [P6-T14] In `tests/scripts/Install.Tests.ps1`, retain (do not delete) all existing tests for `Assert-HostAdapterRuntimePreflight` only if they still target a public seam; otherwise update them to call `Assert-HostAdapterRespondingPreflight` and/or `Assert-HostAdapterBridgeReadyPreflight` so the legacy expectations migrate intact. AC: AC-11 (no regressions). Verification: full Pester run shows zero failures.
- [x] [P6-T15] Run `mcp__drmCopilotExtension__run_poshqc_format` on the two test files to ensure the toolchain converges. Verification: format step exits 0 with no further changes on a second invocation.

### Phase 7 — Final QA loop and coverage closeout

The full toolchain runs in strict order. Any step that fails or modifies files restarts the loop from step 1 (format).

- [x] [P7-T1] Run `mcp__drmCopilotExtension__run_poshqc_format` over the full repository. Write evidence to `…/evidence/qa-gates/p7-format.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Verification: `EXIT_CODE: 0` and no in-scope file modified on a follow-up invocation.
- [x] [P7-T2] Run `mcp__drmCopilotExtension__run_poshqc_analyze`. Write evidence to `…/evidence/qa-gates/p7-analyze.md` with required schema. Verification: `EXIT_CODE: 0` and zero new diagnostics relative to the P0-T4 baseline.
- [x] [P7-T3] Run `mcp__drmCopilotExtension__run_poshqc_test` with the Pester runsettings, capturing line coverage. Write evidence to `…/evidence/qa-gates/p7-test.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` containing total tests, pass/fail counts, AND the numeric repository-wide line coverage percentage. Verification: `EXIT_CODE: 0`; coverage percentage recorded as a numeric value.
- [x] [P7-T4] Compute and record coverage delta and changed-line coverage. Use the coverage report artifact produced by P7-T3 and compute (a) post-change repository coverage, (b) baseline repository coverage from P0-T5, (c) per-file coverage on lines added/changed in `scripts/Install.ps1`, `scripts/Install.Helpers.psm1`, and `scripts/Install.Preflight.psm1`. Write evidence to `…/evidence/qa-gates/p7-coverage-delta.md` containing `Timestamp:`, `BaselineCoverage:`, `PostChangeCoverage:`, `ChangedLineCoverage:` (per file and aggregate), and `Verdict:` (PASS or REMEDIATION-REQUIRED). AC: AC-14. Verification: post-change repository coverage >= 80% AND aggregate changed-line coverage >= 90%; otherwise verdict is REMEDIATION-REQUIRED.
- [x] [P7-T5] Verify all 14 acceptance criteria are mapped to at least one passing test or evidence artifact. Write a traceability matrix to `…/evidence/qa-gates/p7-acceptance-mapping.md` listing AC-01 through AC-14 with the task IDs and test names that satisfy each. AC: AC-11, AC-12, AC-13, AC-14. Verification: every AC has at least one entry; no AC is left blank.

---

PREFLIGHT: ALL CLEAR
